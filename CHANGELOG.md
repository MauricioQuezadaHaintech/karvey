# Changelog

Format based on [Keep a Changelog](https://keepachangelog.com/) + human/AI traceability (Karvey policy).

## [Unreleased]

### Wave 2 (`wave2-structural`) — summary for the release block
The cost of the process now scales with the size and risk of the change, and every gate, lane and judge is measured
from the change's own artifacts. Every new check ships **advisory or warn** in this release; no Wave 1 allow turns
into a block (guard table `compat.json`). The version number and date are set at release.

**Check modes** (`plugins/karvey/schemas/check-modes.json`; a project overrides one with `project.json:checks.{id}`):

| Check | Would refuse | This release | 4.0 |
|---|---|---|---|
| `schema.strict` | `lane`, `phase_history`, `skipped` missing; legacy shapes | warn | blocking |
| `gates.merged` | (a default) three human gates instead of seven | granular | merged |
| `release.manifest` | unmapped commit or change without QA in the release manifest | warn | blocking |
| `lane.diff` | the diff exceeds the lane's criteria | warn | warn |
| `coverage.requirements` | requirement without a green test or a `manual` exception | warn | warn |
| `trailer.guard` | commit on a change branch without `Karvey-Change` | off | off |
| `judges.mode` | open Critical/High judge finding at approve | advisory | advisory |
| `security.tools` | a security category not evaluated | advisory | advisory |
| `postdeploy.contract` | deploy without a post-deploy contract | advisory | advisory |

**Upgrade (manual, until the project-upgrade catalogue carries these steps;** declared in
`docs/spec/changes/wave2-structural/upgrade-steps.handoff.json`**):**
- `lane-infer` — run `karvey-state.py validate --all --fix`, read the proposed `lane` of each active change, then
  `--fix --accept-proposed` (from `type`, a generated mockup or the recorded skips; shown as a diff first).
- `approvals-deploy-retire` — the same `--fix` moves a legacy `approvals.deploy` into `deploys[]` when it holds data
  (never an approval), else removes it.
- `branch-flow-mode` — set `project.json:branch_flow.mode`: `trunk` when integration = production (recommended),
  else `env-branches`.
- `knowledge-sync-declare` — if the project uses graphify (`graphify-out/` exists), set `knowledge_sync: graphify`:
  an absent key now means `none`.
- `judges-budget-ignored` — remove `project.json:judges.budget` if present (cost is measured, never capped).
- `evidence-into-changes` — split a shared `docs/test_evidence.md` / `docs/test_plan.md` into
  `docs/spec/changes/{id}/` (the human reviews the split).
- `checks-overrides-known` — keep only `project.json:checks` keys that exist in `check-modes.json`.
- `global-patch-lane` — optionally add the `patch`-lane bullet to your own global instructions (a diff you apply).
- 4.0 only: `strict-schema-ready` (every change passes `validate --strict`) and `manifest-unmapped` (list the
  commits without a trailer before the first blocking release).

**3.13 → 4.0.** 4.0 flips exactly three defaults — `schema.strict`, `gates.merged` and `release.manifest` — and
nothing else. It is proposed only when `karvey-context.py --readiness` shows at least 4 measured changes, with that
report attached, and its approval is its own decision. The checks beyond those three stay warn or advisory in 4.0
until a later decision reads the readiness data. The Wave 1 deprecated shims are removed in the same release.

### Added
- E1.F1.T1 — real hook payloads captured headless from CLI 2.1.281 into `plugins/karvey/tests/fixtures/payloads/` (9 sanitised fixtures); F-02 closed with a result per assumption A-1..A-10, A-8 nuance logged as F-04. Why: freeze the parser and guard tables on the real contract, not on docs.
- E1.F2.T1 — `karvey_lib` package: exit codes, `--json` envelope and `defaults.json` (8 h rotation, 120 min marker, 7 days stalled, ±30 % over 3 changes). Why: one contract and one place for the D-06/D-07 values (REQ-W1-049).
- E1.F2.T2 — `atomicio`: BOM-tolerant read, format-preserving atomic write, `O_EXCL` lock (stale after 30 s) and compare-and-swap (exit 3). Why: concurrent sessions must never half-write or silently overwrite a `spec.json`.
- E1.F2.T3 — `schema_lite`: stdlib JSON-Schema subset validator with `x-karvey-severity: warning` and `x-karvey-format: datetime-tz`; unsupported keywords are an error. Why: validate `spec.json`/`project.json` without pip dependencies (REQ-W1-002).
- E1.F2.T4 — `project` (root discovery bounded by the git top level, the active-change rule, the reviewed `origin/<production>` config read, state dir under `--git-common-dir`) and `audit` (JSONL, 0600, 1 MB rotation, no tokens). Why: hooks and tools must agree on which project and change they act on, and weakening settings must come from the reviewed line.
- E1.F2.T5 — `schemas/spec.schema.json` and `schemas/project.schema.json` (architecture §2.2/§2.6). Why: one machine-readable contract for both files, so validation, migration and the linter read the same rules (REQ-W1-002).
- E1.F2.T6 — `schemas/state-machine.json` (the phase graph as data) and `schemas/legacy-phase-map.json` (exact / proposed / unmappable tiers). Why: the tool, the rule text and the linter read one graph instead of 14 hand-kept phase lists (REQ-W1-005, REQ-W1-009).
- E1.F3.T1 — `karvey-state.py validate [PATH…|--all] [--strict] [--json]`: schema + §2.3 semantic checks (gates past unapproved phases, history gaps and order, embedded skips, legacy phase / history / `approvals: null` as warnings in advisory mode, errors in strict), exit 4 for a newer `schema_version`. Why: one validator every skill, hook and CI job calls (REQ-W1-001, REQ-W1-003, REQ-W1-109).
- E1.F3.T2 — `validate --fix [--dry-run] [--accept-proposed]`: exact-tier phases, proposed tier only with the flag, `gates_skipped` and embedded skips → `skipped`, `management` string/`none` and `clickup.backlog_list_id` → `management.location`, `approvals: null` → `{}`, hand-written `{from,to,at}` history normalised; the unified diff is printed first; unmappable (`shipping`, `iterate`, null) or unmigratable (`42`) values exit 3 and write nothing; idempotent. Why: migrate legacy files without inventing or flipping an approval (REQ-W1-009, REQ-W1-010).
- E1.F3.T3 — `karvey-state.py next <change>` (status `in-progress|awaiting-approval|ready|invalid`, next phase passing skipped ones, preconditions, blockers, skill; an invalid file reports its errors instead of guessing) and `active` (the §5 rule shared with hooks and dashboard). Why: the orchestrator answers "what next" from the graph, not from a second phase table (REQ-W1-005, REQ-W1-045).
- E1.F3.T5 — `karvey_lib/approval.py` marker store and release ledger under `<git-common-dir>/karvey/{approvals,ledger}` (0700/0600): `v:1` markers with kind, scope, repo, TTL clamped 5..1440, `consumed_at`, prompt hash and 80-character excerpt; empty or hand-written files ignored and audited; `KARVEY_COMPAT_MARKER` written as well (D-11); advisory transcript cross-check that hashes only human `user` lines, never `tool_result` (F-04). Why: the marker must be scoped, expiring and unforgeable by a one-command shortcut (REQ-W1-016, REQ-W1-018, REQ-W1-023).
- E1.F3.T4 — `karvey-state.py advance | generated | skip | reopen`: forward edges only (skipped phases passed through), refusals that name the unmet phase and leave the file byte-identical, `phase_history` closed with `exited_at` and appended (never rewritten), `deployed` only with ledger prod + pipeline run + post-deploy check, `archived` only from `deployed` with a human `approvals.prod`, `reopen` moving downstream approvals to `revision_history`; exact-tier legacy phases mapped in memory; lock + compare-and-swap (exit 3). Why: every phase write goes through one tool that enforces the graph (REQ-W1-004, 007, 008, 011).
- E1.F3.T6 — `karvey-state.py approve` (by/role/ref required, `--date` with a zone; prod: role human only, a prod-kind marker required (D-10), written to the release ledger and never to spec.json (D-03) unless `--write-spec`, which copies the ledger or a recorded D-NN / PR URL on the archive branch), `check-prod` (ledger + evidence; a hand-edited `approvals.prod` without a ledger entry is "not recorded through the state tool"), and marker consumption on `advance`. Why: the one non-delegable gate needs a record the agent cannot produce by editing a file (REQ-W1-006, 016, 023, 031, 032).
- E1.F4.T1 — `karvey_lib/hookio.py`: tolerant payload parser built on the captured CLI 2.1.281 payloads (prompt → `user_prompt` → `message.content`; `file_path` → `notebook_path`; cwd → `$CLAUDE_PROJECT_DIR` → `$PWD`; non-JSON stdin → `ok: false` for the fail modes) and `norm_path` for `C:\…`, Git-Bash `/c/…` and `\\wsl$\…`. Why: every guard reads the same parse of the harness contract (REQ-W1-017, REQ-W1-028, REQ-W1-050).
- E1.F4.T2 — `karvey_lib/shellparse.py`: segments on `; && || | |& &` newlines and `( )` with quotes, comments and heredoc bodies respected; assignments and `command/builtin/exec/time/nohup/env/sudo` (plus `nice/timeout/stdbuf`) stripped; `bash -c`, `eval`, `$(…)` and backticks recursed to depth 3; `cd` chains; git `-C/-c/--git-dir/--work-tree/--no-pager` and `GIT_DIR`; redirections recorded as `(op, fd, target)`; unbalanced input marked `unparsed`. Why: guards must classify what actually runs and where, not the raw string (REQ-W1-014, REQ-W1-020).
- E1.F4.T3 — dispatcher `hooks/karvey-hook.sh` (python3 → python 3 → `py -3`; bash-only fail modes without python) and `karvey_lib/karvey_hooks.py` (one process per event, guard registry in the §1.3 order with fail modes, first block wins, audit of every block, internal time budget below each `hooks.json` timeout); `hooks.json` gains UserPromptSubmit, PreToolUse (Bash; Edit|Write|MultiEdit|NotebookEdit) and PostToolUse, all double-quoting `"${CLAUDE_PLUGIN_ROOT}"`; the guards are allow-stubs until batch 3. `test-hooks.sh` now runs every declared command as written, also from a plugin path with spaces (BUG-18 generalised). Why: one parse, one lookup and one audited decision per tool call (REQ-W1-024, REQ-W1-026, REQ-W1-029).
- E1.F4.T4 — guard-table runner `tests/hooks/run_tables.py` (`--tag`, `--only`, `--junit`): each case builds a throw-away git repo with a bare origin, project/spec files, ledger and markers, fakes `gh`/`az`/`glab` with stubs on `PATH`, feeds the payload to the real dispatcher and asserts decision, stdout/stderr substrings, `marker_created` and < 1 s per case; cases tagged `nopy` run again with `PATH` stripped of python. `tables/smoke.json` holds 9 cases (allow, block, no-python). Why: guard behaviour is proven by tables in CI, not by prose (REQ-W1-030).
- E1.F5.T1 — `guards.protect_paths`, always on (also outside Karvey projects and with plan-gate off): blocks any Bash segment or Edit/Write that names the approval markers, the release ledger, the `.git/karvey` state dir or `$KARVEY_COMPAT_MARKER` (after `~`/`$HOME`/`$(git rev-parse …)` expansion and quote removal; read-only commands such as `cat`/`tail` stay allowed) and any write under the plugin root; bash-only fallback without python; `tables/protect-paths.json` (17 cases, 12 nopy). A non-JSON payload now blocks Bash calls (protect-paths fails closed). Why: the agent must never be able to create or edit the evidence the gates rely on (REQ-W1-018, D-01).
- E1.F5.T2 — approval hook (UserPromptSubmit, `guards.approval_hook` + the vocabulary half of `approval.py`, defaults in `karvey_lib/vocabulary.json`): NFKD/accent/case normalisation; fenced and inline code, `>` lines, `"…"`/`“…”`/`«…»` and lines over 200 characters stripped; questions and negation terms win; approval within the first 12 words (anywhere if ≤ 120 chars; > 20 KB prompts scan 2 KB); `kind: prod` only with an approval word AND a production word (D-10); scope = named change → active change → `_project`; TTL and vocabulary from the reviewed line only; prints `[karvey] approval recorded (<kind>, <scope>, expires hh:mm)`; also writes `$KARVEY_COMPAT_MARKER` when set (D-11, never set by the plugin); fails open with an audit line. `tables/approval.json` (39 cases), `test_approval_vocab.py` (12). Why: the approval is an act of the human, never something the agent can produce (REQ-W1-016, 017, 019; D-01).
- E1.F5.T3 — plan-gate (`guards.plan_gate`, opt-in via `enforcement.plan_gate_hook` on in the working copy OR on `origin/{production}`): Edit/Write/MultiEdit/NotebookEdit and the §3.4 classes (redirection and `tee` writes; recursive `rm`, `git clean`, `reset --hard`, `checkout --`, `restore`, `push --force`, `sed -i`, `perl -i`, `truncate`, `find -delete/-exec rm`, SQL `DROP`/`TRUNCATE`/`DELETE` without `WHERE` in SQL clients, `terraform destroy`, `az`/`gcloud`/`kubectl … delete`, `aws … delete-*/rm/rb`) need a live, well-formed marker for (repo, active change) or (repo, `_project`) within the reviewed TTL; `2>/dev/null`, `2>&1`, `>&2`, `&>/dev/null` and quoted `>` are not writes; unparsable input falls back to a conservative regex; the block names the marker state and never tells the agent to create anything; bash-only fallback blocks when enabled. `tables/plan-gate.json` (63 cases, 13 nopy). Why: H-10 — `ls 2>/dev/null` was blocked while `git clean -fdx`, `find -delete` and `sed -i` passed (REQ-W1-014, 015, 016).
- E1.F5.T4 — git-flow (`guards.git_flow`, opt-in via `enforcement.git_flow_hook`): resolves the **target repository per segment** (`cd` chains, `git -C`, `GIT_DIR`/`--git-dir`, `bash -c`, git aliases incl. `!` shell aliases) and its own `branch_flow`; whole-name branch match; blocks commit/amend/cherry-pick/revert/am on integration or production, merges on production (and on integration in trunk), bare push on production, pushes to production in any refspec form (`HEAD:P`, `refs/heads/P`, `:P`, `+…`, `--all`, `--mirror`), force/delete of integration, and a push into integration from anything but local integration; allows `git push origin dev` from a feature branch (Q-A9, D-09) and `master-notes`; blocks the manual deploy commands; unresolvable target or refspec → block naming the rewrite; bash-only fallback. `tables/git-flow.json` (59 cases). Why: H-12 — `git -C <repo-on-master> commit`, bare `git push` on master and `feature/x:dev` passed; `master-notes` was a false positive (REQ-W1-020, 021, 022, 035).
- E1.F5.T5 — prod-gate (`guards.prod_gate`, ON by default, D-02): candidates `gh pr merge` (any selector/flags), `gh api …/pulls/<N>/merge` (GraphQL `mergePullRequest`/`enablePullRequestAutoMerge` → block, fail closed), `az repos pr update --status completed|--auto-complete true`, `glab mr merge` and `git push` to the production set, also inside `bash -c`, env prefixes, wrappers, absolute paths and `$(…)`; the production set is `branch_flow.production` ∪ `origin/HEAD` ∪ `master`/`main` on the remote, minus integration (F-12); the PR base comes from `gh|az|glab … view` within a 6 s budget; the released change from the head branch, then a `[Deploy] <id>` title, then the only `deploying` change; allowed only when `check-prod` confirms the ledger approval, with `[karvey] prod-gate ALLOW change=… by=… ref=…` or `[karvey] prod-gate BLOCK change=… missing=… reason=…`; a PR into integration passes silently; non-Karvey repos are inert. `tables/prod-gate.json` part 1 (35 cases). Why: H-12/H-15 — `gh pr merge 12 --merge --admin` merged to production with no approval on record (REQ-W1-023, 024, 035).
- E1.F5.T6 — prod-gate switch-off and fail-closed reasons: off only when `enforcement.prod_gate_hook` is `false` in the working copy **and** on `origin/{production}` (prints `[karvey] prod-gate DISABLED for this project (project.json)`); a missing key, `"no"` or a working-copy-only `false` keeps it on; corrupt or invalid `spec.json`, a corrupt ledger, an unreadable `project.json`, an unparsable command with a merge verb and a hand-edited `approvals.prod` without a ledger entry all block with the reason; `changes/` without `project.json` stays gated and says so; other `deploying` changes without approval are listed in a warning; one `audit.log` line per allow, block and disabled decision (approver, ref, change); bash-only classifier without python (every PR/MR merge and any push naming production or without a refspec block; the switch-off is honoured by grep). `tables/prod-gate.json` now 53 cases (13 nopy). Why: the one non-delegable gate must fail closed and leave a record (REQ-W1-024..027, D-02).
- E1.F5.T7 — PostToolUse: the spec-write validator checks every written `docs/spec/**/spec.json` and `docs/spec/project.json` in-process with the state tool (schema + semantic checks, advisory/strict from `schema_mode`) and exits 2 with the violations on stderr (A-4); valid files and other paths are silent; without python it prints `[karvey] spec.json not validated: …`; the pending-sync recorder appends every `docs/spec/**` write to `docs/spec/.graph-pending` (sorted, deduped, LF, CAS write), never the pending file or `graphify-out/**`, and still runs after a validator block. `tables/spec-write.json` (14 cases). Why: a hand-written `phase: "qa-approved"` must surface at write time, and archive must know which spec files changed since the last sync (REQ-W1-028, REQ-W1-063).
- E1.F6.T1 — SessionStart ported to `karvey_hooks.py session <startup|resume>` (the bash script delegates when python is present and keeps its code as the degraded path): the 3.11.4 profile resolution (BUG-19 layouts) and live-state check (`resolve()` BUG-20, `is_repo()` via `git rev-parse --git-dir` BUG-21) moved unchanged into `karvey_lib/livestate.py`; the active change comes from the §5 rule (archive/ and `IMPLEMENTED` excluded, H-08), compact manifest XOR full (H-09), open board rows ≤ 40 with `… N more open rows in <path>`, handoff ≤ 6 KB cut at a line with `… truncated (X KB of Y KB) — full file: <path>`, `measured:false` repos reported as not measured; the text goes out as `hookSpecificOutput.additionalContext`. Why: the hook restored archived changes and injected unbounded, duplicated context (REQ-W1-045, 046, 047).
- E1.F6.T2 — settings notice only on `startup` (hooks.json now declares two SessionStart entries, `startup` and `resume|compact|clear`, passing the source as an argument, both double-quoted); a Karvey project is found walking up no further than the git top level; settings count as present when the working copy **or** `project.json` on `origin/{integration}` (local ref, no fetch; `origin/HEAD` as fallback) has non-empty `notifications` and `management`; a `management` string is present-but-legacy and the line says `run karvey-state.py validate --fix`; without python the degraded line says the settings could not be read. Why: the notice fired on every resume and in worktrees created before the settings were merged (REQ-W1-050, REQ-W1-083).
- E1.F6.T3 — `scripts/karvey-handoff-capture.py --profile <dir> [--repos-from state|team|project] [--scheduled-tasks N] [--ready-to-rotate] [--dry-run] [--json]`: measures each owned repo (`rev-parse --abbrev-ref HEAD`, `log -1 --pretty=%h`, `status --porcelain`) through the same 3.11.4 resolver the session hook uses (repo name / `.` = the root, BUG-20; worktrees, BUG-21; siblings recorded as `../<repo>`), writes `state.json` atomically, records an unmeasurable repo as `measured: false` with its reason instead of inventing values, re-measures when `state.json` sits inside a measured repo, exit 4 for a missing profile. `test_handoff_capture.py` (8). Why: the handoff state must be measured, never typed by the agent (REQ-W1-048).
- E1.F5.T8 — the 3.11 templates `skills/karvey/hooks/git-flow-guard.sh` and `plan-gate.sh` are now deprecated shims (removed in 4.0.0) that find the dispatcher (`$CLAUDE_PLUGIN_ROOT`, their own plugin tree, or the newest installed plugin) and exec it with `--only <guard> --force-enabled`, so a project that copied them into `settings.json` gets the H-10/H-12 fixes; the old `$KARVEY_PLAN_FLAG` file no longer counts and nothing tells the agent to create a marker. `tables/shims.json` (4 cases). Why: no described-but-diverging hooks, and no template that teaches the agent to approve itself (REQ-W1-016, 020, 029).
- E1.F6.T4 — `tables/session.json` (23 cases: active selection, compact XOR full manifest, 40-row / 6 KB bounds, settings notice on startup only / not on resume / not under a Karvey parent above the git top level / not when only `origin/main` has the settings / degraded line without python, structured `additionalContext`, worktree measured); the runner learned the SessionStart event, setup commands and context assertions; `test-hooks.sh` is now the entry point and runs every guard table at the end. Why: the session hook is table-tested like the guards (REQ-W1-045..048, 050, 051, 083).
- E1.F7.T1 — `karvey_lib/safe_values.py`: the per-channel / per-tool patterns of architecture §3.1 with the common refusal (control characters, shell metacharacters, leading `-`), `://` refused for every notification target, spreadsheet paths confined to `docs/spec/`; `test_no_shell_true.py` greps `scripts/` for shell entry points. Why: a value from `project.json` must never reach a shell or a notification unvalidated (REQ-W1-093, REQ-W1-097).
- E1.F7.T2 — `karvey-config.py resolve | get --shell | propose-settings`: one resolution order for tracker settings (`spec.json` override, then `project.json:management`), legacy string/`none` shapes and the single "external" test, missing/unsupported status report, `origin/{integration}` checked before "missing", `get --shell` validated through `safe_values` or exit 3, `propose-settings` prints and never writes. Why: skills read tracker and notification settings through one resolver instead of re-deriving them in prose (REQ-W1-080, 082, 083, 086..088, 093, REQ-W1-010).
- E1.F7.T3 — `karvey-config.py notify-check [--confirm]` (hash of the resolved destination vs `<git-common-dir>/karvey/notify-last.json`: unchanged → 0, changed or never confirmed → 10 with the new and previous destination, `--confirm` records it 0600, an unsafe or `://` target → 3; detail defaults to counts) and `outbox add|list|done` (`changes/<id>/tracker-outbox.jsonl` queues failed tracker operations; a child of a pending parent is `blocked_by` it and refused by `done` until the parent is applied). Why: a changed notification destination is surfaced before anything is sent, and a failed tracker call is queued in order instead of lost (REQ-W1-090, 097, 098).
- E1.F10.T1 — `scripts/lint-plugin.py` framework: registered checks with id, severity and REQ ids; `--list` (fails when a claimed REQ-W1 id is absent from the requirements), `--only`, `--paths GLOB` (brace alternatives, `**` across segments), `--format text|json|github`, exit 0/1/2; L-01 frontmatter subset, L-02 description length and shape, L-03 generic/third-party/overlapping triggers, L-04 user-only skills; good mini-plugin fixture plus one mutation per failing case. Why: the plugin's text is checked by code in CI, not by review (REQ-W1-055, 077..079).
- E1.F10.T2 — linter L-05..L-10 and L-14: phase literals against the `state-machine.json` enum, no hand edits of `phase`/`approvals`/`skipped`/`phase_history`, no phase→next table in the orchestrator (a `karvey-state.py next` call instead), produces/reads consistency, cited paths resolve, `skills/*/rules` copies byte-identical, instructed tools declared in `allowed-tools`. Why: the skills must not re-encode the phase graph or instruct what the state tool owns (REQ-W1-001, 005, 012, 013, 052, 053, 056, 057).
- E1.F10.T3 — linter L-11..L-13, L-17, L-18: phase/support/skill/rule counts in both READMEs, `plugin.json` and `marketplace.json`; versions agree across `plugin.json`, `marketplace.json`, `project.json:karvey_version` and the top numbered CHANGELOG release; `### Why` in the top release and the current version in every language block of `docs/karvey.html`; every field of the JSON blocks in `project-config.md`/`living-specs.md` declared by a schema, schemas within the `schema_lite` subset; `docs/spec` JSON through the state tool's validator. Why: counts, versions and documented shapes drift unless a check reads the files (REQ-W1-002, 055, 058, 109).
- E1.F10.T4 — linter L-15, L-16 and L-19..L-24: every hook cited in skills/rules is shipped and has guard-table cases; behaviour promises in `enforcement.md` and `hooks/README.md` carry `<!-- guard-case: ID -->` anchors to existing cases whose decision fits the verb; versioning per release, not per commit/task; QA Dimension 6 covers `versioning.md`; no actual time in an estimate field; no rotation-threshold literal without `defaults.json`; knowledge sync only at archive or on demand; status per task, cascade per Feature. Why: a promise in prose must point at the test that proves it (REQ-W1-022, 029, 036, 038..040, 042, 049, 051, 062, 064).
- E1.F10.T5 — linter L-25..L-30: QA writes its review to `changes/{id}/qa/` with no commit instruction and deploy reads it from there; no Axios/apiService/v-html/RUT stack rules in QA; no commit of the prod approval, the 6-step checklist before the first push, archive on `chore/archive-{id}`; no `!= markdown` test or direct `clickup.backlog_list_id` read, one cascade cited not restated; `project.json` values in shell examples only via `karvey-config.py get --shell`, double-quoted; the H-33 minor defects. Why: the audited defects stay fixed (REQ-W1-031, 033, 034, 059, 073..076, 084, 086, 087, 091..095).
- E1.F10.T6 — linter L-31..L-35: public sentences that mention ClickUp also name the other trackers; every RESUELTO incident names an existing regression file or lint id; duplicate D-/C-/BUG-/BL- ids (warning); no subagent prompt allows writing `project.json`; from 3.12.0 the top release carries the CLAUDE.md-destinations compatibility line; `--list` shows L-01..L-35. Why: an incident is closed only with its regression, and ids never collide silently (REQ-W1-060, 081, 099, 107).
- E1.F8.T1 — `scripts/karvey-context.py` read-only dashboard: overview, open work (findings by type and status, BUG-NN not RESUELTO, `[human]` tasks with executor and since, open backlog, pending tracker outbox), days in phase with `stalled`/`unknown`, every approval with by/role/date or `skipped: <reason>`/`approver missing`, WIP N/limit, enforcement (prod-gate `on (default)` / off only when reviewed on `origin/<prod>`, git-flow, plan-gate, the marker); an unreadable file is listed and the rest renders; exit 4 without `docs/spec`. Why: one view of the project's state computed from the files, never from memory (REQ-W1-068..072, REQ-W1-026, REQ-W1-090).
- E1.F8.T2 — `karvey-context.py` close report (closed tasks without actuals listed), calibration (actual / estimate per work type; a recalibration proposed only past `threshold_pct` in each of the last `window` changes, D-07), `convergence --change X` (exit 1 while a bug/spec-gap finding or a routed BUG-NN is open, every offender listed) and block counts per guard from the clone's `audit.log`. Why: estimates are recalibrated from data, and a change converges only when its loop is closed (REQ-W1-043, 044, 107, 108).
- E1.F9.T1 — `scripts/karvey-spec-merge.py <change> [--capability] [--dry-run]`: deterministic spec-delta merge into `docs/spec/specs/<capability>/spec.md` (ADDED appended under `ADDED by <change>` keeping group headings, MODIFIED replaced in place by id, REMOVED left as a struck deprecation line); idempotent; a missing id or conflict writes nothing (exit 1), an unparsable delta names the line (exit 3), `--dry-run` prints the unified diff. Why: archive merges living specs by code, not by hand (REQ-W1-065, REQ-W1-066).
- E1.F11.T1 — statusline: `KARVEY_TZ` resolved once, an invalid zone shows the system time marked `(TZ?)` (BUG-08); limit windows joined with ` · ` and no leading separator (BUG-09); `KARVEY_ROTATE_HOURS` defaults to `defaults.json:rotation_hours`, a copy that cannot find it shows `rot?` (D-06); `tables/statusline.json` (8 cases) run through `run_tables.py`. Why: no silent wrong time and no second copy of the rotation threshold (REQ-W1-049, 100, 101).
- E1.F11.T2 — `docs/karvey.html` script restructured into pure functions (`safeDecodeHash`, `langOf`, `pickLang`, `withLang`, `hashToBlock`) and one `init(window)`: a malformed hash is ignored (BUG-10), `?lang=` limited to the five languages and applied for one visit without overwriting a saved choice, zh-TW/HK/Hant with a Simplified note (BUG-11), other query parameters kept (BUG-12), `hashchange` mapped onto the shown block (BUG-13), the switch shown only with JS (BUG-14); `tests/page/test_page.mjs` (node:test, no npm) and `test_page_static.py`. Why: the public page's language switch works for every link shape (REQ-W1-102..106).
- E1.F12.T1 — the 9 hand-kept rule copies under `skills/{karvey-architecture,karvey-init,karvey-requirements,karvey-tasks}/rules/` deleted (all byte-identical to `skills/karvey/rules/`); every cited rule, `TRADEMARK.md` and hooks path now resolves from the citing file (`../karvey/rules/x.md`, or `${CLAUDE_PLUGIN_ROOT}/…`). Why: one source per rule, and a citation an agent can follow (REQ-W1-052, REQ-W1-053; L-09, L-10).
- E1.F12.T12 — the retroactive review of `team-adapters` moved from the repo root to `docs/spec/changes/team-adapters/qa/REVISION_PR_17-19_20260923.md`; CHANGELOG, backlog, decisions, the change's `spec.json` (`approvals.qa.ref`) and findings point there; `inputs.qa_team_adapters` keeps its `@d38abc9` pin. Why: a review belongs to its change, never to the root (REQ-W1-075; L-25).
- E1.F12.T2 — new rule `rules/state-machine.md`: the phase table (skill, approval key, skippable, produces, reads), edges, reopen targets and preconditions generated from `schemas/state-machine.json` between `<!-- generated:state-machine -->` markers; refusals, and where each phase is committed (D-03: prod approval never a commit; deployed/archived on `chore/archive-{id}`). Agreement test `tests/unit/test_state_machine_rule.py` (`--write` regenerates). Why: one graph, read by the tool, the text and the linter (REQ-W1-001, REQ-W1-004, REQ-W1-005).
- E1.F12.T3 — orchestrator, `karvey-init` and `karvey-requirements` rewritten around the scripts: the orchestrator answers from `karvey-state.py next {id} --json` (the phase→next table is gone) and says `[Unreleased]` per commit, one bump per release; init creates the state with the new `karvey-state.py init <change> [--by]` (F-25), resolves the tracker with `karvey-config.py resolve management`, creates the Epic once in `todo` by natural key, persists "not now" as `notifications.deferred`, writes settings on a docs branch with a `Settings:` line, sets enforcement flags in project.json, defaults `knowledge_sync` to `none` without graphify and no longer syncs; requirements reads `prd.md`, writes `spec-delta.md` at the change root, records `generated`/`approve` through the tool and drops the graphify step; descriptions ≤ 250 with method-scoped triggers. 955 → 795 lines. Why: the state and settings are written by code, not promised in prose (REQ-W1-005, 006, 012, 013, 039, 056, 059, 061, 062, 077, 078, 083, 095).
- E1.F12.T4 — `karvey-mockup`, `karvey-design-graphic`, `karvey-architecture`, `karvey-infra`, `karvey-tasks`: preconditions from `karvey-state.py next` (approved **or skipped**), `advance`/`generated`/`approve` through the tool (the `*-generated`/`*-approved` phase values are gone), `skip` paths for no-UI (mockup, design_graphic) and no-cloud (infra) changes, design-graphic asks instead of approving itself, no per-phase knowledge sync, `prd.md` instead of `proposal.md`; tasks resolves the tracker with `karvey-config.py resolve management`, cites the one missing-map clause, finds or creates items by natural key, writes `estimate_min · actual_ai_min · actual_review_min` rows and never overwrites an estimate. Why: REQ-W1-004, 007, 012, 013, 043, 062, 077, 078, 080, 081, 089.
- E1.F12.T5 — `karvey-impl`, `karvey-test`, `karvey-qa`: each opens with `karvey-state.py advance`; impl picks tasks from one declared source (todo, or orphan in_progress with deps at review|done) and reports drift, adds one `[Unreleased]` line per commit and never bumps, records actuals as time entry or PLAN.md `actual_*_min` and never overwrites the estimate, cites the cascade; test takes architecture §6 as its contract and loses the duplicate E2E block; QA never commits (defects go to findings.md), writes its review to `docs/spec/changes/{change-id}/qa/`, checks `[Unreleased]` and the versioning QA items in Dimension 6, drops the Axios/apiService, v-html and RUT stack rules, notifies after `karvey-config.py notify-check` with counts, and on approval moves the change's `review` items to `done`. 910 → 798 lines. Why: REQ-W1-011, 036, 038, 040, 042, 043, 059, 073, 074, 076, 084, 085, 091, 097, 098.
- E1.F12.T9 — rules A: `enforcement.md` describes only what the dispatcher ships (switches from the reviewed line, protect-paths, the approval hook with TTL from `defaults.json`, prod-gate on by default, git-flow, plan-gate with the interpreter-write limitation, the spec-write validator, the legacy shims), each promise anchored `<!-- guard-case: ID -->` to a table case, and no `touch` override; `deploy-workflow.md` records the prod approval as D-NN + PR + ledger (copied at archive on `chore/archive-{id}`), puts the checklist before the first push, adds the trunk variant and reads branches with `karvey-config.py get --shell`; `versioning.md` says each release increments once and lists its QA items as anchors; `knowledge-sync.md` syncs at archive and on demand only (`none` default); `engineering-standards.md` no longer names a `standards-guard` hook. Why: REQ-W1-016, 018, 022, 026, 027, 029, 031, 034..036, 039, 040, 051, 061..063.
- E1.F12.T6 — `karvey-deploy`, `karvey-archive`, `karvey-iterate`: deploy runs the 6-step checklist (Step 1.9) before the first push, reads the review from `docs/spec/changes/{change-id}/qa/` and `[Unreleased]`, runs `advance {id} deploying` on the feature branch, bumps once at the release step, records the prod OK as D-NN + PR + `approve {id} prod` (the ledger; never a commit on integration), one PR in trunk flow, visible version as a recommendation, branches from `karvey-config.py get --shell`; archive starts on `chore/archive-{change-id}`, then `advance deployed` with the pipeline evidence, `approve prod --write-spec`, `advance archived`, `karvey-spec-merge.py --dry-run` then apply, nothing left in `review`, calibration from `karvey-context.py`, and the knowledge sync (only here); iterate reopens with `karvey-state.py reopen` and tests the tracker with `resolve management`. 717 → 447 lines. Why: REQ-W1-011, 031..035, 037, 041, 044, 063, 065..067, 074, 077, 078, 084, 087.
- E1.F12.T7 — support skills: `karvey-context` runs `karvey-context.py` and relays it (the grep parsing is gone); `karvey-checkpoint save` commits the handoff first and runs `karvey-handoff-capture.py` last (F-14), never writes `state.json` by hand and cites `defaults.json` for the rotation threshold; `karvey-guard` sets `project.json:enforcement.*` on a docs branch, drops `--override` and every `touch`, and flags 3.11 template entries as deprecated shims; `disable-model-invocation: true` on guard, team, benchmark-models, scrape, import, retro; import creates the state with `karvey-state.py init`; browse/health declare `Write`, health checks python ≥ 3.9; one decision-log path in karvey-decisions and `rules/multi-agent.md`. 1312 → 1159 lines. Why: REQ-W1-013, 017, 018, 046, 048, 049, 056, 059, 068..072, 077..079.
- E1.F12.T8 — descriptions of devex, diagram, docs, grill, investigate, second-opinion and standards in the `Karvey phase 0 | support — produces — when` shape (≤ 250 characters); generic and third-party triggers (Garry Tan, gstack, kiro, PRD, SDD, mermaid, excalidraw, README, documentation, codex, …) removed, bilingual `karvey …` triggers kept; grill declares `AskUserQuestion`. L-01..L-04 are green on all 32 skills. Why: triggers must not collide with the user's own skills or built-ins (REQ-W1-077, REQ-W1-078).
- E1.F12.T11 — `hooks/README.md` lists only the hooks that ship (session, approval, protect-paths, prod-gate, git-flow, plan-gate, spec-write, pending-sync), each behaviour anchored to a table case, python ≥ 3.9, rotation from `defaults.json`; `clickup-sync-guard` and `standards-guard` appear only as "not shipped". `README.md`, `plugins/karvey/README.md`, `plugin.json` and `marketplace.json` present the tracker as the team's configured one, name skills `/karvey:karvey-<name>`, drop the rule-copies claim and the third-party attribution; versions untouched. Why: REQ-W1-049, 051, 055, 059, 060.
- E1.F12.T10 — rules B: `management-adapters.md` holds the one resolution order (`karvey-config.py resolve management`, `external`), the one missing-map clause, per-level status maps with `null`, `task_ids`, natural keys, the outbox, the `none` alias, `sprints`, 🙋 awaiting-human → `blocked` and the one cascade, with shell values from `get --shell`; `phase-close.md` names exactly impl/test/qa (+ iterate), status per task and comment + cascade per Feature, outbox retry, no `clickup-sync-guard`; `notifications.md` refuses `://` targets, adds `detail`/`deferred` and the `notify-check` confirmation; `project-config.md`/`living-specs.md` JSON blocks match the schemas (L-17); `team.md` cites `defaults.json` for rotation; `clickup-protocol.md` never overwrites an estimate; backlog/incident-tracking test the tracker with `external` and require a named regression for RESUELTO. Why: REQ-W1-002, 029, 042, 049, 064, 080..094, 097..099, 107.
- E1.F12.T13 — text gate: the whole-repo lint goes from 539 errors to 7, and `plugins/**` is at 0; the 7 left are L-18 validation errors in the `team-adapters` and archived `team-layer` `spec.json` approvals, owned by E1.F15 (F-35). Residue fixed: karvey-init offers `CLAUDE.md` destinations as a pre-filled migration aid (notifications.md), karvey-test declares `AskUserQuestion`. Skill corpus 8900 → 7433 lines, 122 090 → 108 436 tokens (SKILL.md 5992 → 5280 lines). Why: REQ-W1-001, 013, 053, 055, 057.
- E1.F13.T1 — CI workflow `.github/workflows/lint.yml` (jobs `lint`, `tests` ubuntu/macos × 3.9/3.12, `page` node 20, `windows-advisory`), `pull_request` and `push` on main, `permissions: contents: read`, actions pinned by commit SHA, no pip/npm; `.gitattributes` (`*.sh`/`*.py` eol=lf, `docs/spec/.graph-pending merge=union`). Why: the linter, the unit suite, the guard tables and the page tests run on every PR instead of on the author's machine only (REQ-W1-030, REQ-W1-054).
- E1.F14.T1 — legacy `spec.json` fixtures under `plugins/karvey/tests/fixtures/legacy/spec/` (57 anonymised shapes: every phase value of §2.5 plus the two REQ-W1-009 values the scan did not see, embedded skips, `approvals: null`, unknown approval keys, `management` markdown/clickup/none/absent, multi-type fields, `gates_skipped`, a team-adapters-like prod record, a BOM) and the tests that iterate them: `validate` never crashes (advisory and strict), `--fix` is idempotent and never changes an approval, each phase maps per its tier, unmappable → exit 3 with no write; `test_fixtures_anonymous.py` (`fixture-NN`, `…`, no `@`, no 6-digit run, keys only from the catalogue). Why: the migration is proved on every legacy shape found in the 467 real files without committing any of them (REQ-W1-003, REQ-W1-009, REQ-W1-010).
- E1.F14.T2 — legacy `project.json` fixtures under `plugins/karvey/tests/fixtures/legacy/project/` (management as string, object with legacy extras or absent; notifications absent, `google_chat` alias or none; top-level `clickup.backlog_list_id`; trunk `main`/`main`) and `test_config_resolve.py` over each: `resolve management` / `notifications`, `propose-settings --from-legacy` writes nothing, `validate --fix` turns a string into `{"tool": …}` idempotently and never invents `statuses`. Why: the settings resolver is proved on the shapes the 66 real project files have (REQ-W1-010, REQ-W1-086..088).
- E1.F14.T4 — 10 agent-behaviour manual scripts under `plugins/karvey/tests/manual/` (missing status map, no human no mapping, per-level maps, settings on a docs branch, QA review → done, impl resume, find-or-create, init "not now", `--settings` merge, visible version), each with setup in a scratch repo, the exact prompt, the expected observable result and the evidence to capture. Why: skill text is agent instructions with no unit test; these make REQ-W1-041, 080..085, 089, 095 and 096 verifiable by a person in a real session (AC-7).
- E1.F14.T3 — regression index `plugins/karvey/tests/regression/test_incidents.py` (BUG-05..21): each incident names the check that proves it (lint id, guard-table case, unit or node test, `test-hooks.sh` section, manual script); it fails if a named check disappears, if a named lint check reports an error on this repo, or if the tracker marks an incident RESUELTO without an automated check its Regression section names; CI `tests` job runs it. BUG-06..17 moved to RESUELTO in `docs/bugs_dev_testing.md` and `docs/spec/incidents-index.md`; BUG-05 stays DETECTADO (F-39). Why: an incident is closed only by a test that keeps it closed (REQ-W1-107).
- E1.F15.T2/T3 — the retroactive `team-adapters` prod approval is recorded through the state tool (`approve team-adapters prod --role human --ref D-08 --write-spec`) after the owner's own phrase: `approvals.prod` gains `role`, an ISO date-time and ref `D-08`; `approvals.qa` stays false. `validate --all` and the whole-repo lint reach 0 errors. Why: REQ-W1-003, REQ-W1-108, REQ-W1-109.
- E1.F13.T2 — CI observed on draft PR #24: run 36063032643 @febcf59 green on all 7 jobs (lint, tests ubuntu/macos × 3.9/3.12, page, windows-advisory). Why: REQ-W1-030, REQ-W1-054.
- Test phase of wave1-hardening — `docs/test_plan.md` maps architecture §6 to commands, `docs/test_evidence.md` records the run (all automated suites green, CI 7/7, live-session E2E of the state edges, approval markers and protect-paths; CLI latency baseline) and what was not run (manual scripts, F-47). Why: the method's test contract before QA (Mauricio Quezada Ibáñez · Claude Opus 5.5).
- Iteration 1 of wave1-hardening (D-19) — architecture reopened from `test` and amended as revision 1 (§1.4 profile-only commits, §1.2 `--date` on retro prod records, §3.1 per-kind exemptions and status `( )`, §6.5 manual-script executor, §7.3 option a); tasks E1.F17.T1..T3 added and E1.F16.T2 made `[human]`; BUG-22 (F-40) registered; findings closed per D-17 and F-26..F-32 moved to BL-44..BL-50. Why: route every open finding before QA (Mauricio Quezada Ibáñez · Claude Opus 5.5).
- wave2-structural E1.F1.T1 — schema additions: `lane` enum (six lanes), `lane_history`, `lane_evidence`, `gate_outcomes`, `deploys`, `judge_runs`, role `auto` (prod stays `human`), `generated_at`/`imported` on approvals, `skipped` over every approvable phase with the semantic check `state.skip_not_lane`, `D-NN@repo` decisions; project `gates`, `judges`, `lanes`, `checks`, `branch_flow.mode`, `enforcement.trailer_guard`, `tests`, `security`, `knowledge_sync` default `none`. Why: every Wave 2 record and setting needs one validated shape before any code writes it (REQ-W2-019, 074). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T2 — check-mode registry `schemas/check-modes.json` (the nine Wave 2 checks with their 3.13 and 4.0 defaults) and `karvey_lib/modes.py` (`resolve`: project override, laxer-than-4.0 warning, invalid value → default; `record_hit` → `changes/{id}/checks.jsonl`). Why: one place declares every new check's mode, so no 3.13 default refuses and 4.0 flips exactly D-24's three (REQ-W2-083..085, 010). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T3 — state tool: `generated` writes `generated_at` once (never overwritten); `approve` appends a `gate_outcomes` entry and accepts `--role auto` outside prod (`approve prod --role auto` → "production approval is never automatic"); new `outcome <change> <phase|gate> changes_requested --by --role --ref [--reason] [--kind plan-exception]` (empty reason → "no reason given", missing role/ref → exit 3 with spec.json byte-identical, the phase is kept). Why: approval wait and gate rejection rate need timed, append-only outcomes, and `-y` must be visible as automatic (REQ-W2-001, 038, 040, 042). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T4 — state tool: `deploy-record <change> --env --version --verification pass|regression|not-evaluated [--rollback] [--evidence]` appends `deploys[]`; `deploying` carries no approval (`state-machine.json`, rule table regenerated) and a legacy `approvals.deploy` is the warning `state.legacy_deploy_approval`; `advance deployed --attested --ref D-NN --pipeline-run URL` records `evidence.attested: true` in a clone without the release ledger, refuses with both evidences named, and refuses `--attested` where a ledger exists. Why: deploys become measurable records, and a second clone can close a deploy without faking the ledger (REQ-W2-002, 051, 053). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T5 — `karvey-state.py next` lists each blocker once, in order (the current phase was also listed as a precondition of the next one). Why: F-26 — the duplicate line read as two problems (REQ-W2-074). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T6 — `karvey_lib/metrics.py`: one pure function per flow metric (lead time, cycle time per phase, approval wait per gate, throughput, deploy frequency, change failure rate, time to restore, spec-gap rate, ripple, gate rejection rate, estimate accuracy, judge acceptance and cost, automatic approvals), each returning `(value | None, reasons)`, per lane and in total; anonymised fixtures (three archived changes + one legacy date-only). Why: the 4.0 defaults are decided on measured data, and missing data must read `n/a — {reason} ({id})`, never 0 (REQ-W2-003, 004). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T7 — `karvey-context.py --metrics [--from --to --as-of --lane --json]`: the metrics of every change archived in the period, per lane and total, as a table or JSON with sorted keys, no wall clock and no absolute path; read-only (`git status` clean after a run); an empty period prints `n/a — no archived change in period`; findings rows now carry `origin` and `phase`. Why: a baseline and a retro are only comparable when the same repo and arguments give the same bytes (REQ-W2-003, 005). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T8 — `karvey-context.py --readiness`: measured changes (deployed or archived, with lane, timed history and a gate outcome), per registry check the `would_refuse` hits from `checks.jsonl` and how many were confirmed (linked finding routed as bug or spec-gap), `schema.strict` computed with `validate --strict`, `no data` for a check without hits, and `ready for 4.0: N of 4` / `not ready: N of 4`. Why: turning a 3.13 default into a 4.0 refusal is decided on how often it would have refused and how often that was right (REQ-W2-010, 086). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T9 — metrics baseline of this repo `docs/spec/retros/baseline-2026-09-25.json` (`--metrics --from 2026-09-01 --to 2026-09-25 --as-of 2026-09-25 --json`, byte-identical on re-run) and lint L-48: a `project.json` that sets `gates`, `judges`, `checks` or `lanes` without a baseline, or with a baseline dated after the first commit that set them, is an error; lint `reqs` accept `W2-NNN`. Why: the 3.13 → 4.0 comparison needs the before-picture taken before any Wave 2 default changes the process here (REQ-W2-006, 088). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T10 — `karvey-retro` works on the method's artifacts: metrics from `karvey-context.py --metrics` for an explicit period (n/a stays n/a), findings by type and phase, estimate accuracy, judge cost and acceptance, follow-up of every previous action, `retro-{date}.md`, each agreed action a `process` BL-NN with an owner (no owner → no backlog row); the per-author commit analysis runs only with `--per-person`; manual script `retro-from-metrics.md`. Why: a retro built on commit counts per person measures people, not the process (REQ-W2-008, 009). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F1.T11 — tracker adapters declare a `log_time` operation per tool (ClickUp time entry, Jira worklog, Azure Boards `Completed Work`, `none` for the others, which fall back to `actual_ai_min` / `actual_review_min`); `karvey-impl` records the actual through `log_time`; lint L-50 fails a tool row without a `log_time` cell or an impl text without the fallback. Why: F-27 — "a time entry where the tool has one" left each session to guess, and the actual time is the input of estimate accuracy (REQ-W2-007). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T1 — lane table `schemas/lanes.json` (patch, standard, feature-ui, ops, hotfix, docs: m/o/s per phase, merged gates, judges per judged phase, patch criteria, neutral globs) and `karvey_lib/lanes.py` (`load` with schema + semantic checks, `phase_rule`, `lane_of` with the `type` / legacy fallback, `gates_of`, `judges_for` with the per-lane override, `globs`); metrics use `lanes.lane_of`. Why: the cost of the process scales with the change only if the lane is data the state tool reads, not prose (REQ-W2-011, 019, 031). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T2 — `karvey_lib/gitlog.py` (read-only git queries through an argv allow-list: `diff_names`, `log_trailers`, `changed_paths`, `grep_refs`; option-like refs refused) and `lanes.admit_patch` / `propose` / `measure_diff`: the D-29 patch criterion (≤ 3 code files, no schema, API contract or permissions change, Tier < 3) checked on the init answers (unknown → `standard` with the reason) and on the real diff (`lane exceeded: 5 > 3 code files`). Why: a lane that is only declared is abused; the same criterion must hold at init and on the diff QA reviews (REQ-W2-012, 013, 017). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T3 — state tool is lane-aware: a phase the change's `spec.json:lane` marks `s` counts as skipped for `next` and the preconditions, and `advance` records it as `skipped[phase] = "lane:{lane}"` (a manual skip is never overwritten); a `lane:` reason that does not match the lane, or names a phase the lane runs, is the error `state.skip_not_lane`; `next` reports `lane` / `lane_source` and one `state.lane_missing` warning for a change without a lane (the 3.12 pipeline). Why: `standard` changes must go from requirements to architecture without two hand-written skips, while `feature-ui` still refuses architecture without a mockup (REQ-W2-015, 019, 021). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T4 — state tool `lane <change> set|raise|lower <lane>`: `set` only at init (`set patch` checks the D-29 answers and refuses with the criterion and the proposed lane), `raise` appends `lane_history` and turns the phases the new lane requires back to pending (moving the phase back to the first of them; manual skips survive), `lower` needs `--by --role human --ref` and a valid human approval marker; `lane-evidence` records BUG-NN, finding and regression test for patch/hotfix; a hotfix enters impl without a tasks approval only with BUG-NN and regression test (both named when missing), and a lane-optional phase (hotfix/ops QA) accepts a skip with a reason. Why: a lane may grow freely but never shrink without the human, and the hotfix path must be consistent with its own preconditions (REQ-W2-014, 016, 018). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T5 — `rules/lanes.md`: the lane table rendered from `schemas/lanes.json` between generated markers (`lanes.render`), the objective init questions, the patch criterion (D-25, D-29), ops / hotfix / docs, raising freely and lowering only with the human; `rules/multi-agent.md` §6–§7 now point to it; lint L-40 fails a hand-edited block, a lane that misses a phase or a missing rule. Why: one statement of the lanes, kept equal to the data the state tool reads (REQ-W2-011, 021). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T6 — `karvey-init` decides the lane from objective answers (UI, schema, API contract, permissions/trust, Tier, code files; unknown → `standard` with the reason) and records it with `karvey-state.py lane set --answers`; `karvey-qa` Step 0B runs the lane check (`karvey-state.py lane-check <change> --base --head --finding`, new, read-only on spec.json: one `lane.diff` hit per exceeded criterion in `checks.jsonl`, a `lane raise` proposal, warn in 3.13) and defines QA-lite. Why: the lane is chosen on facts and re-checked on the real diff, so `patch` cannot be used for a large change (REQ-W2-012, 017). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T7 — dashboard: a `lane` column in the overview and in the approvals section, lane-skipped phases shown as `skipped (lane {lane})` (never pending or awaiting approval), and `role: auto` approvals shown as `auto-approved (-y)` and listed apart from the human ones. Why: a reader must see which gates the lane removed and which approvals no human gave (REQ-W2-021, 040). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T8 — `docs/spec/changes/wave2-structural/global-instructions.diff`: the one bullet of the owner's global instructions about bugs and support tickets, replaced so that a small bug under D-29 takes the `patch` lane (BUG-NN + finding + fix + regression test, one production gate), under a neutral `a/global-instructions` header; the owner applies it (E1.F13.T8), no agent step writes outside the repository. Why: the owner's standing instruction routed every bug through the full iterate loop (REQ-W2-020). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F3.T1 — `rules/judges.md` (settings, closed inputs, the read-only prompt template with its JSON output contract, collection and routing) and one rubric per judged phase with a section per default lens (`requirements`: domain, methods; `architecture`: security, methods, agents-cost; `qa`: fiscal, security); `defaults.json` gains the judge defaults and an estimate-only `judge_price_table`; lint L-51 fails a rubric without a default lens, a missing rubric, or a template that allows more than Read/Grep/Glob or omits "Do not edit any file". Why: an independent verdict at the cheap end of the cycle needs a fixed rubric and a judge that cannot change what it reviews (REQ-W2-023, 024, 032). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F3.T2 — `karvey_lib/judges.py` (settings over `defaults.json:judges`, `pick_lenses` with the qa fiscal always first, `build_inputs`) and `karvey-judges.py inputs <change> <phase> [--extra] [--base] [--json]`: the closed input list (the phase's produces + reads, the goal, the rubric and its lens sections; for qa the diff in a temp file and the review), lenses per lane (`judges: none for lane patch`), `judges: disabled by project setting`, extras `dropped: … (not a phase input)`, `judges.budget: ignored`. Why: a judge that sees the author's conversation is not independent; its inputs are decided by code, not by the prompt (REQ-W2-022, 023, 031). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F3.T3 — `karvey-judges.py collect <change> <phase> --results DIR [--out] [--diff] [--model] [--intra-model] [--dry-run]`: an unparsable result is `not run (invalid output)`, a `cite` outside the inputs or past the file's end is discarded and counted, text is sanitised (300 characters, `|` and newlines escaped, code blocks and patches stripped), cost is measured from the runtime's usage or estimated (characters ÷ 4 × the estimate-only price table, `estimated: true`), `judges.budget` is reported ignored; kept findings are appended to `findings.md` as `open` rows with origin `judge:{lens}` (compare-and-swap write) and the run records go to a file for `judge-run`. Why: a judge finding is only worth the human's time when it cites the artifact, and its cost must be visible, never capped (REQ-W2-025, 026, 029, 030). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F3.T4 — state tool `judge-run <change> <phase> --from FILE` appends the collected run records to `judge_runs[]` (a record without `model` or a boolean `intra_model`, or with a non-numeric cost, refuses the whole batch; the change total is printed) and `approve` refuses, when `judges.mode` resolves to `blocking`, while an `open` Critical/High judge finding of that phase is in `findings.md` (advisory, the default, never blocks). Why: the judge cost log belongs to the file owner, and blocking judges are an explicit project choice (REQ-W2-028, 029, 030). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F3.T6 — `karvey-iterate` records the decision on a judge row as `accepted:{bug|spec-gap|emergent} {ref}` or `rejected: {reason}`; `karvey-context.py --section convergence` lists a closed judge row with neither form as `unresolved (no routing or reason)`; `judge_acceptance` reads those forms (two accepted + one rejected = 0.67 for the lens). Why: without a recorded accept/reject the acceptance rate per lens, and so the value of each judge, cannot be computed (REQ-W2-033). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F6.T1 — `karvey-spec-merge.py <change> --check`: read-only answer `merged` · `unmerged` (the ADDED / MODIFIED / REMOVED ids still pending) · `conflict` (an ADDED id with other text, or a MODIFIED / REMOVED id missing), exit 0 only when merged. Why: deploy must know before the production PR whether the living spec already holds the delta, and archive must not merge it twice (REQ-W2-054, 055). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F3.T5 — skill `karvey-judges` (support): closed inputs, one clean-context subagent per lens in parallel, `collect` + `judge-run`, verdicts handed back to the calling gate; `karvey-requirements` and `karvey-architecture` run it before their gate, `karvey-qa` runs the fiscal before `approve qa`; counts 33 skills / 19 support in README, plugin README, `plugin.json`, `marketplace.json`, the orchestrator and `rules/support-skills.md`. Why: the judges only protect a gate when the phase skills actually call them (REQ-W2-022, 027, 032). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F3.T7 — manual script `tests/manual/judges-gate.md`: real subagents per lens on paths only, nothing edited by a judge, `judge_runs` with `model` and `intra_model: true`, a cited `judge:security` finding, verdicts in the gate summary, *Request changes* recorded. Why: whether judges get a clean context can only be observed in a real session (REQ-W2-022, 027, 029). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F4.T1 — `state-machine.json` gives every approvable phase its `gate` (`what`: requirements, mockup, design_graphic · `how`: architecture, infra, tasks · `release`: qa, prod); state tool `approve-gate <change> what|how|release` writes one approval record per covered phase (skipped and lane-skipped phases passed, already-approved ones listed) and one `gate_outcomes` entry, refuses a covered phase whose artifact is not generated (naming it), records prod only through the Wave 1 conditions (human, prod-kind marker, ledger — `auto` refused then) and otherwise says prod is pending; `generated --imported` marks an imported artifact, whose approval needs a human with a valid marker (exit 3 otherwise, also in 3.13). Why: one human answer per gate instead of one per phase, without weakening the prod rule or trusting imported artifacts nobody reviewed here (REQ-W2-034, 036, 080). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F4.T2 — gate mode: `project.json:gates` resolved through the check-mode registry (`gates.merged`: 3.13 granular, 4.0 merged), `--granular-gates` wins for one invocation, an invalid value is refused (exit 3; `validate` already errors on it); new state command `gate <change> <phase> [--granular-gates]` says whether the phase closes its gate in that mode and what to record it with (`approve` or `approve-gate`). Why: the one closing block of every phase skill needs a deterministic answer to "ask now or continue" (REQ-W2-039). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F4.T3 — `karvey-context.py --section gate --change X --gate what|how|release`: the one-page gate summary built by the script — phases with their state (`skipped (lane)` apart) and artifacts present/missing, per judge verdict · counts · model · intra-model, `judge {lens}: not run (…)`, `judges disagree`, Critical/High judge findings in full, `none for lane` / `disabled`, judge cost; for *how* the decisions and risks sections of `architecture.md`, `deviations.md` entries (an id the summary cannot show is reported as an OMISSION), estimate from `tasks.md`, `[human]` tasks, uncovered requirement ids and post-deploy / rollback contract gaps; for *release* the lane, coverage, security and manifest hits of `checks.jsonl`; a missing source prints `missing: {path}`. Why: a merged gate is only safe when the human sees everything it covers on one page that cannot silently drop a section (REQ-W2-027, 037). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F4.T4 — new rule `rules/gates.md`: the three gates, the mode query (`karvey-state.py gate`), the one closing block (granular: judges → one question *Approve and advance (recommended)* · *Approve and stop* · *Request changes*; merged: a phase that does not close its gate records `generated` and continues), phases without a gate ask nothing, `-y` = `--role auto` and never production, plan-rule exceptions are not gates; the 12 phase-skill "Shall we advance" closings and `rules/phase-close.md` now cite it, and the `-y` sentences of requirements, architecture, infra and tasks say `--role auto`. Why: seven "shall we advance?" questions plus an approval question per phase made the gate a rubber stamp (REQ-W2-034, 035, 038, 040, 042). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F4.T5 — lint L-41 (no skill or rule asks a second "shall we advance?" question; every phase skill except archive closes with a section citing `rules/gates.md`) and L-52 (`rules/gates.md` says `-y` records `--role auto`; no skill or rule line describes `-y` with production without a "never"). Why: the one-question gate and "`-y` is never production" must not drift back through later skill edits (REQ-W2-034, 035, 040). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F4.T6 — `karvey-grill` asks in batches of at most four questions (one `AskUserQuestion` call), each with its recommended option first; the stack is inferred from lockfiles, manifests and CI files and confirmed in one question, asking item by item only what was not inferred or was corrected; manual script `tests/manual/grill-batches.md`. Why: 27 questions one at a time made the interview the longest step of a small change (REQ-W2-041). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F4.T7 — manual script `tests/manual/merged-gates-three-questions.md`: a real `standard` run with `gates: merged` asks exactly three gate questions (*what*, *how*, *release*), no "shall we advance?", architecture continues without a question, three `gate_outcomes` entries and prod still pending. Why: AC-4 counts questions a human is asked, which only a real session shows (REQ-W2-034, 035, 038). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T1 — `karvey_lib/manifest.py`: every commit of `base..head` mapped to one change by its `Karvey-Change` trailer (one value, the change-id pattern; malformed or several values → unmapped with the reason), a merge commit without a trailer through the commits it brings in (all one change), or a commit touching only `docs/spec/changes/{id}/**` (`mapped_by: path`); read-only through `gitlog`. Why: "what was approved = what shipped" needs every shipped commit to name its change (REQ-W2-043, 045, 088). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T2 — `karvey-release-gate.py manifest [--base] [--head] [--no-record]` (default base `origin/{production}`): every change of the range with its version (the CHANGELOG block that names it), lane, QA state (`approved` · `lane-skipped` · `missing`) and commits, plus the unmapped commits with their reason; verdict `pass`, else `warn` (3.13) or `fail` (`release.manifest: blocking`, exit 1); a non-pass verdict appends one `checks.jsonl` hit per active change; a range git cannot answer is exit 4 "not computable". The manifest builder lives in `karvey_lib/manifest.py` so the prod gate can reuse it. Why: the release must show which approved changes it carries and flag anything it carries without approval (REQ-W2-045, 046). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T9 — inherited base commits `390e6cb`, `02b460b`, `62ffc6d`, `38f42bf` mapped to `wave1-hardening` by a PLAN History note (no rewrite); the manifest against `origin/main` lists them among that change's inherited commits until it merges. Why: the release manifest of this change must not carry another change's commits unexplained (REQ-W2-088). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T4 — trailer guard (`pre-bash`, opt-in, fails open): with `enforcement.trailer_guard: warn|blocking` (the stricter of the working copy and the reviewed line), a `git commit` on `{feature_prefix}{id}` of an active change whose `-m` / `-F` / `--trailer` message lacks `Karvey-Change: {id}` prints `[karvey] trailer WARNING` (warn) or is blocked (blocking); an editor commit, another branch or `off` stays silent; guard table `trailer.json` (10 cases) and the hooks README row with its `guard-case` anchors. Why: a commit without the trailer is unmapped in the release manifest, and the cheapest place to catch it is the commit itself (REQ-W2-044). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T5 — prod gate: after the Wave 1 allow it computes the release manifest `origin/{production}..{head}` within the same time budget — `release.manifest: warn` (3.13; the stricter of the working copy and the reviewed line) allows with `[karvey] prod-gate MANIFEST WARNING: unmapped=N without-qa=…` or `MANIFEST not evaluated (…)`; `blocking` blocks a non-pass or not computable manifest and a manifest change without its own human prod approval, naming it; state tool `approve <change> prod --manifest [--base]` writes the same human ledger record for every manifest change when each has a valid prod-kind marker (its own or `_project`) and consumes the markers once after all the writes; 8 new `prod-gate.json` cases (`pgm-*`). Why: the prod OK must cover everything the release carries, not only the change named in the PR (REQ-W2-046, 047). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T7 — `branch_flow.mode` (`trunk` | `env-branches`): derived when absent (trunk when integration = production, `project.branch_mode`), and a declared mode that contradicts the branches is a `validate` error (`state.branch_mode`); `karvey-init` recommends trunk in the project defaults and asks the branch mode in `--settings` with trunk first. Why: one PR per change into production is what makes the release equal to the approved unit (REQ-W2-049). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F9.T4 — `karvey-evidence.py [--change] [--label] [--junit] -- <cmd…>`: runs the argv with no shell, streams stdout/stderr through unchanged, returns the command's own exit code and appends one line to `changes/{id}/evidence.jsonl` (argv, exit, duration, sha256 and size of the output — never the output text); with no active change it still runs and says `[karvey] evidence not recorded: no active change`; `rules/verification.md` asks closing claims to cite `evidence.jsonl:{line}`. Why: "tests pass" should be a line anyone can re-check, without storing output that could leak a secret (REQ-W2-073). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F9.T1 — `karvey-id.py next BUG|D|BL|F|Q [--change] [--qualified]`: under an `O_EXCL` lock in the git common dir (stale after `lock_stale_seconds`; a held lock → exit 3 and no number), max + 1 over the working tree (both decision-log shapes: `decisions.md` and `decisions/*.md`; F-NN per change), every local and `refs/remotes/*` branch (read-only `git grep`) and the clone-local reservations in `ids.json`; `--qualified` prints `KIND-NN@{repo}`; `karvey-iterate`, `karvey-test`, `karvey-decisions` and `karvey-retro` take their BUG / BL / D numbers from it. Why: parallel sessions minted the same BUG-NN by counting by hand (REQ-W2-070, 071, 081). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F9.T2 — lint L-45 (no skill bounds Epic numbers: `E{1..99}`, `E1..E99`, "Epics 1..99") and L-33 upgraded: a duplicate D / BUG / BL id above the highest one `origin/{production}` holds for that file (created after the release) is an error pointing to `karvey-id.py next`; older duplicates stay warnings, and without a readable release line everything stays a warning. Why: once the ID tool exists a new duplicate is a defect, while old duplicates are history (REQ-W2-071). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F9.T3 — `karvey-health-score.py --inputs FILE.json`: the 0–10 score from named pure sub-score functions (`score_types`, `score_lint`, `score_tests` with the coverage bonus, `score_coverage`, `score_deadcode`), weights in `defaults.json:health_weights`, a missing tool excluded and its weight redistributed, same input → same score; the report time uses `KARVEY_TZ` and an invalid zone prints `fallback zone: …`; `karvey-health` writes the tool results to a JSON and relays the script. Why: a score computed by the model differed between runs over the same results (REQ-W2-072). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F6.T2 — `karvey-archive` asks `karvey-spec-merge.py --check` first: `merged` → nothing is merged again (archive moves and closes); `unmerged` (a legacy change) → merged on `chore/archive-{id}` with the diff reviewed first; `conflict` → routed through `/karvey-iterate`; the commit example carries the trailer. Why: the spec-delta now merges on the change branch before production, and archive must not merge it twice (REQ-W2-055). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F6.T3 — lint L-49 (a change in `deployed`, not archived, whose `karvey-spec-merge.py --check` is not `merged` is an error naming the pending ids) and the dashboard line `deployed {n} d, not archived — run /karvey-archive` past `deployed_stall_days` (`defaults.json`, 7; the `deployed` entry of `phase_history`, else the last prod deploy). Why: a deployed change whose living spec lags or that never gets archived leaves the next change reading a stale spec (REQ-W2-056). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F11.T1 — knowledge sync optional everywhere: `rules/knowledge-sync.md` makes `none` the default (also when the key is absent) with no sync step attempted or reported; `karvey-archive` syncs only when `knowledge_sync` is declared; `karvey-init` offers a mechanism only when one is available and wanted; the README says nothing in the method needs graphify; lint L-46 fails any skill, rule or README sentence that calls graphify or knowledge sync required (without a negation) and a knowledge-sync rule whose `none` row is not the default. Why: an optional tool presented as a required step made projects without it look broken (REQ-W2-079). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F12.T2 — `karvey-decisions`: `decisions.md` is the one log written; per-period files (`decisions/*.md`) are still read by `cross` and `show` (and counted by `karvey-id.py`), with a migration note shown once; the same D-NN in both shapes is reported as a duplicate with both locations, never resolved silently; lint L-30 ignores the per-period glob (a read-only legacy shape, not a second log path). Why: two shapes of the decision log let a decision be missed or a number reused (REQ-W2-081). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F12.T3 — guard table case `sl-fail-01-unreadable-stdin-shows-down-line` (`statusline.json`: a stdin the script cannot read prints `karvey statusline down (rc=…)` and where the last stdin was kept), its `guard-case` anchor on the hooks README sentence, and lint L-54 (the README's statusline failure-line sentence must anchor a statusline case that asserts the line). Why: the failure line is the only thing that tells a user the statusline is down rather than off, and it had no test (REQ-W2-082). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F7.T1 — `karvey-trace.py <change> [--base]` (read-only): requirement ids from `requirements.md` headings and the ADDED / MODIFIED items of `spec-delta.md`; tasks from `tasks.md` blocks (covered by a `[Test]` task, a task with `Tests added`, or a `manual:` line naming the requirement; else `uncovered`; `test_first` when every implementation task depends on a test task); the change's trailer commits linked by task or requirement id (`no commit` otherwise); test files by `tests.globs` with `@req` tags or `test_REQ_*` names, and a test file added by the change with no reference listed as `unmapped test`. Why: "is every requirement tested?" needs one deterministic answer before the gates (REQ-W2-057, 058, 060). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F12.T1 — `karvey-import` records every imported artifact with `generated … --imported` (one call per artifact), so only the human with a valid approval marker can approve it; it then walks the gates with the human in pipeline order (merged *what*/*how* with `approve-gate` when `gates: merged`, one question per phase otherwise), stops at the first gate not approved and resumes there; manual script `import-through-gates.md`. Why: an import that copied or skipped approvals let an artifact reach implementation without ever passing this project's gate (REQ-W2-080). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F7.T2 — `karvey-trace.py --write` renders `changes/{id}/traceability.md` (requirement → tasks → commits or `no commit` → tests → last result → coverage; deterministic, no timestamps) and `--check` is the coverage gate: every requirement needs a green test or a `manual` exception, `coverage: N/N`, one `coverage.requirements` hit per requirement not green, warn in 3.13, exit 1 only when the project sets it blocking; the last result comes from the JUnit files named in `evidence.jsonl`, else from the newest evidence command that ran the test file, else `not run`. Why: a requirement listed as covered by a task but never run green was indistinguishable from a tested one (REQ-W2-060, REQ-W2-062). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T3 — `karvey-release-gate.py check <change> [--pr-body FILE]` — the deploy pre-check as one read-only verdict: `qa_gate`, `tests` (latest `evidence.jsonl` run + coverage in its `coverage.requirements` mode; no run → `not evaluated`), `changelog`, `version_match` (plugin manifests, package.json, pyproject, VERSION and the top CHANGELOG release agree), `lane_triplet` (BUG-NN + finding + an existing regression test for patch / hotfix), `manifest`, `spec_merged` (`karvey-spec-merge.py --check`) and `pr_body` (every manifest change and numbered version listed); items are pass / warn / fail / not-applicable, exit 1 only on a fail; `release-branch [--version]` prints the read-only plan of a `release/{version}` branch from production with the approved changes' commits oldest first and the changes left out, running nothing. Why: the deploy skill recomputed each pre-check by prose, so a missing CHANGELOG line or an unlisted change reached the production PR (REQ-W2-014, REQ-W2-047, REQ-W2-050, REQ-W2-069). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F2.T9 — integration test `test_patch_lane_flow.py` in a temp repository: `lane set patch` admitted on the D-29 answers, BUG-NN + finding + fix + regression test committed with the trailer, `lane-evidence`, impl from init with the six design phases recorded `lane:patch`, QA-lite by `role: auto`, `release-gate check` passing with `lane_triplet: pass`, prod refused to `auto` (the one human gate); a migration is refused at `lane set`; no regression test → `lane_triplet: fail`. Why: the patch lane was only tested piece by piece; AC-2 needs the path proven end to end (REQ-W2-013, REQ-W2-014). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F8.T1 — `karvey_lib/security_tools.json` — the fixed catalogue per category (secrets, sast, sca, iac) of public scanners, each an argv template whose only placeholders are `{repo}` and `{out}`, with its version command, report parser and documented findings exit codes — and `karvey-security-scan.py run <change> [--categories]`: `not applicable` when no tracked file matches the category, the first catalogue tool on PATH (a project may only narrow the list to catalogue ids, validated before use; a command or a value with a shell metacharacter is refused), no shell, timeout `security_tool_timeout_s` (new in `defaults.json`), 5 MB cap, report under `changes/{id}/qa/`, a hashed `evidence.jsonl` line, findings by severity; a tool error, timeout or unreadable report is `not evaluated (tool error)` and no tool is `not evaluated (no tool)`, each with a `security.tools` hit. Why: the security dimension was the model's reading, and a scanner that crashed read as a pass (REQ-W2-064, REQ-W2-065, REQ-W2-067). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F8.T2 — `karvey-security-scan.py validate-suppressions <change>` reports every `qa/suppressions.json` entry without tool, rule, path, reason or scope (exit 1); QA Dimension 1 now runs the catalogue first and cites each category's tool, version, command, findings and `evidence.jsonl` line, writes `not evaluated` / `not applicable` as such, triages false positives into suppressions with a reason and scope, then reviews what tools miss (authorisation per object, tenant isolation, business logic); `karvey-infra` requires a `security-scan` PR stage with the same four categories, and the *how* gate summary lists an `infra.md` without it as a deviation. Why: a suppression with no reason hides a finding for good, and a verdict held only by a session is lost with it (REQ-W2-066, REQ-W2-068). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F8.T3 — manual script `security-tools-present.md`: with real scanners installed, QA runs the catalogue before reading code, cites tool, version, command, findings and the evidence line per category, writes a missing scanner as `not evaluated (no tool)` and a category with no files as `not applicable`, records the fixture-key suppression with a reason and scope, and infra writes a `security-scan` stage (its absence shows as a deviation at the *how* gate). Why: the stub tests prove the script, only a real run proves the agent cites the tools instead of its own reading (REQ-W2-064, REQ-W2-068). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F10.T1 — `karvey-postdeploy.py probe|evaluate <change> [--env]`: parses the `karvey-postdeploy` JSON contract of `infra.md` (health, routes, thresholds, window, metrics source, rollback; a missing rollback command is reported incomplete), probes with urllib over https only (plain http only to the local host outside prod), 5 s per request, never following a redirect to another host, samples spread over the window; `evaluate` compares probes and the observed metrics with the thresholds (error rate, p95 against the production baseline, new 5xx) → `pass` / `regression` / `not-evaluated` (no contract, no thresholds, nothing observed — never `pass`), writes the env section of `changes/{id}/deploy_evidence.md`, shows the rollback command without running it and prints the `deploy-record` command; nothing from `infra.md` is executed. Why: "canary OK" was a reading of a dashboard with no threshold, so a regression could be recorded as a success (REQ-W2-002, REQ-W2-075..077). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T6 — deploy flow text (`karvey-deploy`, `rules/deploy-workflow.md`): Step 0 is the release gate script, explained and never recomputed; 2.1 checks the trailers through the manifest; 2.4-bis merges the living spec on the change branch (dry run first, commit with the trailer, a conflict stops before any PR); 2.5 integrates by a PR whose CI is the DEV gate, merged by the host (the local merge + push into integration is gone); 2.7 drafts a PR body listing every manifest change; 2.8-bis runs the release gate on it before the PR opens and offers the read-only `release/{version}` plan (cherry-picks only after the human's OK, a conflict aborts and is reported); 2.9 states where the production OK lives in order (PR body + ledger with `--manifest` at deploy, D-NN in the decision log at archive); the attested `deployed` for a clone without the ledger. Why: the released unit was not the approved unit and integration bypassed review (REQ-W2-045, 048, 050, 052, 053, 054). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F10.T2 — `karvey-infra` writes one `karvey-postdeploy` JSON contract per deployable service and environment (health, routes, thresholds against the production baseline, window, metrics source, rollback; never executed) and the *how* gate summary lists a missing or incomplete contract; `karvey-deploy` 2.6 / 2.10 / Step 2-bis are now the **post-deploy verification** (`karvey-postdeploy.py probe` + `evaluate`, "canary" only where traffic is split): results said as the tool says them, recorded with `deploy-record`, a missing contract is `not-evaluated`, a production regression shows the rollback command and asks the human (it runs only after the answer, under the plan gate), records `--rollback`, and opens the incident with `karvey-id.py next BUG`; manual script `deploy-postdeploy.md`. Why: a rollback decided by the agent, or a regression recorded as "canary OK", both happened without a threshold (REQ-W2-075, 076, 078). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F5.T8 — lint L-42 (every `git commit` example with a message in skill or rule text carries `Karvey-Change`, continuation lines joined; the path-only form is exempt), L-43 (no local `git merge` followed by a push into the integration branch, same line or within the next commands; `merge-tree` is not a merge) and L-53 (deploy text: 2.4-bis living-spec merge and 2.8-bis release gate before the production PR, the prod OK in the PR body at deploy and a D-NN on `chore/archive-{id}` at archive, the step named post-deploy verification, `canary` only beside traffic splitting); the archive and checkpoint commit examples now carry the trailer, the orchestrator's phase-11 summary follows the new flow. Why: without a check the old flow comes back one example at a time (REQ-W2-043, 045, 048, 052, 054, 076). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F7.T3 — test-first and evidence text: `karvey-tasks` writes a `[Test]` task per requirement that its implementation task depends on, stating the failing result expected first (or a `manual: {reason}` line), and checks no requirement is `uncovered`; `karvey-test` starts from the architecture's coverage plan and ends each row `executed` or `planned, not executed`, keeps its plan and evidence in `docs/spec/changes/{change-id}/`, runs commands through `karvey-evidence.py` and closes with `karvey-trace.py --write --check`; QA runs the suite through the wrapper or cites the CI run of the exact commit reviewed — else `Tests: not evaluated` — and quotes `coverage: N/N`; lint L-44 fails a `docs/test_evidence.md` / `docs/test_plan.md` path in skill or rule text. Why: tests written after the code and evidence shared across changes made "tested" unprovable per change (REQ-W2-057, 059, 061, 063). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F13.T1 — `validate --fix` for the Wave 2 shape: a spec.json without `lane` gets a proposed-tier lane (`type: ops|hotfix|docs` → that lane, a mockup generated → `feature-ui`, else `standard` with the reason), written only with `--accept-proposed` and shown in the diff; a legacy `approvals.deploy` moves to `deploys[]` only when it holds data (env, version `unknown` when not kept, the date with its missing time stated, `not-evaluated`, the old by/ref as evidence), else it is removed; idempotent, and no approval is created or flipped; one more legacy fixture. Why: every pre-3.13 change must reach the new shape by a reviewed diff, not by hand (REQ-W2-051, REQ-W2-087). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F13.T2 — guard table `compat.json`: the 74 Wave 1 allow cases of prod-gate, git-flow, plan-gate and spec-write replayed with the Wave 2 3.13 defaults written explicitly in `project.json` (manifest, lane diff and coverage warn; judges, security tools and post-deploy contract advisory; gates granular) — every one still allows and keeps its Wave 1 output substrings. Why: 3.13 promises that no Wave 2 default blocks what 3.12 allowed; the promise needs a table, not a reading (REQ-W2-084). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F13.T3 — integration test `test_wave2_flow.py`: a `standard` change in a temp repository with `gates: merged` — init, `lane set standard`, exactly three human gate answers (`approve-gate what|how|release`, each with a planted approval marker), every commit with the trailer, evidence recorded — ends with `release-gate check` passing and the manifest `pass`. Why: AC-4 and AC-5 are claims about the whole path; only an end-to-end run proves three questions and a clean manifest (REQ-W2-034, 036, 045, 046, 069). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F13.T4 — this repository on the Wave 2 settings, after its 2026-09-25 baseline (L-48): `validate --fix --accept-proposed` on the active changes (`team-adapters`: `lane: standard` proposed from no mockup recorded, its `approvals.deploy` moved to `deploys[]` as `not-evaluated`; `wave1-hardening`: the empty `approvals.deploy` removed), the archived `team-layer` gets only its proposed `lane`, and `project.json` sets `branch_flow.mode: trunk`, `gates: merged` and `judges` (enabled, advisory, cross-model preferred); `validate --all` 0 errors. Why: the method must run on itself before it asks projects to (REQ-W2-006, REQ-W2-088). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F13.T5 — hand-off `docs/spec/changes/wave2-structural/upgrade-steps.handoff.json`: the ten project-upgrade steps of architecture §7.4 (`lane-infer`, `approvals-deploy-retire`, `branch-flow-mode`, `knowledge-sync-declare`, `judges-budget-ignored`, `evidence-into-changes`, `checks-overrides-known`, `global-patch-lane`, and the 4.0-only `strict-schema-ready`, `manifest-unmapped`) in the catalogue's field shape, `since` a placeholder fixed at release, status `declared` — no engine code. Why: a release that changes the upgrade surface must declare how a project catches up; the catalogue entries follow once both changes are on the main branch (REQ-W2-083, 084, 087). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F13.T6 — whole-repo gate on the change, every command through `karvey-evidence.py` (`evidence.jsonl` 1–10): unit, regression, the 406 guard-table cases (JUnit under `test-results/`), `test-hooks.sh`, the page tests, lint 0 errors, `validate --all` 0 errors, and `karvey-trace.py wave2-structural --write --check` → `traceability.md`, `coverage: 97/97`; the trace also reads JSON test tables and manual scripts (F-07), this repo declares its `tests.globs`, and tests name the MODIFIED Wave 1 requirements they verify. Why: the release needs one reproducible, cited gate result, not a session's memory (REQ-W2-059, 062, 088). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural E1.F13.T7 — release docs: the `[Unreleased]` block opens with the Wave 2 summary — the check-mode table (this release vs 4.0), the manual Upgrade list from the upgrade-steps hand-off (`lane-infer`, `approvals-deploy-retire`, `branch-flow-mode`, …) and the 3.13 → 4.0 note (three defaults flip, only after ≥ 4 measured changes, its own decision) — with no version or date; `docs/karvey.html` counts fixed in the five languages (33 skills, 19 support, 26 rules) with the `karvey-judges` card and the `gates`, `judges`, `lanes`, `state-machine` rule rows and chips (F-05); PLAN feature states updated. Why: the release notes must say what turns on, when, and how a project catches up (REQ-W2-083, 085, 086). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave2-structural F-06 — merged gates can be walked: with `gates: merged`, `advance` and `next` pass a generated phase that does not close its gate when the target is in the same gate (architecture → tasks inside *how*); leaving the gate still needs `approve-gate`; granular mode is unchanged. Why: `advance tasks` refused on the pending architecture approval, so a merged gate with more than one phase was unreachable (found by the Wave 2 flow test; regression `test_state_gates.py` `MergedGateAdvance`). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F1.T1 — `karvey_lib/loadlist.py`: the `Load:` line (first 15 lines after the frontmatter), citations outside code fences and footnote definitions, the rule citation graph, a breadth-first cycle-safe closure, and `{bytes, words, tokens_est}` sizes (`tokens_est = ceil(bytes/4)`, marked estimated; words without frontmatter); a placeholder such as `adapters/{tool}.md` gives `closure_min` (smallest file, conditional `?` entries out) and `closure_max` (largest, all in). Why: one like-for-like measure for the 4.0 baseline, the after snapshot and CI (REQ-W3-001; F-48, F-49). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F1.T2 — `karvey-context-budget.py measure [--label --date --plugin --json]`: one row per phase skill of the state machine plus the orchestrator (own, direct, `closure_min`/`closure_max`, file lists, phases) and a `session_hook` row — the bytes the session hook prints on the new fixture `tests/fixtures/budget-project/` (temp path replaced by `{project}`); the top block carries the label, the date of the last commit touching `skills/` (no wall clock) and the sha256 of the measured files; keys and rows sorted, no absolute path; a `Load:` entry naming a missing file exits 1 with skill, line and file. Why: a reproducible per-phase size is the precondition of the 40% target and of the CI growth warning (REQ-W3-001, 071, 072; F-50). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F1.T3 — `karvey-context-budget.py compare BASE [AFTER|--live] [--target-median 40] [--warn-growth 10] [--reasons FILE]`: per-phase reduction of `closure_max` bytes, the median, each phase below target with its recorded reason (`contracts.json:reasons`, or `unexplained`); exit 1 when the median misses the target; with `--warn-growth` it is the CI mode — one `::warning::` line per phase (and the session hook) grown more than N%, exit 0. Why: the 40% median is the test-phase gate and growth must be visible on every PR, never capped (REQ-W3-010, 011). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F1.T4 — baseline `docs/spec/retros/context-size-4.0.0.json` (`measure --label 4.0.0 --json` on the fork-point skill and rule text, byte-identical on re-run), committed before any commit of this change that moves a skill or rule; `karvey-context-budget.py order --baseline … --change …` checks in git history that the baseline's commit is an ancestor of every renaming/deleting skill or rule commit and of every added `references/` or `rules/adapters/` file, else `baseline missing or taken after the reorganisation` (`test_REQ_W3_002_baseline_precedes_moves`). Why: the reduction is only provable against a snapshot taken before the reorganisation (REQ-W3-002, 071). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F1.T5 — lint L-62 (blocking): every `Load:` entry of a skill names an existing file (rules, adapters with a placeholder, `references/`), reported with skill, line and file; the linter's `--list` now reads REQ-W3 ids from the requirements. Why: a load list naming a deleted reference must fail CI, not silently load less (REQ-W3-072). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F1.T6 — CI: the `lint` job runs `karvey-context-budget.py compare docs/spec/retros/context-size-4.0.0.json --live --warn-growth 10` after the linter — a `::warning::` per phase grown more than 10% over the last released snapshot, never a failure; `test_ci_workflow.py` checks the step's place and that the workflow stays read-only and SHA-pinned. Why: size drift must show on every PR (REQ-W3-011). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F11.T1 — `check-modes.json`: a `4.1` default in every Wave 2 row (= its 4.0 value) and the sixteen Wave 3 rows with only `4.1` — advisory or warn except `sponsor.leak` and `loadlist.missing` (blocking); `modes.release_line` maps 4.1.x+ to `4.1` and a check on an older line takes its first declared default; lint L-73 fails on a check id passed to `karvey_lib.modes` that is not registered or a row without a valid 4.1 default; L-47 (designed in Wave 2, never registered — F-82) now guards the Wave 2 rows' 3.13/4.0 contract. Why: every later Wave 3 check reads its mode from one registry, and nothing that passed under 4.0 may start refusing (REQ-W3-061). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T1 — `spec.schema.json`: optional `effort[]` (`{kind, phase, at, session, usd, tokens, review_min}`, each value `{value, quality: exact|estimated|n/a, source, reason}`) and `judge_runs[].tokens_total / usd_estimated / source (runtime|agent-reported|estimate)`; a 4.0 spec.json without them still passes `--strict`. Why: the cost of a change and of its judges are recorded apart, each with its quality, before any tool writes them (REQ-W3-014, 016, 062). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T2 — the statusline captures, inside a Karvey project and only when the runtime passes `cost.total_cost_usd`, `{root_key, usd, transcript, context_pct, context_tokens, at}` atomically (mode 600) to `<git-common-dir>/karvey/cost/<sha256(session id)[:16]>.json` — no model turn, no repository write, the session id hashed, any failure silent; guard table cases `sl-cost-01/02` and two new table assertions (`file_not_contains`, `tree_clean`); hooks README paragraph. Why: the per-phase effort record needs the runtime's own cost figure, taken outside the model's account (REQ-W3-015). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T3 — `karvey_lib/effort.py`: picks this root's latest capture (two within 120 s → `estimated`, reason `two sessions active on this repository`), sums the transcript's per-message `usage` (a message id once; never the context-window figures, F-43), keeps what was charged in a separate `{hash}.charged.json` only it writes (F-42), counts the first close from the session start, starts a new token interval on a new or shorter transcript, and returns `n/a` with the reason — never 0 — for an unreadable capture (the gap charged once, next close) or no capture (`statusline not installed`); fixtures `tests/unit/fixtures/effort/`. Why: cost per phase from the runtime's own figures, correct across rewrites, gaps and several changes in one session (REQ-W3-015). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T4 — `karvey-state.py effort <change> <phase> [--review-min N]`: appends the closing phase's interval to `spec.json:effort[]` through the lock + compare-and-swap write, then updates the charged file; US$ and tokens `exact` from the runtime, review minutes `exact` only when stated, `n/a — statusline not installed` without a capture (never 0), and the checkpoint advice when the last capture's context is at or above the red threshold (F-47); `validate` warns (every mode, `--strict` too) on an effort entry of another kind or one that repeats a `judge_runs` record (`mixed entry`). Why: cost per change recorded per phase, judge cost kept apart (REQ-W3-014, 016). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T5 — `validate` reports every cost-limit key of project.json (`cost_limit`, `budget`, `max_usd`, `max_tokens`, `spend_cap`, the Wave 2 `judges.budget` included) as `cost.cap_key` `unsupported (D-30)` — a warning in every mode that never changes the exit code; the schema no longer promotes `judges.budget` to an error under `--strict` (it did in 4.0, F-62); lint L-63 fails on skill or rule text that stops, shortens, skips or asks to confirm because of cost. Why: cost is measured, never capped, and a 4.0 project that passed must still pass (REQ-W3-017, 062). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T6 — judge cost (MODIFIES REQ-W2-030): `karvey-judges.py collect --transcript auto|FILE` reads each judge subagent's token total from the session transcript (the `Agent`/`Task` call whose prompt carries the lens line, its `toolUseResult`) → `source: runtime`, tokens exact, US$ `usd_estimated`; a `usage` the model wrote into its reply is kept as `agent-reported`, estimated (F-45, F-78); without either, characters ÷ 4 over the prompt template and every closed input (`source: estimate`); `judge-run` records `tokens_total`, `usd_estimated`, `source`; judges skill step 2–3 and `rules/judges.md` say so. Why: a number that passes through the model is not a runtime measure (REQ-W3-077). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T7 — `--metrics` (MODIFIES REQ-W2-003) adds `cost_per_change` — per change US$, tokens, review minutes, judge US$ and estimated share from `effort[]`; totals per lane (the existing grouping), per client (`spec.client`, else the tracker's client tag, else `unassigned`) and for the period — and `phases_per_session` (effort entries per hashed session, the measure of one phase per session); a change without effort is `n/a (no effort)` and left out of the cost only; table and JSON, byte-identical across runs; fixture `tests/unit/fixtures/metrics/cost/`. Why: the sponsor and the retro need what a change cost, per client, with how much of it is estimated (REQ-W3-018). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F3.T8 — `cost_outliers` in `--metrics` (MODIFIES REQ-W2-008): the changes above 3× the median of their lane in the period, with ratio, lane median, the phase that cost most and the phase shares; a lane with fewer than three measured changes says `too few changes in lane`; the retro skill reads cost per change, phases per session and the outliers as an input for the conversation, not a verdict. Why: an unusually expensive change is a question for the retro, answered from the change's own record (REQ-W3-019). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T1 — `stakeholders` (`sponsor`, `approver`, `executor`: role, display name defaulting to the role, destination `{channel, target}`) in `project.schema.json` with a per-role override in `spec.json`, and `project.json:leak.deny_terms`; `validate` sends every destination through `safe_values.check_target` — a literal webhook URL, with or without its scheme, is refused asking for the secret's name (F-71); `project.stakeholders()` merges project and change per role; the init skill pre-fills the PRD Stakeholders section from them. Why: the sponsor page and the "your turn" events need to know who to tell, without a credential ever entering the repository (REQ-W3-020). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T2 — `schemas/wording.json`: the business wording (English, Spanish) of every phase, lane, risk state and page label — risks read "being watched", "reduced", "accepted as is", "no longer a risk", "carried to later work"; lint L-65 names the item and the language when a phase of the state machine, a lane, a risk state or a label lacks a wording. Why: the sponsor reads business words, never the method's state names (REQ-W3-080). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T3 — `karvey_lib/leakcheck.py` + `leak_patterns.json` (public token formats and generic shapes only): rules `secret` (token prefixes, credential assignments, connection strings, URL credentials, signed URLs, high-entropy strings), `path`, `host` (non-public suffixes, private/loopback/link-local ranges), `email` (any address but a declared email-channel stakeholder, F-81), `pii` (phone shapes and 8+ digit runs that are not a date, amount or version, F-73) and `client` (portfolio names and `leak.deny_terms`, F-72; with neither, `other-client names not checked` and the rest still run); hits name field and rule, never the value; an exception is a `check-error` refusal. Why: nothing internal may leave with the sponsor page (REQ-W3-023). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T4 — `karvey_lib/sponsor.py`: `build_model(root, change)` reads only the allow-list — goal, PRD scope bullets and requirement area titles; phase history and lane through the wording table; `effort[]` (US$, review minutes, estimated share, per step) with judge cost apart, `not measured` when there is none; the risk register with state words; the open questions whose owner matches the sponsor's role or name (case-insensitive) and, when the sponsor is also the approver, the gate awaiting that approval; `deploys[]` to production — every section dated (`as_of`), free text stripped of ids, file paths and backticked commands; the change's language when the wording table has it, else English with a note. Fixture `tests/unit/fixtures/sponsor/`. Why: the sponsor page shows only what the change's own records say, in business language (REQ-W3-021, 080). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T5 — `templates/sponsor.html`: the approved sponsor mockup without its chrome — design-spec tokens, `prefers-color-scheme` dark scheme, a CSP meta (`default-src 'none'`), no script, a print style that hides only the section navigation and opens every `details`, one column capped at 880 px with capped grids and scrolling tables; slots filled escaped by the renderer; node page test `test_sponsor_page.mjs` (static: no external request, print rules, no width over 360 px); lint L-64 fails on a `src`, `link href`, `@import` or `url()` request in the template or `docs/karvey.html`. Why: the sponsor opens the page offline, on a phone or on paper (REQ-W3-024, 068). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T6 — `karvey-sponsor.py build <change> --gate … [--outcome …]`: renders the model into `templates/sponsor.html` (every value escaped, *Waiting for you* first) and leak-checks model and rendered text **before writing** — a hit exits 3, writes nothing, keeps the last page byte-identical and appends field and rule (never the value) to `sponsor-refusals.jsonl`; a clean page goes to `changes/{id}/sponsor.html` with `{at, gate, outcome, sha256}` in `sponsor-history.jsonl`; no sponsor → `sponsor page: no sponsor declared` once per change. `deliver` builds the payload from the checked model only (e-mail: attachment; chat: summary + repo-relative path) and leak-checks it too (F-74). Two wording labels added. Why: the sponsor is told at every gate, and nothing internal leaves (REQ-W3-022, 023). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T7 — `karvey-context.py --report [--from --to --as-of] [--client NAME] [--json]`: read-only, business-language status of a period — released (production deploys), in progress (step wording, age and days in the step), blocked (⛔ rows and awaiting-human tasks with who unblocks, `no executor declared` when unknown), open risks with their owner, decisions awaited per stakeholder (open questions by owner); `--client` filters by `spec.client` (else the tracker tag), an unknown client prints `no changes for client` and exits 0; JSON byte-identical. Why: one status a sponsor or manager can read without the method's vocabulary (REQ-W3-025). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T8 — "your turn" events (MODIFIES REQ-ADP-011): `notifications.events` accepts `approval_requested`, `awaiting_human`, `blocked` (and `qa_every_run`); `karvey-config.py resolve notifications --event E --change C --item X [--verdict V] [--run-id R]` returns the destination of the stakeholder whose role acts (approver; executor, who also unblocks) — the change's override first — else the team destination with `no approver declared` / `no executor declared`, and a payload with change, item, what is expected, run id and timestamp; a judge-raised `blocked` carries its verdict; `rules/notifications.md` documents them. Why: the person who must act is told directly (REQ-W3-026). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T9 — sent-log `changes/{id}/notifications.jsonl` and `karvey-config.py notify-sent <change> --event E [--item --state --version --env --run-id] [--record]` → `new` or `sent`: `deploy` once per version and environment (a retry reports its run id and sends nothing), `qa` on the first run and on a verdict change (every run with `notifications.qa_every_run`), a "your turn" event once per state of its item; the log holds hashes, the time and the run id, never a destination; the qa and deploy skills send only on `new`. Why: a QA micro-loop or a deploy retry must not flood the channel (REQ-W3-027). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T10 — `karvey-close.py <change> <phase> --outcome approved|changes_requested [--gate] [--review-min] [--verdict] [--version --env] [--run-id]` (C-26): run once after the gate answer is recorded — (1) sponsor page build + deliver (changes requested regenerates it too), (2) the notifications due, filtered by the sent-log, as payloads, (3) the risk review slot for *qa*/*release*, (4) `effort` last so the close steps are charged to the closing phase (F-44), (5) the checkpoint line with the fresh-session advice; each failure is reported and the next step still runs; the gate outcome is never touched. `rules/gates.md`'s closing block is now that one call plus sending the payloads and asking the listed owners. Why: seven model-run close steps become one deterministic call at every gate (REQ-W3-022; F-46, F-53). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F4.T11 — manual script `tests/manual/sponsor-at-gate.md`: a real gate on the sponsor fixture — the close runs once after the recorded answer, the page opened offline at 360/1440 px, both schemes and print preview, a leak refusal that names field and rule and keeps the last page byte-identical, a delivery failure recorded in the outbox with the gate still closed. Why: a person's gate answer, a real channel and a real browser cannot run in unit tests (REQ-W3-022). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F5.T1 — open questions `Q-NN`: `karvey_lib/questions.py` holds the `docs/spec/questions.md` table (`ID | Question | Owner | Needed by | Changes | Context | State | Resolved by`), parses it, adds a row only with an owner and a valid needed-by date (the refusal names the missing field; the id must come from `karvey-id.py next Q`), resolves a question to `resolved → D-NN` keeping the row, and lists the open ones with `overdue` / `date invalid`; the decisions skill gains `ask`, and `cross` ending without an answer offers it. Why: an open question gets an owner and a date instead of living in a message (REQ-W3-028, 029). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.
- wave3-optimization E1.F5.T2 — `validate --all` reports `dangling reference Q-NN` (a warning naming the `D-NN`, the file and the line) when a decision of `docs/spec/decisions.md` or `decisions/*.md` cites a question absent from `docs/spec/questions.md`; it runs only once the register exists, so a log that predates it (citing questions kept elsewhere) is not flagged. Why: a question answered by a decision must be traceable both ways (REQ-W3-029). Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5.

### Fixed
- F-06/F-05 — a legacy date-only `approvals.*.date` is a warning in advisory mode (error in strict); `approvals.prod.date` stays strict; architecture §2.2 documents `maxLength`, the annotations and `schema.legacy`. Why: REQ-W1-003 says legacy shapes warn, and every existing `spec.json` holds date-only approvals.
- Integration of batch 3 lanes B/C/D — `capability` declared in `spec.schema.json` (documented in `living-specs.md`, read by `karvey-spec-merge.py`; F-21); the tracker-outbox format and its ready/blocked rule shared in `karvey_lib/outbox.py` by `karvey-config.py` and `karvey-context.py` (F-23); the §3.5 enforcement switch rules (`enforcement_of`, `reviewed_value`, `opt_in_state`, `prod_gate_state`) moved to `karvey_lib/project.py` and used by the guards and the dashboard, so the dashboard now reports git-flow/plan-gate enabled on the reviewed line and the TTL the plan-gate applies (F-10). Why: one implementation per rule, so the dashboard cannot disagree with the guards.
- BUG-05 / F-39 — karvey-impl no longer waits for a dependency to be "completed": a dependency is satisfied at `review` or `done` (Step 2 states it once; Steps 5 and 7 use logical states); new lint check L-36 fails on non-logical states (`pending`, `completed`, `finished`) or a done-only wait in impl's selection/dependency text, red on `main` and green here, indexed as BUG-05's regression; BUG-05 RESUELTO. Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5. Why: impl leaves its tasks at `review` and nothing moves them to `done` before QA, so "completed" read as `done` deadlocked every dependent task.
- F-15 (D-16) — `karvey-config.py notify-check --confirm` is now backed by a human: the approval hook records `confirmo notificacion <code>` (or `confirm notification <code>`, code = first 8 hex of the destination hash) as a confirmation marker scoped to the clone, the project and that destination, with the approval-marker TTL; `--confirm` records the destination only against a live matching marker (single use) and otherwise returns 10 with the phrase the human must type; protect-paths also covers that marker and the notify record. Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5. Why: the agent could run `--confirm` itself and accept a redirected notification destination with no human involved (REQ-W1-097 was enforced only by instruction).
- F-12 (D-15) — prod-gate: the integration branch is not production when it differs from `branch_flow.production`, even when it is the remote default (`origin/HEAD` = `dev`, the Azure Repos default), but `main` and `master` are never removed from the production set, even when one of them is named as the integration branch; architecture §3.4 states the rule; 6 new prod-gate table cases. Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5. Why: taken literally, §3.4 gated every PR into `dev` in the HainTech repos, and the code that fixed it could drop `main` from the set when `main` was the integration branch.
- F-35 (D-14) — `karvey-state.py validate` reports the approval-format errors of an archived change whose approval dates all predate the 3.12.0 release (date-only dates, prose `ref`, missing `role`, archived without a human prod record) as warnings with the reason "pre-3.12 recorded history (D-14)"; the cut-off lives in `karvey_lib/defaults.json:pre_3_12_history.released_on` (null until the release) and L-35 requires it to match the `[3.12.0]` date; non-archived changes stay strict. Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5. Why: archived history recorded before the 3.12 approval format existed cannot be corrected without back-filling it (REQ-W1-013), so it must not keep the linter red; a live change must still be fixed through the state tool.
- F-36 — the CI `windows-advisory` job no longer exits 1 for selecting nothing: six dispatcher smoke cases carry the `windows` tag, the job also runs the path-translation, BOM and CRLF unit tests, and `test_ci_workflow.py` fails if a tag selection of the job is empty; the job stays advisory. Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5. Why: a job that is red on every run because it ran zero cases proves nothing and teaches everyone to ignore it.
- F-37 — the CI `tests` job runs the guard tables once: `test-hooks.sh` gets `KARVEY_SKIP_TABLES=1` because `run_tables.py --junit` already runs them in the previous step; `test_ci_workflow.py` checks the switch. Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5. Why: each of the four legs ran every table twice for no extra coverage.
- F-38 — a legacy `project.json` `management.status_flow` keyed by the logical states is proposed as `statuses` by `karvey-config.py propose-settings --from-legacy` and moved there by `karvey-state.py validate --fix --accept-proposed` (plain `--fix` names it and keeps it; a map with other keys is kept); `validate --fix` also rewrites the `google_chat` channel alias to `google-chat`. Responsible: Mauricio Quezada (mauricio.quezada@haintech.cl) · AI: Claude Opus 5.5. Why: a repo that already wrote its status map (Tarien's shape) was asked for it again, because the tools ignored `status_flow`.
- E1.F17.T1 / BUG-22 (F-40) — the session hook no longer reports DRIFT when the only commits since the save touch the profile's own files (`state.json`, `handoff.md`, `board.md`, `manifest.md`, `checklist.md`): the recorded commit must be an ancestor of HEAD, then a lower-or-equal uncommitted count also matches and the line reads `matches (<branch> @<commit>; profile-only commits since the save)`; `livestate.profile_only_since` on the python path, the same rule in the degraded bash block; 4 cases × 2 paths in `test-hooks.sh`. Why: committing `state.json` after the capture made every restored session read its own save as drift.
- E1.F17.T2 / F-19 — tracker status names may contain `( )`: `KIND_EXEMPTIONS["status"]` in `safe_values.py`, so `In Progress (QA)` passes `check_status` and `karvey-config.py get management.statuses.<state> --shell`; `a$(b)`, a backtick, `"` and `;` are still refused (the status pattern refuses `` ` `` and `$`); the exemption is per kind, sprints and locations still refuse `( )`. Why: real tracker lists name states like `In Progress (QA)` and the shell-safe read refused them.

### Changed
- Statusline context lights by percent of the window: amber at 30 %, red + TIME TO ROTATE at 50 % (`defaults.json:context_pct`, env `KARVEY_ROTATE_CTX_{YELLOW,RED}_PCT`); the 100k/150k token pair is only the fallback without a window size. Why: fixed tokens fired at 15 % of a 1M window (D-18, F-41, F-33).

## [3.11.4] - 2026-09-23 — hotfix

### Fixed
- **BUG-20 — false "NOT FOUND" drift for in-repo teams.** `state.json` writes `repos[].path` as the repo's name; with `team.json` inside that repo the hook resolved `<repo>/<repo>` and announced, on every session start, that the handoff described a tree that is not there. A path equal to the root's name, `.` or empty now resolves to the root.
- **BUG-21 — git worktrees reported as NOT FOUND.** The live-state check looked for a `.git/` directory; worktrees have a `.git` file. It now asks git (`rev-parse --git-dir`).

### Added
- Regression tests for both (test-hooks.sh 32 cases; 2 fail on 3.11.3). Verified read-only against paautin-kloketen: the section now compares for real and reports actual drift instead of NOT FOUND.

### Why
`agente-kloketen` verified 3.11.3 in its repo and found this residue of BUG-19 in the one section meant to detect real drift; a false alarm there on every start trains agents to ignore it. BUG-21 was found independently as finding F-01 of the `wave1-hardening` architecture and shares the same function. Knowledge sync deferred to the `wave1-hardening` archive.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: hotfix lane (BUG-20, BUG-21 + regression tests) · Apache 2.0

## [3.11.3] - 2026-09-23 — hotfix

### Fixed
- **BUG-18 — the SessionStart hook never ran, since 3.8.0.** `hooks.json` wrapped `${CLAUDE_PLUGIN_ROOT}` in single quotes; bash does not expand it and the CLI does not substitute it, so every session start failed with `No such file or directory` — no handoff reinjection, no drift check, no settings notice. Now double-quoted (the CLI's own guidance), and paths with spaces stay one word.
- **BUG-19 — `team.json` inside the repo resolved a profile that did not exist.** The hook built `<repo>/<ops_repo>/agents/<role>` while `karvey-checkpoint save` writes `docs/spec/agents/<role>/` and `docs/spec/board/<role>.md`. The hook now resolves both layouts (sibling ops repo, or the folder that holds `team.json`), looks the role up by the root's name when the session starts at the root, finds `manifest-compact` inside the profile, and says so when the profile or the handoff is missing. `karvey-checkpoint` and `rules/team.md` document both layouts.

### Added
- Regression tests in `plugins/karvey/hooks/tests/test-hooks.sh` (28 cases): the SessionStart `command` is now executed **exactly as declared in `hooks.json`** — the gap that let BUG-18 through — and the in-repo team layout. 5 of them fail on 3.11.2.

### Why
Reported by `agente-kloketen` (paautin-kloketen), relayed at the owner's request, both reproduced: its session started without identity and `/karvey-checkpoint restore` found no handoff. The 3.11.2 tests ran the script directly and never the declared command, so a quoting error that disabled the hook for every user since 3.8.0 went unseen. Bug-now protocol: hotfix. Knowledge sync (graphify) for this hotfix is deferred to the `wave1-hardening` archive, which moves graphify to archive only (REQ-W1-062).

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: hotfix lane (BUG-18, BUG-19 + regression tests) · Apache 2.0

## [3.11.2] - 2026-09-23 — hotfix

### Fixed
- **BUG-01 — `karvey-init --settings` created a phantom change and a real tracker Epic.** The settings step had no end: after asking the team settings the skill kept going through Step 1…9 (change-id, `spec.json`, `prd.md`, Epic in ClickUp/Jira…). New **Step 0 — settings-only mode**: it reads `project.json`, runs Step 3.2 with the current values pre-filled, merges the answers without dropping untouched keys, writes, reports and **stops**; it never creates a change or any tracker item.
- **BUG-02 — the session-hook settings notice fired where it should not.** It now appears only in a Karvey project (`docs/spec/project.json` or `docs/spec/changes/`) — not in a bare `docs/spec/` (OpenAPI, RFCs, studies) — checks the project root the hook already found instead of the nearest `docs/spec` (nested repos), treats a BOM, a non-object JSON and empty blocks correctly, no longer hangs on a relative `CLAUDE_PROJECT_DIR`, and is worded as information for the user ("settings only, creates nothing"), not as an order to the agent.
- **BUG-03 — an odd `resets_at` took the whole statusline down** (and with it the TIME TO ROTATE warning). The reset clock now accepts seconds, milliseconds and ISO strings, ignores NaN/inf/absurd/past values, and rounds the time left instead of truncating it. Applied to the plugin script and noted for hand-installed copies.
- **BUG-04 — the statusline debug copy was shared across OS users** (`/tmp/.karvey-statusline-last.json`, world-readable, exposing another user's session id). It is now per user and private: `$TMPDIR/.karvey-statusline-last.<uid>.json`, mode 600.

### Added
- **`plugins/karvey/hooks/tests/test-hooks.sh`** — regression tests for BUG-01…04 (21 cases). Passes 21/21 on this release and fails 15/21 against the 3.11.1 hooks (clean, isolated `TMPDIR`).
- **Retroactive QA of `team-adapters`** (`docs/spec/changes/team-adapters/qa/REVISION_PR_17-19_20260923.md`): NOT approved (open highs; security gate passes). Its remaining bugs are tracked as BUG-05+ in `docs/bugs_dev_testing.md` and fixed inside `wave1-hardening`.

### Why
The retroactive QA that the expert panel asked for (the change had reached prod without `approvals.qa`) found a live risk: the notice shown on every session in ~60 `docs/spec` folders recommended a command that could create junk Epics in a client's tracker. Following the bug-now / change-later protocol, the four defects with active impact ship as a hotfix; the structural fixes wait for Wave 1.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: hotfix lane (BUG-01…04 + regression test) from the retroactive QA of `team-adapters` · Apache 2.0

## [3.11.1] - 2026-09-23

### Fixed
- **`docs/karvey.html` picks the browser's language** on a first visit: order `?lang=` → saved choice → the browser's primary language (`navigator.languages[0]`) if it is `en/es/pt/de/zh` → English. A browser in French or Japanese gets English. Tested in 9 cases (explicit param, saved choice, `es-CL`, `pt-BR`, `zh-CN`, `de-AT`, unsupported language, empty, `localStorage` throwing).
- **The tab title follows the language** (`Karvey Method` · `Método Karvey` · `Karvey-Methode` · `Karvey 方法`) instead of staying in English — reported by the Mac review.
- **The hero's decorative wind lines no longer cross the figure cards** (32 / 13 / 22 / 1+2): the SVG now sits behind the content (`z-index`) — reported by the Mac review.

### Why
The real-browser review on the Mac found that the multilingual page still showed an English tab title in every language and that the hero decoration crossed the figures; the owner also asked for the browser's language to be picked on a first visit. (Section added in 3.11.2: the 3.11.1 entry shipped without its "Why", flagged by QA Dimension 6.)

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: change `team-adapters` iteration (QA findings on REQ-ADP-031, browser review) · Apache 2.0

## [3.11.0] - 2026-09-22

### Added
- **Visible version by environment** (`rules/versioning.md`, `karvey-deploy` 2.4-bis): a front shows the **dev version in DEV** — the bumped version as a semver pre-release + build metadata, `v3.10.1-dev.42+74571ae`, with a visible `DEV` mark — and the **release version in PROD**, clean `v3.10.1`. The version is read from the **version file** at build time, never from a pipeline variable (it goes stale silently: the label lies while the code is current); the pipeline stage only provides environment, build number and commit.
- **The canary checks the visible version** (`karvey-deploy` 2.7 / 2.11): DEV must show `-dev` of the version just bumped, PROD exactly the released one; a mismatch is a finding (stale build, wrong stage variable, wrong version source).
- **`docs/karvey.html` is multilingual** (REQ-ADP-031): **English by default** (what renders without JavaScript) with a switch to **Español · Português · Deutsch · 中文**, all in the same self-contained file; `?lang=xx` selects a language and the choice is remembered per browser. Ids are namespaced per language (`en-…`, `es-…`), filters and table-of-contents highlighting work per block, CJK font fallbacks for Chinese. New card on version bump + visible version, in every language. The version history now opens by explaining why it starts at 3.0.0: pure whim — Paáutin, HainTech's flagship software, was at version 3 when the plugin was built; there never was a 1.x or 2.x.

### Why
Owner feedback on 3.10.0: the page had to be consistently in English with a language switch, and the method's "show the version in the front" recommendation did not say that DEV and PROD must show different things — which is exactly how a stuck pipeline variable once made a production front display an old version while running new code.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: change `team-adapters` iteration 1 (spec revision REQ-ADP-031 + versioning rule) · Apache 2.0

## [3.10.0] - 2026-09-22

### Added — team settings on first use (`team-adapters`)
- **A plugin cannot run anything at install time**, so `karvey-init` now asks the team's settings **the first time Karvey is used** in a project (new Step 3.2) and stores them in `project.json`; `/karvey:karvey-init --settings` changes them later. The session hook prints a one-line reminder when a Karvey project (one with `docs/spec/`) lacks them — and stays silent everywhere else.
- **New rule `rules/notifications.md`** — the team's channel: `google-chat | slack | teams | email | webhook | none`, with `target`, `via` (`mcp | cli | webhook | api`) and `events` (`qa`, `deploy`, opt-in `incident`). Same content in each channel's own markup; unset or `none` → skipped **and said**; destinations are never read from a `CLAUDE.md` table; no webhook URL or token in the repo.
- **New rule `rules/management-adapters.md`** — the team's tracker: `clickup | jira | linear | azure-boards | github-projects | spreadsheet | markdown | other`, the logical operations (`create_epic`, `create_feature`, `create_task`, `set_status`, `comment`, `cascade`, `link`, `mirror_backlog`) and how each tool does them, and **5 logical states** — `todo · in_progress · review · done · blocked` — mapped to the team's real statuses in `project.json:management.statuses`.
- `karvey-context` shows the team settings (channel, tool, whether the statuses are mapped).
- **Statusline:** each account window now shows **when it resets and how long is left** — `5h 29% ↻18:05 (1h31m) · 7d 35% ↻Thu 21:20 (2d4h)` — from `rate_limits.*.resets_at`, which the CLI already provided and the script ignored; clock zone via `KARVEY_TZ`.
- **`docs/karvey.html`** — self-contained explainer complementary to the README: what the method is, the name (*Karvey* = **Afán**, Ona language of the Selknam people of Patagonia), and a map of the whole plugin.

### Changed
- **No skill assumes a tool or a status name any more.** ~20 skills and rules that branched on "ClickUp or Markdown" or wrote `listo! para pap` literally now use the logical operations/states; ClickUp commands survive only as the ClickUp adapter (`clickup-protocol.md`) or labeled examples. `karvey-qa` Step 4 notifies the configured channel instead of Google Chat via `CLAUDE.md`; `karvey-deploy` notifies the `deploy` event; `karvey-iterate` the opt-in `incident` event. `PLAN.md` gains the `👀 review` marker. Rule copies re-synced byte-identical.

### Compatibility
- `spec.json:management` still names the tool, so existing changes (`"clickup"`, `"markdown"`) stay valid. A project with only `"management": "clickup"` and no status map keeps working: the statuses are read from the list and the mapping is confirmed once. **HainTech repos must declare `notifications` (Google Chat) in their `project.json`** — until then QA skips the notice and says so.

### Repo maintenance (no plugin change)
- **`graphify-out/`** — knowledge graph of the method's own repo (82 files → 399 nodes, 753 edges, 21 communities), versioned with repo-relative paths; `.graphify_python` (machine-specific) is git-ignored. Closes backlog **BL-01**. It lives outside `plugins/karvey/`, so the installed plugin is unchanged and no version bump applies. README documents how to refresh it (`graphify . --update`).

### Why
A graphify review of the repo showed the "stack-agnostic" plugin hard-coded one team's tooling: QA always notified Google Chat through a HainTech `CLAUDE.md` table, and every status update used ClickUp with a HainTech status name. Another team hit steps that failed or did not apply. The owner asked for both to be asked when the plugin is first used, for any tool (Slack, Jira, a spreadsheet…). Backlog BL-02 and BL-03, promoted to `team-adapters`.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: change `team-adapters` (requirements → impl) · Apache 2.0

## [3.9.1] - 2026-09-22

### Fixed — documentation drift
- **Counts were wrong across the docs.** README said "12-phase pipeline" (it is **13 phases, 0–12**); `plugin.json` / `marketplace.json` said "16 support skills" (it is **18**); `plugins/karvey/README.md` said "12 phases + 12 support"; `rules/support-skills.md` said "12 pipeline phases". All corrected against the 32 skills actually shipped (1 orchestrator + 13 phases + 18 support).
- **`plugins/karvey/README.md` claimed no hook activates on install.** False since 3.8.0: the plugin registers a `SessionStart` hook. The README now separates the hooks **active on install** (session context, inert without a handoff; statusline installed by hand) from the **opt-in** enforcement hooks (`git-flow-guard`, `plan-gate`, via `karvey-guard`).
- **Orchestrator (`skills/karvey/SKILL.md`)** still described an 8-dimension QA and a deploy without PR gates or branch hygiene; now 9 dimensions (standards conformance), deploy phase with PR-gate verification, git-host detection and branch hygiene, and the features list includes branch hygiene. `deploy-workflow.md` is listed as applying to archive and context too.
- **`karvey-context` was missing from the support-skills catalog** (`rules/support-skills.md`) and from the README's support list.
- **Stale local copies of shared rules.** `karvey-init` and `karvey-tasks` carried an old `clickup-protocol.md` that still estimated in **hours** (the 3.4.0 minutes-based AI estimation never reached them), and `karvey-init` / `karvey-requirements` carried an old `living-specs.md` without `findings.md`, `backlog.md`, `incidents-index.md` or the 3.7 `spec.json` fields. Re-synced from `skills/karvey/rules/`; every local copy is now byte-identical to its canonical rule.
- `deploy-workflow.md` referenced `project.json:protected_branches`; the field lives in `branch_flow.protected_branches`.

### Added
- **Skills catalog** in the README: the 32 skills in tables — orchestrator, the 13 phases (what each produces + key rules) and the 18 support skills (role).
- **`docs/spec/backlog.md`** for the method's own repo, with **BL-01**: run graphify (`project.json:knowledge_sync`) over the repo once the pending changes land.

### Why
The owner asked whether the whole plugin documentation reflected what shipped. It did not: counts had drifted over several releases, one README described the hooks incorrectly, and three phase skills were reading outdated copies of shared rules — a silent behavior bug, not just a docs one.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: docs sync (karvey-docs scope) + rule-copy re-sync · Apache 2.0

## [3.9.0] - 2026-09-22

### Added — Branch hygiene (nothing left in branches)
- **New principle 7 + section *Branch hygiene* in `rules/deploy-workflow.md`.** Once a feature branch is absorbed into `{production}` (it already went through `{integration}`), it is deleted — remote and local — in the same deploy. **Absorption is verified, never assumed**: `git branch --merged` (merge commit / fast-forward), `git cherry` (cherry-pick / rebase) and a tree test with `git merge-tree --write-tree` (squash merges, which hide from the other two). Absorbed + open PR → the PR is closed naming what absorbed it. **A branch that is not absorbed is never deleted**: it is reported with its unique commits and PR, and the human decides (rescue / keep / discard). Rescued branches whose patches changed during conflict resolution are closed as *superseded by* the release, with the human's approval. Local deletion uses `git branch -d`, never `-D`, so git's refusal stays a signal. Counts are reported, never cleaned silently.
- **`karvey-deploy` step 2.12** — branch cleanup after the PROD merge and canary, extended to every non-protected branch of the repo; new hard rule and a `Branches:` line in the final output.
- **`karvey-archive` Step 7F — branch sweep**: the change is not closed while one of its branches is alive; the final output reports deleted / kept.
- **`karvey-context`** dashboard: new *LIVE BRANCHES* section (absorbed-but-alive vs carrying unreleased work).
- **`project.json:branch_flow.protected_branches`** (optional globs) — long-lived branches the cleanup never touches, besides `integration`/`production`.

### Added — Standards conformance in QA (rescued from PR #6, originally drafted as 3.6.0)
- **Dimension 9 — Standards conformance (golden path)** in `karvey-qa`. QA now verifies the diff against the engineering standards of the layers it touches (`standards/{layer}.md`), citing the concrete rule breached, and checks that **every departure has an approved entry in `deviations.md`**. A departure with no entry is a **High**, blocking finding; a gray zone the standard does not cover is Medium tagged `gray-zone` and escalates to design mode, never Critical by the reviewer's own reading. A standard in `draft` does not by itself produce Critical/High findings, and with no standards for the project the dimension is recorded as **not evaluated** — which is not the same as conformant. Wired into the review document, the pre-merge checklist and the `approvals.qa` gate.

#### Fixed
- `rules/engineering-standards.md` claimed that "`karvey-qa` (the **Consistency** dimension) checks conformance". It did not: Consistency measures coherence *internal* to the module (patterns, naming, duplication), not agreement with the documented standard. The rule now points at Dimension 9 and says so explicitly, and the conformance gate is described as **three** phases — design decides (`karvey-architecture`), implementation obeys (`karvey-impl`), **QA verifies** (`karvey-qa`) — with its own outcome table.

#### Why
`karvey-impl` already loaded the standards as a hard constraint and required a Deviation Request before departing from them, but **nothing ever verified that it happened**. The method only trusted: an implementation that skipped the golden path without raising the request reached production with no phase having checked. A gate that is never verified is not a gate. Surfaced while adding the same missing dimension to a team's own QA standard, where the identical hole existed.

### Added — PR gates + git host in deploy (rescued from PR #7, originally drafted as 3.7.0)
- **`karvey-deploy` verifies the PR's own gates before requesting the prod OK** (new step 2.9-bis). After opening the PR to `master` the skill retrieved nothing: it went straight to asking for approval. But the PR carries checks the repo enforces — CI and branch policies (build validation, required reviewers, status checks) — that are **not** the release gate of Step 0: they run on *this* PR over the *merge commit*, and fail for reasons the local pre-check cannot see (a conflict with what advanced on production, a policy added since). Now it retrieves them (`gh pr checks` / `az repos pr policy list`), **waits for them to settle** instead of reading the queued state as passed, and routes the outcome: green → continue; running → wait; red → **stop**, back to `karvey-iterate`; none configured → report that production has no gate. New hard rule: **never request the prod OK over a red or unresolved gate** — that turns the human into a rubber stamp, which is what the gate exists to prevent. Bypassing a policy stays the human's explicit decision, never the agent's initiative to unblock itself.
- **Git host detection** (new step 1.5-bis). The deploy platform and the git host are different things — a repo can deploy to Azure and live on GitHub. The skill hardcoded `gh pr create` / `gh pr merge`, so on Azure Repos or GitLab it failed at the worst moment: with the branch already merged into `dev`. It now resolves `project.json:git_platform` (falling back to detection from the remote, and the remote wins if the config is stale) and issues the right CLI: `gh pr` · `az repos pr` · `glab mr`.

#### Changed
- `rules/deploy-workflow.md`: gate verification added as principle 5 and as step 8 of the flow. `rules/project-config.md`: `git_platform` now also documents that it picks `karvey-deploy`'s PR CLI, not only the pipelines `karvey-infra` generates.

#### Why
The method took you to the door of production and never checked whether it opened. Everything a team places on the PR to protect `master` — tests, policies, reviewers — was invisible to the phase whose whole job is crossing that door.

### Why
Merged branches were piling up in the repos — four already-absorbed ones in this very repository, plus two open PRs whose work had never reached `main`. No phase of the method deleted anything after a merge, so cleanup depended on memory. A branch that outlives its merge reads as pending work, invites rebasing on stale code, and hides the branches that *do* carry unreleased work. The two stranded PRs (#6, #7) were rebuilt on top of 3.8 and shipped here instead of being discarded.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5.5 (1M context)
> 🔗 Karvey phase: method extension (deploy-workflow branch hygiene) + rescue of PRs #6/#7 · Apache 2.0

## [3.8.0] - 2026-09-22

### Added
- **The agent handoff, for one agent or many** — see the `karvey-checkpoint` entry below. Previously this lived outside the method, in a separate skill and one project's operations repo.
- **New shared rule `rules/team.md` — the OPTIONAL team layer.** Karvey carried the axis of the *work*; this adds the axis of the *worker*: roles, rotation, addressing, minimal communication and the shared-index trap. **It is opt-in and explicitly not the default**, and the rule opens with *When NOT to use a team*, written from a measured failure rather than from theory: 6 agents, 3 days, **≈US$1,000**, ending back on a single agent — because every hop between sessions is new context for the receiver, there is no shared cache, and at 588k a turn costs **7×** what it costs at 80k. A project that never writes `docs/spec/team.json` never sees this layer, and no phase or gate depends on it (team-layer).
- **New shared rule `rules/verification.md`** — the 18 failure modes that make a green report false, each one from a real incident: a citation is not the thing cited · exit 0 is not success · a green test over code nobody calls · "it failed" vs "it never ran" (the discriminator is duration) · a pipe whose first link fails silently returns a *false answer*, not an error · a filename does not identify a version · a versioned file does not prove what is applied · a switch is not a permission · merging onto someone else's file without bringing their branch reverts their fix **without a conflict** · a frozen baseline ages · sweeping by word leaves the promise intact. Registered for every phase close and shipped as a checklist by `karvey-guard --verify` (team-layer).
- **New skill `karvey-team`** (`init` · `census` · `relay` · `cost`) — sets up and runs a team of agent sessions, and **measures what it spends**. `init` shows the cost table and confirms before writing anything; `census` is an inventory that states in the file that it is **not** an address book; `relay` produces the list of sessions ready to rotate and stops there, because no agent can rotate another or itself; `cost` writes spend per agent against the single-agent counterfactual and **says so in the report** when the team is not paying for itself (team-layer).
- **New skill `karvey-decisions`** (`log` · `cross` · `show`) — one numbered, cross-cutting registry (`D-NN` business, `C-NN` direction) that changes cite via `spec.json:decisions`, so a decision taken in one change is visible to the next. Every entry carries **what it does NOT say**, the field that prevents over-applying a decision. **`cross` is mandatory before any deliverable claims to be blocked on a decision**: in the run behind this release, 14 items were escalated as blocked and **13 were already answered** (team-layer).
- **Plugin `hooks/`** — `hooks.json` with a `SessionStart` hook (`startup|resume|compact|clear`) that **brings the session back to being that agent**: it reinjects identity, manifest, board, checklist and handoff, then **measures** — comparing each repo's branch, last commit and uncommitted count against `state.json` and printing `matches` or `DRIFT — branch X -> Y` — and finally **instructs the session to run `/karvey-checkpoint restore` before anything else** when there is an active change, drift, or no handoff. A hook cannot invoke a skill, so it stops there: crossing decisions, recreating scheduled tasks and proposing the next step remain the skill's job, but the session now starts knowing it must ask, and which claims are already suspect. It resolves a single-agent profile (`docs/spec/agent/`) as well as a team, and **with neither it prints nothing and exits 0**. Plus `karvey-statusline.sh`, the rotation statusline (context, account limits, hours, cost, "TIME TO ROTATE"), which **a plugin cannot declare** — only `agent` and `subagentStatusLine` are accepted — so it ships with the three lines the user pastes once, documented rather than installed behind their back (team-layer).

### Changed
- **`karvey-checkpoint` now saves two faces instead of one, and the second one does not depend on having a team.** `save` keeps writing the change's checkpoint and **always** writes the agent's handoff: `docs/spec/agent/handoff.md` for a single agent, `{ops_repo}/agents/<role>/handoff.md` when a team is configured. A lone agent rotates too, and it is the case with the least safety net — nobody else holds the context. If no profile exists, the save **creates it** (manifest, board, checklist) instead of skipping the handoff. Beyond repository state, the handoff carries **who the agent is and what is not theirs, the standing rules referenced by commit rather than copied, the board of open items, and the closing checklist**. It is **produced, not composed**: its state section is command output, every "done" runs the check that measures it, and scheduled tasks are recorded with their full prompt because they die with a context reset in silence. `save` also writes **`state.json`**, the handoff's machine-readable twin. `restore` contrasts the handoff the same way it already contrasted git — **if the branch it declares no longer exists, it says so before anything in it is believed** — and crosses open questions against the decision log before repeating them (team-layer).
- **`karvey-guard` gains `--verify`**, a read-only checklist over `rules/verification.md` for any deliverable before it reports "done". It is a checklist, not a gate: it does not approve `karvey-qa` on its own (team-layer).
- **Orchestrator, support-skills catalog and README** updated with the two new skills, the two new rules and the optional team layer, stating in each place that **one agent is the default**.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5 (1M context) — as `agente-M5D-arquitecto`
> 🔗 Change: team-layer · Karvey phase: impl
> 📌 Origin: the proposal written on 2026-09-21 after running a 6-agent team on a real product, and the decision to keep teams available **as an option, never as an obligation**.

## [3.7.0] - 2026-09-18

### Added
- **New shared rule `rules/multi-agent.md`** — the minimum contract for work split across several agents and repos. Every cross-agent or cross-repo dependency becomes a pinned, verifiable reference in `spec.json`. Registered in the orchestrator's rules table.
- **Parent / child changes** (`spec.json:links.parent` / `links.children`, `"{change-id}@{repo}"`): a change that spans repos (web + app + DNS + cloud) has a parent in the operations repo and one child per repo. `karvey-init` asks for and fills both sides; the orchestrator shows the children's phases and closes the parent only when every child is deployed (#1).
- **Business decisions linked to changes** (`spec.json:decisions: ["D-NN@repo"]`): requirements cite the decision they come from, and contradicting a linked decision is a blocking review-gate failure in `karvey-requirements` (#2).
- **`[human]` tasks** in `karvey-tasks` with executor, exact command, read-only verification, rollback and an **Executed** record; new **`awaiting-human`** state in `karvey-impl` — the agent prepares and verifies, the human executes, only dependents wait (#3).
- **Pinned inputs from non-programmer agents** (`spec.json:inputs.design | design_system | copy | legal`, `"{repo} {path} @{commit}"`): read at the pinned commit by `karvey-requirements`, `karvey-design-graphic` and `karvey-impl`; input drift is routed by `karvey-iterate` as an automatic ripple candidate (#4, #11).
- **`approvals.prod = { by, date, ref: D-NN }`** — mandatory in `karvey-deploy` before the merge to the production branch, committed in the repo, never delegated to an agent. Keeps the prod approval in git history where the platform cannot enforce required reviewers (#5).
- **Change type `ops`** (`spec.json:type`) for changes without application code (IAM, DNS, secrets, console config): lite requirements → command plan in `karvey-infra` (Step 5-bis) → `[human]`/`[Infra]` tasks → execution → verification → archive (#6).
- **Hotfix lane** (`type: "hotfix"`) in `karvey-iterate` and `karvey-deploy`: **fix + `BUG-NN` + regression test in the same PR**, each chained same-day hotfix its own release, recorded in `revision_history` with `bug` and `release`; an interrupted production E2E run is re-run in full after the hotfix (#7, #13).
- **Documentation-only PR lane** in `karvey-deploy` Step 0-bis + `deploy-workflow.md`: light CI (spec lint) instead of build/test/deploy, merger declared in `project.json:docs_pr.merged_by`, never triggers a deploy (#8).
- **Method readiness checks in `karvey-health`** (Step 6, separate from the 0–10 code score): Karvey skills installed in the agent's environment at the expected `project.json:karvey_version`, with install instructions per environment (#9); every `inputs.* @commit`, `links` and `decisions` reference exists, and drift of the source repo is flagged (#11).
- **IAM binding verification as an infrastructure test**: `karvey-infra` requires a versioned, idempotent script for human-executed IAM plus a read-only `.verify.sh` that asserts the binding itself (member · role · resource); `karvey-test` Step 4-bis runs it and records it in `test_evidence.md` (#12).

### Changed
- **Approvals record who and where** (`approvals.<phase>.by`, `role: human | ceo-delegate`, `date`, `ref: D-NN`) — applied at every gate through `phase-close.md` and `karvey-requirements` (#10).
- `spec.json` schema (`living-specs.md`, `karvey-init`) gains `type`, `links`, `decisions`, `inputs`, `approvals.prod`; `project.json` (`project-config.md`) gains optional `ops_repo`, `karvey_version`, `docs_pr`.
- Orchestrator: "Method directory structure" documents where parent changes and the decision log live and the cross-repo reference formats; the status view shows type, links, decisions, inputs, `awaiting-human` tasks and prod approval; routing notes for `ops`, `hotfix` and parent changes.
- `enforcement.md`: a note — **outside the method** — on approval markers with expiry in the user's own hooks (how a human delegates approvals to a coordinating agent or extends a marker is the user's environment, not Karvey) (#14).

### Why
A real project ran with **four specialist agents in four repos** (designer, web, app, operations), **a browser-operator agent**, and **a human who runs the IAM** grants the agents must not run. Audit D-47 found the code itself in good shape — every web change had its test in CI — but the method had no place for what crossed agent and repo boundaries: a change spanning web + app + DNS + cloud had no parent; business decisions (`D-NN`) were not linked to requirements; design, copy and legal arrived as "the latest file" instead of a pinned commit; human-executed steps lived in chat; prod approvals were not recorded in the repo (no GitHub Enterprise to enforce reviewers); chained same-day hotfixes and a hotfix during a production E2E run had no lane; docs-only PRs had no defined CI or merger; and one agent environment did not even have the skills installed. The records drifted as a result (`revision_history` stopped at 1.0.9, ripple out of date). This release makes each of those dependencies an explicit, verifiable field.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5
> 🔗 Karvey phase: method refinement (multi-agent/multi-repo) · Apache 2.0

## [3.6.0] - 2026-09-08

### Added
- **Causal discipline in `karvey-investigate`** — the skill went from 6 steps to 9, with three new ones placed *before* reading any code:
  - **Step 1 now dates the symptom.** The report timestamp and, where possible, the first occurrence, define the **incident window**. Without it the following steps are inert.
  - **New Step 2 — check what is already known:** incident tracker (`docs/bugs_dev_testing.md` / `incidents-index.md`), engineering standards for the layer, and the `CHANGELOG.md` of the window. The team's accumulated knowledge is evidence, not background reading.
  - **New Step 3 — "what CHANGED?" before "what is WRONG?":** for a new symptom the causal question is historical. Inspect the window with `CHANGELOG`, `git log` / `git log -S`, merges to deploy branches, **and non-code changes** — configuration, CSP/security headers, secrets and rotations, infra, flags, provider or quota changes.
  - **New Step 7 — causal-coherence gate:** a cause that predates the incident window cannot explain a symptom that started inside it. A pre-existing finding is **latent fragility, not the cause**; the investigation continues until the trigger appears. The report must label each finding as *cause* or *contributing fragility*.
- **Trace across repo boundaries** (Step 4): follow the path into the component that holds the next hop. If a repo is out of reach, say so and name what you would inspect — never silently downgrade the conclusion to what happened to be reachable.
- **"Never state what a symbol does without opening it"** (Step 6): project-local helpers, wrappers and loggers behave by project decision, not language default. Claims that depend on them are cited by file and line or they are not evidence.
- **Reuse before invention** (Step 9): before recommending retry/backoff, reconnection or degraded-state indicators, check whether a sibling module already implements it, and cite that as the pattern.
- Three new hard **Constraints**: no undated investigation, no pre-existing finding reported as the cause of a new symptom, no claim about a symbol without having read its definition.

### Why
Post-mortem of a real investigation: an engineer correctly found a fragile reconnection path in a frontend and reported it as the root cause of an incident reported that week. Their own evidence said the code had been unchanged since the first commit — which refuted the causal claim, and went unnoticed. The actual cause was a security header (CSP) put in enforce six days earlier, visible in the `CHANGELOG` and in `git log`, and the decisive corroborating detail lived in a **different repo** (a token TTL). Three of the four gaps were method, not skill: nobody dated the incident, nobody asked what changed, and a pre-existing finding was allowed to stand as a cause. The fourth — asserting a project-local `logger.error` was a no-op without opening it — is now a constraint.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 5
> 🔗 Karvey phase: skill refinement (investigate → causal discipline) · Apache 2.0

## [3.5.0] - 2026-07-05

### Added
- **Visual components catalog** in `karvey-design-graphic` (new Step 9B → `design-components.md`): the design-graphic phase now produces, besides the token-level `design-spec.md`, an **art brief per component** for illustrators / AI art agents. It is **derived exhaustively** from the approved `mockup.html` + `design-spec.md` + requirements (nothing invented, nothing omitted), with a mandatory template: cross-cutting base (style, palette hex light+dark, character/brand, asset format @1x/2x/3x + safe zone), Screens table, Modals/bottom-sheets table, UI components (per component: description · states · required background/fill art), Push notifications table, an optional section per additional surface, and a prioritized deliverable list (1 base illustration + state variants, light+dark, safe zone). **Target-agnostic** per `rules/targets.md` — the component inventory adapts to the declared target(s) (mobile/web/CLI/…), it does not assume web. The knowledge-sync step (renamed 9C) and the phase Output now include `design-components.md`; the orchestrator's PHASE 4 description was updated accordingly.

### Why
Working with designers or illustration agents needs a document that lists **all** visual components (screens, modals, notifications, buttons, inputs, chips, cards, tiles, avatars, bars, badges, bottom-nav, maps, states) with description, states, and a per-component art/background brief in light and dark with a safe zone for text. `design-spec.md` stops at token level and did not fill that gap. Calibrated against a real hand-made catalog (Paáutin wisn mascotas).

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 4.8
> 🔗 Karvey phase: skill refinement (design-graphic → design-components deliverable) · Apache 2.0

## [3.4.0] - 2026-06-23

### Added
- **AI-time estimation in `clickup-protocol.md`**: estimates are expressed in **minutes** (AI execution + human review), not human coding hours. Typical task 10–30 min, **cap ~60 min → split if it exceeds**. Added a per-work-type reference table (SP/endpoint/service/UI/parser/test) and Feature/Epic aggregation. The legacy "6-hour rule" is reframed: it assumed human time; under AI-driven dev the effective cap is ~60 min.

### Why
Reconciliation of the team's `GESTION_PROYECTOS_IA` governance doc into Karvey: that doc was the Paáutin-specific, prior form of the same method (its ClickUp folder is literally "Dev Sprints Metodo Karvey"). The clickup-protocol already covered WBS/naming/dependencies/status; the missing piece was the AI-time estimation model. Project-specific bits (2026 sprint calendar, workspace IDs, client tags) go to the team's `paautin-standards`, not here.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 4.8
> 🔗 Karvey phase: rule refinement (clickup-protocol estimation) · Apache 2.0

## [3.3.0] - 2026-06-23

### Added
- **Engineering Standards layer** (`rules/engineering-standards.md`): a third source of truth beside living specs and `project.json` — the **golden paths** ("how we build here") per layer/target, living in `docs/spec/standards/` (`db.md`, `backend.md`, `frontend.md`, `_index.md`). Fixed file structure (Golden path · MUST · MUST NOT · Gray zones · Migration `current`/`target`) so they are consumed as hard constraints, not prose.
- **Conformance gate (design mode)** in `karvey-architecture` (new Step 4B) and `karvey-impl` (Step 4 execution rules): both load the relevant standards as a **hard input** and validate against them. Conforms → proceed citing the standard; gray zone / outside the standard → **Deviation Request** asked to the user in design mode (never resolved silently); MUST/MUST NOT violation without justification → blocked.
- **`deviations.md`** per change: approved departures from a standard, with rationale + human/AI traceability. Recurring deviations feed back into the standard at `karvey-archive` (mirrors the spec-gap → requirements loop).
- **`karvey-standards`** support skill: lifts the team's engineering golden paths (db/backend/frontend…) from the **real system** into the team's standards repo, in the standard template format. Discovery subagents per layer, picks a golden path where repos diverge, marks undefined things as gray zones, human-approved, re-runnable (`--refresh` promotes recurring deviations into the standard). Registered in the orchestrator + support-skills rule.
- **Method/standards separation (two planes):** the public plugin stays generic and **never** holds a team's concrete standards; those live in the team's own repo. `project.json:standards` gains `source: "local" | "git"` (+ `repo`/`ref`/`path`) so standards can live in a **separate private repo** (e.g. Azure DevOps) that the team installs, read via a cached checkout (`.karvey/standards/`).
- **`project.json:standards`** field (`source`/`dir`/`repo`/`ref`/`path`/`by_layer`) so phases know which standard to load; fallback chain to `_index.md` and, failing that, to "no standard found → ask".
- Migration support as a first-class concept (`Status: migrating`, `current` vs `target`) — covers the frontend v2→v3 case: new work MUST use `target`, touching `current` is a deviation.
- Optional `standards-guard` enforcement hook (opt-in) and registration of the rule in the orchestrator (rules table + `docs/spec/` structure).

### Why
Closed a real blind spot: the method specified *what* to build (living specs) but had no canonical place for *how* to build it per layer, so `architecture`/`impl` drifted on tribal knowledge. Now the pipeline stays inside the house style and, when something must go outside it, it asks in design mode instead of deciding alone — while keeping the generic method and each team's private standards cleanly decoupled.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 4.8
> 🔗 Karvey phase: method extension (rules + architecture/impl gate) · Apache 2.0

## [3.2.0] - 2026-06-17

### Added — Iteration loop (Karvey becomes a spiral, not a line)
- **`karvey-iterate`** support skill: the iteration engine. Reads the change's `findings.md` inbox and routes each finding to its feedback edge — `bug` → incident tracker + QA micro-loop · `spec-gap` → re-open `requirements` (rippling only the affected phases) · `emergent` → discovery backlog. The single place loop logic lives; phase skills only observe/classify.
- **`rules/iteration-loop.md`**: the three feedback edges, the `findings.md` triage artifact, the spec-revision sub-cycle, and the convergence gate (a change is done only when no open `bug`/`spec-gap` remains and all `emergent` are captured).
- **`rules/incident-tracking.md`**: a dedicated `BUG-NN` incident tracker (`docs/bugs_dev_testing.md` per repo + global `incidents-index.md`) with a **state-history** machine (DETECTADO → DIAGNOSTICADO → EN FIX → RESUELTO → REABIERTO). Complementary to ClickUp; integrates with `karvey-investigate` and regression tests.
- **`rules/backlog.md`**: a **dual** discovery backlog — `docs/spec/backlog.md` (source of truth) mirrored into the ClickUp `backlog_list_id`. Swept at archive to promote emergent items into new change-ids (`seed_backlog_id`).
- **`rules/phase-close.md`**: a **mandatory** phase-close ritual (comment + status + cascade on ClickUp/PLAN.md, findings/backlog sweep, spec.json + knowledge update) at the end of every phase and every task — fixes stale ClickUp tasks by making the update a numbered step, not a "should".

### Changed
- **`karvey-mockup`**: navigable depth 3 → **3–4 levels** (sub-flows/states where spec-gaps hide) + a **spec↔mockup validation** pass (Step 4C) to catch spec defects before design/architecture/impl.
- **`karvey-test`**: writes classified findings to `findings.md`, logs bugs to the `BUG-NN` tracker, and routes via `karvey-iterate` (Step 5B/5C/5D).
- **`karvey-qa`**: classifies findings (bug/spec-gap/emergent), routes via `karvey-iterate`, convergence gate before deploy (Step 3E/3F).
- **`karvey-impl`**: per-task completion is now the explicit per-task phase-close ritual.
- **`karvey-archive`**: discovery-backlog sweep (Step 7E) that promotes emergent work into future change-ids.
- **`karvey-browse`**: surfaced defects/gaps are recorded as classified findings.
- **Orchestrator + `living-specs.md`**: state machine gains the feedback edges and convergence gate; `spec.json` gains `iteration_count`, `revision_history`, `seed_backlog_id` and explicit `infra`/`qa`/`deploy` approvals; directory structure adds `findings.md`, `backlog.md`, `incidents-index.md`.
- **`clickup-protocol.md`**: ClickUp updates declared mandatory (phase-close), phase-level status mapping, incident/backlog mirroring.
- Support layer now **14** skills (adds `karvey-iterate`).

### Why
Karvey was a waterfall with a single QA micro-loop: findings that were really spec defects had no edge back to `requirements`, and post-cycle discoveries stayed "in the air". ClickUp updates were documented as a "should" and got skipped, leaving tasks stale, and bugs had no dedicated incremental tracker with history. This release makes the method guide **iteration** itself — close the feedback loops, track incidents with state history, capture emergent work, and enforce the close ritual — so nothing gets dropped.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 4.8 (1M context)
> 🔗 Karvey phase: method evolution (iteration loop + incident tracking + backlog + phase-close) · Apache 2.0

## [3.1.0] - 2026-06-14

### Added
- **`karvey-import`** support skill: converts existing **Kiro** (`.kiro/specs/*` + steering) and **gstack** specs into Karvey's `docs/spec/` structure (prd.md, requirements.md, architecture.md, tasks.md, spec.json/project.json). Non-destructive on the source; missing Karvey-required sections become `> TODO` placeholders; all gates imported as not-approved for re-validation. Registered in the orchestrator, support-skills rule and coverage table.

### Why
Let teams already using Kiro or gstack adopt Karvey without rewriting their existing specs by hand.

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 4.8
> 🔗 Karvey phase: support skill addition · Apache 2.0

## [3.0.0] - 2026-06-14

### Added
- Initial publication of the **Karvey** Method as a Claude Code plugin/marketplace.
- 12-phase pipeline (0–12): grill, init, requirements, mockup, design-graphic, architecture, **infra**, tasks, impl, test, qa, **deploy**, archive.
- Cross-cutting layer of 12 support skills: investigate, second-opinion, health, browse, checkpoint, diagram, docs, guard, devex, retro, scrape, benchmark-models.
- Stack agnosticism (`targets`), PRD base, traceable EARS, blocking security gate (OWASP+STRIDE), ordered deployment with canary, semver versioning, optional hook-based enforcement (git-flow + plan-gate), persistent `goal` field.
- Shared rules: project-config, knowledge-sync, targets, deploy-workflow, changelog-policy, versioning, enforcement, support-skills (+ the previous ones).
- Apache 2.0 license, NOTICE and TRADEMARK.md ("Karvey" trademark = *Afán*, Selknam).
- Skill bodies authored in English with bilingual triggers; generated artifacts follow the project's language.

### Why
Formalize Karvey as HainTech's own stack-agnostic method, absorbing the value of Kiro and gstack, installable/versionable as a plugin (no manual copying of skills).

> 👤 Human owner: Mauricio Quezada Ibáñez <mauricio.quezada@haintech.cl>
> 🤖 AI-assisted: Claude Opus 4.8
> 🔗 Karvey phase: initial publication · Apache 2.0
