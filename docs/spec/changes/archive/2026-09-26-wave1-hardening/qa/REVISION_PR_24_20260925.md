# Code Review — wave1-hardening: feature/wave1-hardening → main

## General Information
- Repository: karvey (Claude Code plugin `plugins/karvey`)
- Stack: Python 3.9+ standard library scripts, bash hooks (bash 3.2 compatible), static HTML/JS method page
- Source branch: `feature/wave1-hardening` (reviewed at `4c9b7c0`)
- Target branch: `main` (`branch_flow.integration` = `branch_flow.production` = `main`)
- PR: #24 (draft)
- Date: 2026-09-25
- Commits included: 125 (to `4c9b7c0`)
- Files modified: 326 (+45 961 / −3 083)
- Reviewers: main QA session plus five review subagents (D1, D2, D3, D4, D7), same model family

## Executive Summary

The review found **20 bugs** and **13 spec-gaps or emergent items**. All 20 bugs were fixed in the QA micro-loop, each with a regression test that fails on `4c9b7c0`: BUG-27..46, all RESUELTO. The most serious were four ways past the guards the change exists to enforce:
- a glob or a shell variable got the agent past protect-paths into the release ledger (BUG-27);
- more than ten push and merge forms reached production unseen by the prod-gate (BUG-28);
- one project-wide prod marker approved every change (BUG-41);
- the conditional "si" counted as an approval (BUG-42).

Two further fixes change existing data or real projects: a spec-merge that silently deleted requirements (BUG-45), and two real legacy shapes that the schema made errors, which blocked the prod-gate (BUG-33).

**Security gate: PASS, QA approved.** The first pass at `4c9b7c0` left the gate closed on two High spec-gaps about what a production approval proves (F-76: `check_prod` did not verify the evidence; F-77: the approval was bound to a change id the agent controls, not to the commits released), with F-79 (a prod approval survived a reopen) in the same decision. The owner decided them in D-34, D-35 and D-36; they were implemented in E1.F18 and re-reviewed in the *QA re-run 2026-09-26* section below, which closes the gate with 0 Critical and 0 High open. The owner's `qa` approval is recorded under D-21.

## Findings by Dimension

Severity: C critical · H high · M medium · L low. "→" gives the routing.

### 1. Security (OWASP Top 10 + STRIDE)

| # | File | Sev | OWASP / STRIDE | Finding | Route |
|---|------|-----|----------------|---------|-------|
| S-1 | `karvey_lib/guards.py` `protect_paths`; `hooks/karvey-hook.sh` | H | A01 / S, T | protect-paths matched only literal paths. `cd .git/kar?ey/ledger && printf … > feat-a.json` forged a ledger entry, and the prod-gate then printed ALLOW. `cd kar*ey` chains and `D=karvey; .git/$D/…` also passed. The reviewer rated this Critical; QA rates it High because the threat model (§3.3) accepts deliberate same-user forgery as a visible residual. | BUG-27 RESUELTO |
| S-2 | `guards.py` `prod_candidates`, `_evaluate_candidate` | H | A01 / E | The prod-gate read push destinations from the command words only, so it missed: `git -c alias.X=push`, configured and shell aliases, `-c remote.*.push` / `push.default`, a configured upstream, `send-pack`, `xargs`, `find -exec` and gh aliases. | BUG-28 RESUELTO |
| S-3 | `guards.py` (`gh api`) | H | A01 / E | `gh api` writes to production through the `merges` endpoint, `git/refs` or GraphQL `mergeBranch` were not candidates. | BUG-28 RESUELTO |
| S-4 | `karvey-state.py` `check_prod` | H | A04 / S | The ledger evidence is not verified: no audit record of the prod marker, and the D-NN is not checked. | F-76 open (owner decision) |
| S-5 | `guards.py` plan-gate classes | M | A04 / T | `cp`, `mv`, `dd of=`, `curl -o`, `git apply` and `rm -f` are not write classes (plan-gate is opt-in). | F-78 → BL-54 |
| S-6 | tests (`notify-confirm.json`, `test_notify_check.py`, `test_safe_values.py`, `test_config_resolve.py`) | L | A05 / I | Two real team chat space ids were in the tests of a public repository. | BUG-32 RESUELTO |
| X-1 | `approval.py` `find_valid`; `karvey-state.py` approve prod | H | A01 / S | A project-wide prod marker approved production for every change and was never consumed. | BUG-41 RESUELTO |
| X-2 | `approval.py` `classify` | H | A04 / S | "revisa si el merge a main rompió algo" recorded a prod approval. | BUG-42 RESUELTO |
| X-3 | `guards.py` `released_change` | H | A04 / S, E | The prod approval is bound to a change id taken from the branch name or PR title, and the ledger never expires. | F-77 open (owner decision) |
| X-4 | `karvey-state.py` reopen | M | A04 / R | A prod approval in the ledger survives a spec-gap reopen. | F-79 open |
| I-9 (part) | `vocabulary.json` | M | A04 / S | "Sí, eso es lo que falla en prod" records a prod marker. | F-81 → BL-56 |

Verified clean:
- no `shell=True`, `os.system` or `popen`; `get --shell` accepts only whitelisted keys that `safe_values` validates;
- atomic writes use a random temp file and 0700/0600 permissions; Edit paths are resolved with realpath;
- `lint.yml` has read-only permissions, no `pull_request_target`, and actions pinned by SHA;
- the page sets text with `textContent` only;
- denial of service: a 3 MB prompt takes 0.34 s, and deep nesting or 100k-part commands block in the documented fail mode;
- the secret scan found nothing.

**Security gate at `4c9b7c0`: FAIL**, because F-76 and F-77 (High) were open. Both were closed by D-34/D-35 (E1.F18); the gate passes in the re-run below.

### 2. Code errors

| # | File | Sev | Finding | Route |
|---|------|-----|---------|-------|
| E-1 | `guards.py` `push_destinations` | H | `git push origin @` on `master` passed both the prod-gate and git-flow. | BUG-28 RESUELTO |
| E-2 | `hookio.py`, `karvey_hooks.py` `main` | M | An exception before any guard (a deleted cwd) made the hook exit 1, which the harness treats as allow. | BUG-34 RESUELTO |
| E-3 | `project.py`, `karvey-state.py`, `karvey-context.py` | M | A non-string `phase` crashed validate, the hooks and the dashboard. | BUG-35 RESUELTO |
| E-4 | `guards.py` `subagent_prompt` | M | False positives on "settings", and a ban-like sentence let a real write through. | BUG-31 RESUELTO |
| E-5 | `karvey-config.py` | L | A list `tool` or `channel` caused a TypeError and exit 5. | BUG-36 RESUELTO |
| E-6 | `atomicio.py` `lock` | L | Race between breaking a stale lock and taking a fresh one; the lock was released without checking ownership. | BUG-37 RESUELTO |
| E-7 | `karvey-hook.sh` no-python prod-gate | L | `HEAD` and `@` on production are not blocked without python. With python and no main/master ref, `git push origin master` passes. | F-80 → BL-55 |
| E-8 | `karvey-spec-merge.py` | L | The BOM and CRLF of the living spec were lost. | BUG-38 RESUELTO |
| X-5 | `karvey-state.py` `consume_on_close` | M | Closing a phase that has no approval consumed a prod marker. | BUG-43 RESUELTO |
| X-6 | `karvey-state.py` `--fix` | M | Migration dropped fields of legacy transitions and of `gates_skipped`. | BUG-44 RESUELTO |
| X-7 | `karvey-spec-merge.py` | H | A duplicated REMOVED id, or MODIFIED plus REMOVED of the same id, deleted neighbouring requirements with exit 0. | BUG-45 RESUELTO |
| X-8 | `karvey-spec-merge.py` | M | A blank line inside an item cut it short. An ADDED section written as `### REQ-` headings was a silent no-op. | F-84 → BL-59 |

Verified clean:
- Python 3.9 syntax and APIs;
- the payload parser on hostile input;
- the fail modes of each guard;
- every subprocess call has a timeout;
- bash 3.2 constructs;
- the page JS passes `node --check`.

### 3. Consistency

The new `subagent-prompt` guard is listed the same way in `hooks.json`, the registry, the dispatcher, `enforcement.md`, `hooks/README.md` and architecture §1.3/§3.2. Other findings:

| # | Finding | Route |
|---|---------|-------|
| C-1, C-2 | The `docs/karvey.html` hooks section describes the 3.11 model and fixed token thresholds. | F-87 → BL-53 |
| C-3, C-5, C-7, C-9, C-10 | Stale sentences: checkpoint drift, markers in `multi-agent.md`, LICENSE footers, a missing pending-sync row, §3.5 wording. | F-87 → BL-53 |
| C-4 | The ban line appears only in impl's subagent prompts, not in the other skills that dispatch subagents. | F-85 → BL-60 |
| C-6, C-8 | Duplicated helpers; TTL literals in three places. | F-86 → BL-61 |
| C-11 | The method text cites this repository's own D-/BUG-/H- ids. | F-88 → BL-53 |

Verified clean:
- the phase graph agrees with the text;
- every documented CLI flag exists;
- no dangling references to deleted rule copies;
- no tabs or CRLF.

### 4. Impact on existing modules

| # | Sev | Finding | Route |
|---|-----|---------|-------|
| I-1 | H | subagent-prompt blocked ordinary UI and settings prompts in real projects. | BUG-31 RESUELTO |
| I-2 | H | Real `project.json` files list `repos` as objects and real specs have `generated` as a date. Both were errors, and the prod-gate blocked with "valid spec.json". | BUG-33 RESUELTO |
| I-3 | M | The prod-gate block did not say how to record the approval. | BUG-30 RESUELTO |
| I-4 | M | `git push origin --tags` from main was blocked. | BUG-29 RESUELTO |
| I-5 | M | protect-paths blocked commit messages and echo text that only mentioned the paths. | BUG-39 RESUELTO |
| I-6 | M/L | A copied statusline shows `rot?`, and the token env vars are ignored under percent thresholds. | F-82 → BL-57 |
| I-7 | M | Without python, an enabled git-flow blocks every commit. Hook latency (135–167 ms median) is over the §9 budget (merged with F-55). | F-83 → BL-58 |
| I-8 | L | The `karvey-guard` shim text does not match what 3.11 projects copied. | F-87 → BL-53 |

Verified clean:
- in non-Karvey repositories every hook is silent;
- no dangling references to the deleted files;
- the Windows advisory job passes.

### 5. Environment variables

This is a plugin, with no Dockerfile or pipeline secrets. The variables it reads:
- `CLAUDE_PLUGIN_ROOT`, `CLAUDE_PROJECT_DIR` (set by the harness);
- `KARVEY_TZ` and `KARVEY_ROTATE_*` (documented in `hooks/README.md`);
- `KARVEY_COMPAT_MARKER` (documented, D-11);
- `XDG_STATE_HOME`, `HOME`, `TMPDIR` (standard; each has a fallback).

Undocumented: `KARVEY_HOOK_SELFTEST` (diagnostic, can only add a block), `KARVEY_DEFAULTS_JSON` (set internally by the statusline) and `KARVEY_SKIP_TABLES` (test runner). These are routed to F-87 → BL-53. None lacks a fallback.

### 6. Versioning

| Item | Result |
|------|--------|
| `unreleased-section` | PASS: `## [Unreleased]` has one line per task or fix, including the QA fixes BUG-27..46. |
| `one-bump-per-release` | PASS: `plugin.json`, `marketplace.json` and the page stay at 3.11.4. The bump to 3.12.0 happens at the release step (E1.F16). |
| `versions-agree` | PASS: 3.11.4 everywhere, and L-12 is green. |
| `changelog-why` | FAIL at review, FIXED: [Unreleased] had no human owner or AI model. **BUG-40 RESUELTO**: the section now carries the policy's traceability block. |

### 7. Second opinion (cross-model)

**Model:** same model family as the main review. This was an **intra-model adversarial review (CHALLENGE mode)**, not a cross-model one: no external model API key is configured, so no GPT or Gemini run was possible. Treat it as weaker evidence than a true cross-model review.

**New findings:**
- X-1 High → BUG-41;
- X-2 High → BUG-42;
- X-3 High → F-77 open;
- X-4 Medium → F-79 open;
- X-5 Medium → BUG-43;
- X-6 Medium → BUG-44;
- X-7 High → BUG-45;
- X-8 Medium → F-84;
- X-9 Medium (L-06 false negative, a REQ-W1-013 regression could have passed CI) → BUG-46.

**Discrepancies:**
1. The second opinion rates the approval classifier false positive and the unverified evidence (S-4) as High. QA agrees:
   - X-2 is fixed;
   - S-4 is F-76, High, open.
2. It rates the spec-merge BOM/CRLF finding as cosmetic next to X-7 and X-8. QA agrees; it was fixed anyway (Low).
3. The D1 reviewer rated S-1 and S-2 Critical. QA rates them High, because of the accepted same-user residual in §3.3. Both are fixed, so the rating does not change the gate.

**Verified clean by the second opinion:**
- CI jobs all test something (the node job runs all 22 page tests, and the table runner fails on an empty selection);
- notify confirmation binding;
- compare-and-swap in `transact`;
- the dashboard is read-only.

### 8. Visual audit (implemented vs design-spec)

No `design-spec.md` exists for this change: its layers are Backend and Infra, and no mockup phase was run. `docs/karvey.html` changed only in logic (BUG-10..14: language switch, hash handling) and in one hidden-by-default element (`.lang-note`). The static audit found:
- balanced markup;
- the `--ink-2` token defined for both themes;
- reduced motion respected;
- the language nav hidden until JS binds it (BUG-14).

The page's behaviour is covered by the 22 node tests. **No rendering capture:** the review host has no browser and no design-spec to compare against, so this is a partial audit and a manual area below. Its text is out of date (C-1, C-2) → F-87.

### 9. Standards conformance (golden path)

**Not evaluated.** `docs/spec/project.json` declares no `standards` and there is no `docs/spec/standards/`, the same as at the architecture gate (§ Engineering-standards conformance gate). Not evaluated does not mean conformant.

## Summary Table by Severity

At review, before the micro-loop fixes:

| Severity | Count | Open after this QA |
|----------|-------|--------------------|
| Critical | 0 (two proposed by a reviewer, rated High by QA) | 0 |
| High | 11 | 2 (F-76, F-77), closed by D-34/D-35 in the re-run |
| Medium | 19 | F-79 closed by D-36 in the re-run; the rest deferred to the backlog |
| Low | 9 | deferred to the backlog |

**Bugs:** 20 found, 20 RESUELTO (BUG-27..46). **Spec-gaps and emergent items:** 13 (F-76..F-88). Of these, 3 needed the owner (F-76, F-77, F-79) and were closed by D-34..D-36 and E1.F18; 10 are deferred to BL-53..BL-61.

## Pre-merge checklist
- [ ] All critical findings resolved: none open
- [x] All high findings resolved: F-76 and F-77 closed by D-34/D-35 (re-run below)
- [x] Security gate passed (OWASP Top 10 + STRIDE with no criticals or highs): yes, in the re-run at `c2b0a7b`
- [x] Second opinion executed and integrated (intra-model; declared above)
- [x] Visual audit vs design-spec with no blocking deviations (no design-spec; static audit only)
- [ ] Standards conformance verified: not evaluated (no standards)
- [x] Environment variables verified
- [x] Tests pass: unit 735, regression 10, test-hooks 68, guard tables 363 cases / 438 runs, page 22, lint 0 errors, `validate --all` 0 errors
- [ ] Production build successful: n/a (no build). The release step (E1.F16) is pending.

## Areas requiring manual testing
- **Prod approval end to end** (F-76 and F-77 decided in D-34/D-35): a real session approves production with the owner's own words, runs `approve prod`, then does the release PR merge through the gate (E2E-04 in `karvey-deploy`).
- **Method page rendering** in a browser, both themes and phone width (D8 was static only).
- **Real projects with legacy shapes** (`repos` objects, date `generated`): `validate --all` and a prod-gate dry run after upgrading.

## QA re-run 2026-09-26 — D1 and D7 on `13170b1..HEAD` (D-34..D-36, E1.F18)

Scope: the owner's decisions D-34 (the evidence is the approval hook's audit record), D-35 (the approval is bound to the approved head commit for 24 h) and D-36 (a reopen supersedes it), routed by `karvey-iterate` as a spec revision (REQ-W1-017/023 rewritten, architecture revision 4, tasks E1.F18) and implemented in `7e110f3`. D1 by the main QA session; D7 by an independent review subagent of the same model family (intra-model, declared), in three passes (7e110f3, 0ce4a7b, d0153c2, c2b0a7b), each claim reproduced through the table runner and with real pushes to a throw-away bare origin.

### 1. Security (OWASP Top 10 + STRIDE) — the diff since 13170b1

| # | File | Sev | OWASP / STRIDE | Finding | Route |
|---|------|-----|----------------|---------|-------|
| S-7 | `guards.py` push parsing | C | A01 / E | Push forms released an unapproved commit: wildcard and matching refspecs, a configured mirror, `push.default matching`, a tag shadowing the pushed branch, a second production destination (D7 X-1..X-7); `refs/*:refs/*`, `-on`, `--mirro`, a remote name with a slash (D7 N-1..N-4); `--dry-run --no-dry-run` and `--repo` (N-5, N-6). Several predate this diff; REQ-W1-023 now claims every form. | BUG-47, BUG-50, BUG-51 RESUELTO |
| S-8 | `guards.py` merge candidates | M | A04 / T | A deferred merge (`gh --auto`, `az --auto-complete`, `glab mr merge`) could land a commit pushed after the check (X-9). | BUG-48 RESUELTO |
| S-9 | `guards.py` `other_writer` | H | A04 / T | A release chained with, or backgrounded next to, a command that moves the branch was checked before the move (found by D1). | fixed in E1.F18.T2 / 0ce4a7b (pg5-07, pg5-08, pg5-12) |
| S-10 | `karvey-state.py` `cmd_reopen` | L | A04 / R | The ledger was superseded after spec.json was written (X-10). | BUG-49 RESUELTO |
| S-11 | `karvey-state.py` `approve prod --sha` | M | A04 / S | The commit is named by the agent; the marker does not bind the human's words to it (X-8). | F-90 → BL-62 (owner decision) |
| S-12 | state machine | L | A04 / R | `approve prod` happens in `deploying`, where `reopen` is refused, so D-36 covers only earlier approvals (X-10). | F-91 → BL-63 |
| S-13 | same-user residual | L | STRIDE S, T | An agent can still forge a ledger entry **and** its audit line in the protected state dir; a writer started in an earlier call can move a ref in the milliseconds between the check and the push. Accepted residuals (§3.3), now two deliberate steps. | documented; BL-62 |

Verified clean in the new code:
- `check-prod` refuses a ledger entry without the approval hook's audit line (hash, session, time), a marker of another change, a legacy entry without `head_sha`/`expires_at`, an expired one and a different released commit (unit `ProdEvidence`, `ProdShaBinding`; tables pg5-01..06, pg5-11);
- git is called with argv only; a revision starting with `-`, `$` or a backtick is refused; SHAs from `gh`/`az`/`glab` JSON are validated against a hex pattern; the audit read is bounded (current + rotated log, 1 MB each);
- every unresolvable case fails closed (PR answer without head commit, unknown push option, unparsable `@{push}`); no crash-open (the dispatcher blocks on exceptions and on the watchdog);
- the documented deploy flow (karvey-deploy 2.9: `approve … --sha <PR head>`, then the merge on its own) is allowed (pg1-02, pg5-10, pg6-09);
- no-python fallback: it **fails closed, keeping its documented behaviour**: every PR merge blocks, and a push blocks when it names a production branch, has no refspec, or (new) has a wildcard or matching refspec. It never reads the ledger, so the commit and expiry checks do not apply there; `HEAD`/`@` on production without python stays F-80 (BL-55, low).

**Security gate: PASS.** No Critical or High is open: S-7 and S-9 are fixed with regression tests red first; S-11 (Medium) and S-12 (Low) are routed to the backlog for the owner; S-13 is the accepted same-user residual.

### 7. Second opinion (intra-model, three passes)

- Pass 1 on `7e110f3`: FAIL (X-1..X-7 push forms, X-9 deferred merges, X-8 agent-chosen SHA, X-10 reopen). Micro-loop: BUG-47, BUG-48, BUG-49; X-8 → F-90 / BL-62, X-10 reachability → F-91 / BL-63.
- Pass 2 on `0ce4a7b`: X-1..X-7 and X-9 fixed; new N-1..N-4 (`refs/*`, `-on`, `--mirro`, slashed remote). Micro-loop: BUG-50, plus the no-python fallback now blocks wildcard and matching refspecs.
- Pass 3 on `d0153c2`: N-1..N-4 fixed; new N-5 (`--dry-run --no-dry-run`), N-6 (`--repo`). Micro-loop: BUG-51.
- Pass 4 on `c2b0a7b`: every reproduction (40 table cases, real pushes to a bare origin) blocks; option-state variants (`-n --no-dry-run`, `--no-dry` prefix, repeated `--repo`) block; the approved commit pushed alone is allowed. **PASS on "no High/Critical"** for the push and merge forms tested; the reviewer notes this is not a proof that no other push form exists. The authoritative production control stays server-side branch protection (Q-A8), which the gate complements.

### Result

| Severity | Found in this re-run | Open |
|---|---|---|
| Critical | 1 (S-7, three batches) | 0 |
| High | 1 (S-9) | 0 |
| Medium | 2 (S-8, S-11) | S-11 routed to BL-62 (owner decision) |
| Low | 3 (S-10, S-12, S-13) | S-12 → BL-63; S-13 accepted residual |

Bugs: BUG-47..51 RESUELTO, each with its regression test red first (tables pg6-01..20; unit `ReopenSupersedesProd`). Spec-gaps F-76, F-77, F-79 closed by revision 4; F-90, F-91 routed.

Tests at `c2b0a7b`: unit 751, regression 10, test-hooks 67, guard tables 396 cases / 474 runs (78 nopy), page 22 — all green; lint 0 errors / 3 warnings; `validate --all` 0 errors / 33 warnings.

**QA approved** by the owner (`qa` approval recorded with `ref: D-21`). Manual area still open: the prod approval end to end in a real session (E2E-04), now with `approve … --sha <PR head>`.
