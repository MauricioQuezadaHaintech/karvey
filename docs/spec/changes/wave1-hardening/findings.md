# Findings: wave1-hardening

Triage inbox of this change (`plugins/karvey/skills/karvey/rules/iteration-loop.md`). `karvey-test`,
`karvey-qa` and `karvey-browse` append observations here with a type guess; only `karvey-iterate` routes them.
Types: `bug` → incident `BUG-NN` in `docs/bugs_dev_testing.md` (continue from BUG-18) · `spec-gap` → re-open
requirements · `emergent` → `docs/spec/backlog.md` (continue from BL-44).

Status: `open` → `routed` → `closed`. Convergence needs no `open`/`routed` `bug` or `spec-gap`.

The open work inherited from `team-adapters` is **not** re-logged here: it lives in
`docs/spec/changes/team-adapters/findings.md` and `docs/bugs_dev_testing.md`, and is covered by
REQ-W1-080..106 (traceability in `requirements.md`; convergence rule REQ-W1-107, REQ-W1-108).

| # | Date | Source phase | Type | Severity | Title | Status | Routed to |
|---|------|--------------|------|----------|-------|--------|-----------|
| F-01 | 2026-09-23 | architecture | bug | medium | Session hook checks for a `.git` *directory*, so every git worktree is reported as "NOT FOUND" (worktrees have a `.git` file). Fixed by design with `git rev-parse --git-dir`. | routed → BUG-21, RESUELTO in hotfix 3.11.4 |
| F-02 | 2026-09-23 | architecture | spec-gap | medium | Hook payload field names are unconfirmed (installed CLI 2.1.281 vs local docs: `prompt` vs `user_prompt`, …). Tolerant parser designed (assumptions A-1..A-10); first test task captures one real payload per event. **T-0 result (E1.F1.T1, 2026-09-24, CLI 2.1.281 headless `-p --setting-sources project,local --plugin-dir`, fixtures in `plugins/karvey/tests/fixtures/payloads/`):** A-1 confirmed — every event carries `session_id`, `transcript_path`, `cwd`, `hook_event_name`; tool events add `tool_name`, `tool_input`, `tool_use_id`, `prompt_id`, `permission_mode`; PostToolUse adds `tool_response` (camelCase inside: `filePath`, `oldString`…) and `duration_ms`; SessionStart has no `tool_*`/`prompt_id`. A-2 confirmed — the text is in `prompt`; `user_prompt` does not appear (keep it only as a fallback). A-3 confirmed — Write and Edit both use `tool_input.file_path` (Edit adds `old_string`, `new_string`, `replace_all`); Bash is `tool_input.command` plus `description`. NotebookEdit not captured. A-4 confirmed — PostToolUse exit 2 feeds stderr back to the model as a blocking error (`hook_blocking_error` in the transcript); the tool has already run (the file was written). No JSON fallback needed. A-5 confirmed — the matcher selects by source (a `compact`-only matcher did not fire on startup); the payload also carries `source` (`startup`/`resume`), and resume adds `seconds_since_last_response`, `context_tokens`, `prompt_cache_likely_expired`, `estimated_cache_write_usd`. A-6 confirmed — `hookSpecificOutput.additionalContext` on stdout reaches the model as "SessionStart hook additional context: …". A-7 confirmed — a PreToolUse hook past its 3 s timeout is cancelled (`hook_cancelled`, `timedOut: true`) and the tool call proceeds: non-blocking. A-8 corrected (nuance): the transcript is JSONL and the prompt is a `type: "user"` line with `message.role: "user"` and `message.content` a string; but tool results are also `type: "user"` lines (content = list of `tool_result`), and the prompt text also appears in `queue-operation.content` and (truncated) `last-prompt.lastPrompt`. The cross-check must hash only user lines whose content is a string or `text` blocks, never `tool_result` — delta for F3.T5 (see F-04). A-9 not tested — the capture excludes the user's settings by design; unchanged. A-10 not testable on Linux; unchanged (unit-tested in F4.T1). No correction to A-2/A-3/A-4/A-6, so F4.T1 / F5.T7 / F6.T1 need no delta. | closed | E1.F1.T1 (A-8 delta → F-04) |
| F-03 | 2026-09-24 | tasks | spec-gap | low | The architecture's `hooks.json` example single-quoted `${CLAUDE_PLUGIN_ROOT}` — the exact BUG-18 defect. Corrected in architecture.md to double quotes; tasks widen the regression test to run every declared hook command. | resolved in architecture.md |
| F-04 | 2026-09-24 | impl | spec-gap | low | A-8 transcript cross-check: `tool_result` entries are also `type: "user"` lines, so "hash every string value of lines whose type/role is user" (architecture §3.6, §3.3) would count agent-influenced tool output as the human's prompt. Advisory-only check, so no guard is weakened; E1.F3.T5 should hash only user lines whose `message.content` is a string or `text` blocks. **Resolved 2026-09-24 in E1.F3.T5:** `approval.verify_transcript` hashes only `type/role: user` lines whose content is a string or a list made only of `text` blocks; a line with any `tool_result` block, `queue-operation` and assistant lines never count (`test_marker.py: test_tool_result_lines_never_count`). | resolved | E1.F3.T5 |
| F-05 | 2026-09-24 | impl | spec-gap | low | Architecture §2.2 lists the schema subset without `maxLength`, yet §2.2/§2.6 use it (`evidence.prompt_excerpt`, `approval_vocabulary`); it also does not say which annotations are allowed (`$schema`, `$id`, `$comment`, `$defs`, `x-karvey-note/-default/-safe/-schema-version`) nor how a `x-karvey-severity: warning` node reports a legacy shape that *validates* (`gates_skipped`, unknown approval key, string `management`, top-level `clickup`). E1.F2.T3 adds `maxLength`, accepts exactly those annotations, and reports a matched warning node that carries an `x-karvey-note` as a `schema.legacy` warning (documented in `schema_lite.py`). §2.2 and L-17 should say the same. **Resolved 2026-09-24:** architecture §2.2 now lists `maxLength` and cross-file `$ref`, the allowed annotations and the `schema.legacy` warning behaviour, matching `schema_lite.py` (L-17 implements that list in E1.F10.T3). | resolved | architecture.md §2.2 |
| F-06 | 2026-09-24 | impl | spec-gap | medium | In `spec.schema.json` (§2.2, implemented verbatim by E1.F2.T5) `$defs/approval.date` carries `x-karvey-format: datetime-tz` **without** `x-karvey-severity: warning`, so every legacy date-only approval (`"date": "2026-09-22"`, the shape in all three spec.json files of this repo) is an **error** in advisory mode. That contradicts REQ-W1-003 (legacy shapes are warnings, exit 0) and §2.7 (no existing spec.json becomes invalid in 3.12.x), and §2.5 has no `--fix` rule for dates, so E1.F15.T3 ("this repo validates with 0 errors") cannot pass as specified. Proposed: add `x-karvey-severity: warning` to `approval.date` (not to `prodApproval`, which must stay strict), or a `--fix` rule that is not allowed to invent a time. Needs a decision before E1.F3.T1. **Resolved 2026-09-24 as REQ-W1-003 requires** (legacy shapes are warnings in advisory mode): `approval.date` carries `x-karvey-severity: warning`, so a date-only value is a warning (error in strict); `prodApproval.date` stays an error. Unit tests `test_schemas.py: test_legacy_date_only_approval_is_a_warning`, `test_prod_date_only_stays_an_error`; architecture §2.2 updated. No `--fix` date rule (it would invent a time). | resolved | REQ-W1-003 (spec.schema.json, architecture §2.2) |
| F-07 | 2026-09-24 | impl | spec-gap | low | Same class as F-06, for shapes §2.5 migrates but the schema rejects: a hand-written `phase_history` transition `{from,to,at}` (this change's own spec.json), `approvals: null`, and a legacy phase value of the exact **or proposed** tier are schema errors, while REQ-W1-003 says legacy shapes are warnings in advisory mode. E1.F3.T1 resolves it in `karvey-state.py validate`, not in the schema: each recognised shape replaces its raw schema errors with one `state.legacy_*` issue (warning in advisory, error in strict) that names the field and the `--fix` that migrates it. §2.3 lists only the exact tier as a warning; the proposed tier is treated the same because its values are listed legacy spellings (§2.5), and `--fix` still needs `--accept-proposed` to apply them (D-09). Unknown values (`qa-approved`, `shipping`) and the unmappable ones (`iterate`, null) stay errors. Owner may prefer the proposed tier as an error: a one-line change. | resolved | E1.F3.T1 (REQ-W1-003) |
| F-08 | 2026-09-24 | impl | emergent | low | E1.F4.T3 registers every guard as an allow-stub (batch 3 wires them), so nothing in the dispatcher can block yet, while E1.F4.T4's `smoke.json` must hold a **block** case. Added a diagnostic, block-only `selftest` guard (`karvey_hooks.py` and the no-python classifier): it runs only when `KARVEY_HOOK_SELFTEST=1` is in the hook's environment and blocks a call whose command or path contains `KARVEY-SELFTEST-BLOCK`. It can add a block, never remove one, so it cannot weaken a guard. Owner decides whether to keep it as a permanent dispatcher self-test (recommended: keep; it proves the exit-2 path in CI independently of any guard's logic) or remove it once E1.F5.T1 gives smoke.json a real block case. **Update 2026-09-24 (batch 3, lane A):** every real guard now has block cases in its own table (protect-paths, plan-gate, git-flow, prod-gate, spec-write, shims), so the selftest guard is no longer needed for coverage; it was kept unchanged pending this decision. | open | owner |
| F-09 | 2026-09-24 | impl | emergent | low | protect-paths blocks Edit/Write on `${CLAUDE_PLUGIN_ROOT}/**` (§1.3, STRIDE "Tampering / plugin scripts"). A session that loads the plugin **from the working copy it is editing** (`--plugin-dir ~/Dev/karvey/plugins/karvey`, dogfooding) can therefore not edit the plugin's own source with Edit/Write (Bash writes are blocked only for redirections and mutating commands; running the plugin's scripts and copying templates out of it stay allowed). Implemented as specified (E1.F5.T1, `protect-paths.json` pp-10/pp-11). Owner decides: keep (recommended — develop Karvey with the installed release or a second worktree as the plugin dir, never the tree being edited) or exempt a plugin root that lies inside the session's own git top level. | open | — |
| F-10 | 2026-09-24 | impl | emergent | low | **Merge reconciliation (lane A ↔ lane B).** The guards need the §3.5 reviewed-line reads (`enforcement.*` from the working copy and from `origin/{production}`, TTL, vocabulary) before `karvey-config.py` (E1.F7, lane B) exists. Lane A adds minimal local helpers in `karvey_lib/guards.py` (`project_wc`, `project_reviewed`, `enforcement`, `reviewed_setting`, `opt_in_enabled`, `prod_gate_setting`, `ttl_min`), built only on `project.py` (`load_project_json`, `read_reviewed_project_json`). At merge the orchestrator should either keep them (they are the hook-side, no-subprocess path) or point them at lane B's resolver, keeping the §3.5 rules and the table cases that pin them (`approval.json` ap-34..37, `prod-gate.json` config cases, `plan-gate.json`/`git-flow.json` enable cases). **Resolved at integration (2026-09-24):** there was no duplication with lane B — the one reviewed-line read is `project.read_reviewed_project_json`, used by both; `karvey-config.py` reads it on `origin/{integration}` for the settings fallback (REQ-W1-083), the guards on `origin/{production}` for `enforcement.*`, so the hook-side helpers stay (no subprocess into the resolver). The duplicate was in lane D's dashboard, which re-implemented the §3.5 switch rules and had drifted (git-flow/plan-gate ignored the reviewed line; TTL read from the working copy). The rules now live once in `karvey_lib/project.py` (`enforcement_of`, `reviewed_value`, `opt_in_state`, `prod_gate_state`, lazy reviewed read) and both `guards.py` and `karvey-context.py` use them; guard texts unchanged; all 289 table cases green; `test_project.py: EnforcementRules`, `test_context.py: test_opt_in_guard_on_when_only_the_reviewed_line_enables_it`. | resolved | karvey_lib/project.py |
| F-11 | 2026-09-24 | impl | spec-gap | low | §3.3 step 2 drops **any line longer than 200 characters** as pasted material. A human who types a long single-line approval ("ok, adelante, y además …" > 200 chars) therefore records no marker (the safe side: fail open, no `[karvey] approval recorded` line, the human repeats a short "ok"). Implemented as specified and pinned as a `limitation` case (`approval.json` ap-39). Owner may prefer: apply the 200-char rule only to lines that look pasted (timestamps, `key: value`, log levels) or only after the first line. | open | — |
| F-12 | 2026-09-24 | impl | spec-gap | medium | §3.4 defines the prod-gate **production set** as `branch_flow.production ∪ origin/HEAD ∪ {master, main on the remote}`. In the HainTech repos the remote default branch (`origin/HEAD`) is often the **integration** branch (`dev`, Azure Repos' default), so taken literally every PR into `dev` would need a prod approval, contradicting the table case "PR into dev → allow silent" and flow step 3. E1.F5.T5 removes the integration branch from the set when it differs from production (`guards.production_set`); a rename of `production` still cannot hide `main`/`master`/`origin/HEAD` (pg1-11). §3.4 should say "minus the integration branch when `I ≠ P`". The same holds for git-flow, which uses `branch_flow.production` (fallback `main`/`master`) and never `origin/HEAD`. **Resolved 2026-09-24:** per D-15: `guards.production_set` removes the integration branch only when it differs from production and is not `main`/`master` (before, an integration branch named `main` was dropped from the set); architecture §3.4 and the §3.5 table now say so; table cases pg3-01..06 (PR / merge / push into `dev` with `origin/HEAD` = `dev` not gated; `master`, `main` and `HEAD:master` gated with `origin/HEAD` = `dev`; `integration: main` still gated — red on the previous code). | resolved | D-15 · architecture.md §3.4 · guards.production_set · tables/prod-gate.json pg3-01..06 |
| F-13 | 2026-09-24 | impl | spec-gap | low | E1.F6.T1 says "the 32 existing `test-hooks.sh` cases must stay green after the port", but one of them (BUG-19, "manifest-compact, manifest and handoff reinjected") asserts that **both** the compact and the full manifest are injected — exactly what REQ-W1-046 (H-09) forbids. The assertion is changed to "compact XOR full" (the compact one wins); the full manifest is still checked when no compact exists (`session.json`). Every other legacy case passes unchanged against the Python port. | resolved | E1.F6.T1 (REQ-W1-046) |
| F-14 | 2026-09-24 | impl | spec-gap | low | When the profile lives **inside** a repo it measures (in-repo team, solo `docs/spec/agent/`), writing `state.json` changes that repo's uncommitted count, and committing the handoff afterwards changes its commit. `karvey-handoff-capture.py` re-measures after writing so the count matches the tree *with* `state.json` (the 3.11.4 convention the BUG-20 cases encode), but it cannot foresee a later commit. The `karvey-checkpoint save` text (E1.F12.T7) must say: commit the rest of the handoff first, run the capture last, and leave `state.json` uncommitted (or commit it and accept one "commit" drift line). | open | E1.F12.T7 |
| F-15 | 2026-09-24 | impl | security | medium | `karvey-config.py notify-check --confirm` is not tied to a human: the agent can run `--confirm` itself and so accept a changed notification destination with no human involved. REQ-W1-097 is enforced only by instruction. Candidate fix: take the confirmation from the UserPromptSubmit marker path (a human prompt records it, as the approval hook does), and add the notify record to the protect-paths needles. (lane B, E1.F7.T3) **Resolved 2026-09-24:** per D-16: the UserPromptSubmit hook records `confirmo notificacion <code>` / `confirm notification <code>` (`<code>` = first 8 hex of the destination hash) as a marker under `approvals/notify/`, scoped to the clone, the project key and that destination, TTL `plan_marker_ttl_min`; `notify-check --confirm` records the destination only against a live matching marker (single use), else exits 10 with the phrase to type; protect-paths needles now also name `approvals/notify` and the notify record (python and no-python). Unit `test_notify_check.py` `HumanConfirmation` (10 tests), table `notify-confirm.json` nc-01..12; `rules/notifications.md`, `rules/enforcement.md`, `hooks/README.md`, karvey-qa and architecture §1.8/§2.4/§6.1 updated. | resolved | D-16 · karvey_lib/approval.py, guards.py, karvey-config.py notify-check · tables/notify-confirm.json |
| F-16 | 2026-09-24 | impl | spec-gap | low | Architecture §3.1 says every kind refuses `{ }` and `\`, but the markdown location needs braces and Azure Boards paths need `\`. `safe_values.py` allows them only for those two kinds; §3.1 should say so. (lane B, E1.F7.T1) | open | architecture.md §3.1 |
| F-17 | 2026-09-24 | impl | spec-gap | low | The §3.1 markdown-location pattern accepted `..`; `safe_values.py` adds an explicit refusal. §3.1 should include it. (lane B, E1.F7.T1) | open | architecture.md §3.1 |
| F-18 | 2026-09-24 | impl | spec-gap | low | §3.1 has no pattern for `management.sprints` or for tool `other`; conservative patterns were used in `safe_values.py`. §3.1 should define them. (lane B, E1.F7.T1) | open | architecture.md §3.1 |
| F-19 | 2026-09-24 | impl | spec-gap | low | The §3.1 common refusal rejects status names such as `In Progress (QA)`, which exist in real ClickUp lists, so a valid tracker configuration cannot be expressed. Needs a status-name kind that allows parentheses (still no shell metacharacters when used via `get --shell`). (lane B, E1.F7.T1) | open | architecture.md §3.1 |
| F-20 | 2026-09-24 | impl | spec-gap | medium | L-29 counts the `branch_flow` placeholders `{integration}` / `{production}` in shell examples as project.json values: 18 hits. E1.F12 must rewrite those examples with `karvey-config.py get --shell`, double-quoted. (lane C, E1.F10.T5) | open | E1.F12 |
| F-21 | 2026-09-24 | impl | spec-gap | low | Real schema gap found by L-17: `capability` is in the `living-specs.md` spec.json block (and read by `karvey-spec-merge.py`) but was missing from `spec.schema.json`. **Resolved at integration:** declared as a string with the spec-merge pattern `^[a-z0-9][a-z0-9._-]*$`; `test_schemas.py: test_capability_is_declared`; L-17 is clean. (lane C, E1.F10.T3) | resolved | spec.schema.json |
| F-22 | 2026-09-24 | impl | bug | low | `test_karvey_hooks.py` used the real clone as cwd and wrote audit lines into this clone's `.git/karvey/audit.log`. **Resolved:** lane A's fix (`CLAUDE_PROJECT_DIR=/nonexistent-karvey-unit` in the test runner) verified at integration — a full unit + hooks + tables run leaves `.git/karvey/` empty. The polluted log held 57 lines, all from tests (`boom-closed` 25, `selftest` 25, `protect-paths` non-JSON stdin 7, every `session_id` empty), and was deleted. (lane D) | resolved | test_karvey_hooks.py |
| F-23 | 2026-09-24 | impl | spec-gap | low | The dashboard read `tracker-outbox.jsonl` with its own parser (lane D built without lane B): its `blocked_by` showed a stale parent that `karvey-config.py` counts as ready, and it did not show `key`. **Resolved at integration:** `karvey_lib/outbox.py` holds the file name, the pending rule and the ready/blocked rule; `karvey-config.py` and `karvey-context.py` both use it; the dashboard shows `state` and prints `blocked_by` only when blocked; `test_context.py: test_outbox_written_by_karvey_config_reads_the_same` round-trips add/list/done. (lane D, E1.F8.T1) | resolved | karvey_lib/outbox.py |
| F-24 | 2026-09-24 | impl | spec-gap | low | L-09 checked `docs/…` citations in `plugins/karvey/hooks/README.md` and `plugins/karvey/README.md` against this repository, but those READMEs describe the **user's** project (`docs/spec/project.json`, `docs/spec/agent/`), exactly as SKILL.md bodies do (where `docs/` is already not a citation). Heuristic narrowed in E1.F12.T1: the plugin READMEs use the plugin segments; only the repository `README.md` resolves `docs/…`. Tests `test_lint_plugin.py: L09.test_project_paths_in_plugin_readmes_are_not_citations`, `test_repo_readme_docs_paths_still_resolve`. | resolved | lint-plugin.py L-09 |
| F-25 | 2026-09-24 | impl | spec-gap | medium | Architecture §8 says init's spec.json step becomes a "state call (creates `phase: init`, `phase_history`)", and REQ-W1-013 bars hand-written state fields, but §1.2 and E1.F3 gave `karvey-state.py` no command that creates a change: `advance {id} init` on a phase-less file exits 3 (unmappable), so karvey-init and karvey-import could only keep a hand-written `"phase": "init"` block (L-06). Added in E1.F12.T3: `karvey-state.py init <change> [--by]` creates `docs/spec/changes/<change>/spec.json` (`schema_version`, `change_id`, `phase: init`, one `phase_history` entry, `approvals: {}`, timestamps; compare-and-swap create) or adds those fields to a spec.json the skill wrote without a phase, keeping its keys; refuses (exit 3, file byte-identical) when a phase exists and rejects an invalid id (exit 2). Tests `test_state_init.py` (5). §1.2 should list `init`. | resolved | karvey-state.py init (architecture §1.2) |
| F-26 | 2026-09-24 | impl | spec-gap | low | `spec.schema.json` accepts only `^[DC]-\d+$` in `decisions`, while `rules/multi-agent.md` §2 prescribes cross-repo references as `"D-12@{ops-repo}"`: a decision cited the way the rule says fails validation. The karvey-init example now uses `"D-NN"`; the schema pattern or the rule must change. Also cosmetic: `karvey-state.py next` prints the blocker line twice. (E1.F12.T3) | open | spec.schema.json / multi-agent.md |
| F-27 | 2026-09-24 | impl | spec-gap | low | `rules/management-adapters.md` has no time-entry / worklog operation, so karvey-impl can only say "record the actual as a time entry where the tool has one, else the PLAN.md actual columns" — prose, not an adapter call (REQ-W1-042). The adapter should list a `log_time` operation per tool (ClickUp time entry, Jira worklog, …) with `none` for tools that have none. (E1.F12.T5) | open | management-adapters.md |
| F-28 | 2026-09-24 | impl | spec-gap | low | `approvals.deploy` has no write path: Wave 1 does not use it as a precondition (state-machine.json), so the rewritten karvey-deploy no longer records it. Architecture should say whether deploy ever runs `approve {id} deploy`, or drop the key. (E1.F12.T6) | open | architecture.md §2.1 |
| F-29 | 2026-09-24 | impl | spec-gap | low | D-03 puts the prod OK "in the decision log", but writing `decisions.md` during deploy is a commit on some branch. The rewritten deploy keeps the D-NN text in the PR body and archive writes it into `decisions.md` on `chore/archive-{id}`; D-03 / `deploy-workflow.md` should state that order. (E1.F12.T6) | open | D-03 |
| F-30 | 2026-09-24 | impl | spec-gap | medium | The release ledger lives under the local `<git-common-dir>/karvey/`, so `advance {id} deployed` and `approve {id} prod --write-spec` at archive work only in the clone where deploy recorded it. `approve --write-spec` can fall back to a D-NN / PR URL; `advance deployed` has no fallback and requires the ledger. Needs a decision (e.g. `advance deployed --ref D-NN --pipeline-run URL` accepted when the ledger is absent). (E1.F12.T6) | open | architecture.md §2.4 |
| F-31 | 2026-09-24 | impl | spec-gap | low | karvey-import can no longer resume "at the furthest phase the content supports": `karvey-state.py advance` refuses to pass an unapproved gate and imported gates are unapproved by design, so an import resumes at `requirements` and walks the gates in order. Either accept that (current text) or add an import path that records the imported artifacts as `generated` and asks the human per gate. (E1.F12.T7) | open | REQ-W1-013 |
| F-32 | 2026-09-24 | impl | spec-gap | low | One decision-log path (L-30): karvey-decisions described one file per period under `{ops_repo}/decisions/`, multi-agent.md named `docs/decisiones.md`; both now say `{ops_repo}/docs/spec/decisions.md` (the shape this repo uses). A project keeping per-period files needs a migration note; architecture §8 does not say which shape wins. (E1.F12.T7) | open | architecture.md §8 |
| F-33 | 2026-09-24 | impl | spec-gap | low | `karvey-statusline.sh` hardcodes the two context-token thresholds although `karvey_lib/defaults.json` defines `context_tokens` (REQ-W1-049 "one place"); and its "karvey statusline down" failure line has no guard-table case (only `test-hooks.sh` covers it indirectly), so hooks/README.md cannot anchor it. (E1.F12.T11) | open | karvey-statusline.sh |
| F-34 | 2026-09-24 | impl | spec-gap | low | Three linter heuristics hit correct text or hid real hits (fixed narrowly in E1.F12.T10/T13, each with a unit test): (1) `iter_lines` reduced every opening fence to three characters, so a ````` ````markdown ````` wrapper closed at the first inner ```` ``` ```` and the rest of the wrapper was read as prose while later prose was read as code — it hid broken `karvey/rules/*` paths and a graphify line in karvey-test (`Fences` tests); (2) L-16 accepted only `stdout_contains`/`stderr_contains` as proof of a "prints" promise, so the session hook, whose cases assert `context_contains`, could never be anchored (`L16.test_context_contains_counts_as_prints`); (3) L-24 matched "Status per task, comment and cascade per Feature" — the rule itself — across the comma (`L24.test_status_per_task_then_comment_per_feature_passes`). | resolved | lint-plugin.py |
| F-35 | 2026-09-24 | impl | spec-gap | medium | E1.F12.T13 cannot end at 0 errors as specified: after the text work the whole-repo lint leaves 7 **L-18** errors, all in `spec.json` data, none in skill or rule text. `team-adapters` (3: `approvals.prod` date-only, prose `ref`, no `role`) is cleared only by E1.F15.T2 [human] + E1.F15.T3 (`approve team-adapters prod --ref D-08 --write-spec`). `archive/2026-09-22-team-layer` (4: the same three plus "archived without a human approvals.prod") is, per E1.F15.T1 and D-03 ("does not apply retroactively"), recorded history that should be **warnings** — but the validator makes them errors. Not patched here (editing a recorded approval by hand is what REQ-W1-013 forbids). Proposed: T13's criterion becomes "0 errors outside `docs/spec/**/*.json`" and E1.F15.T1 decides how archived pre-3.12 approvals are reported (warning for `changes/archive/**` recorded before 3.12.0, or a one-time D-NN-backed `--write-spec`). **Resolved 2026-09-24:** per D-14: `karvey-state.py validate` reports the approval-format errors (date-only, prose `ref`, missing `role`, "archived without a human approvals.prod") of a change under `docs/spec/changes/archive/` whose approval dates all predate the 3.12.0 release as **warnings** suffixed "pre-3.12 recorded history (D-14)"; the cut-off is `defaults.json:pre_3_12_history.released_on` (null until the release; L-35 requires it to match the `[3.12.0]` CHANGELOG date, E1.F16.T1 sets it). Non-archived changes stay strict: `team-adapters` keeps its 3 L-18 errors until E1.F15.T2/T3. Whole-repo lint 7 → 3 errors. Unit `test_state_validate.py` `Pre312History` (7 tests), `test_lint_plugin.py` L35; architecture §2.3 row added. | resolved | D-14 · karvey-state.py validate · defaults.json · L-35 · architecture §2.3 · E1.F15.T2/T3 for team-adapters |
| F-36 | 2026-09-24 | impl | spec-gap | low | The §1.11 `windows-advisory` job runs `run_tables.py --tag windows`, but no table case carries the `windows` tag, so the runner selects 0 cases and exits 1 ("no case selected"): the advisory job is red on every run and proves nothing. Written as specified (it is `continue-on-error`). Either tag the path-translation / CRLF cases `windows` (they do not exist yet; `test_paths.py` is the unit-level cover) or run `python -m unittest … -p test_paths.py` there instead. (E1.F13.T1) **Resolved 2026-09-24:** six smoke cases are tagged `windows` (allow, block, an Edit path, a prompt marker, the post-edit validator; 6 cases / 11 runs), and the job also runs the path-translation / BOM / CRLF unit tests (`test_paths`, `test_hookio`, `test_atomicio`), with `python` (setup-python on Windows) and `fetch-depth: 0`; still `continue-on-error`. New `test_ci_workflow.py` fails if a `--tag` selection of the job selects no case. Architecture §1.11 updated. Not yet observed on a Windows runner (E1.F13.T2). | resolved | lint.yml windows-advisory · tables/smoke.json · test_ci_workflow.py · architecture §1.11 |
| F-37 | 2026-09-24 | impl | spec-gap | low | `test-hooks.sh` already runs every guard table (E1.F6.T4), and §1.11 also runs `run_tables.py --junit` in the same job, so each `tests` leg runs the 351 table runs twice. Kept as specified; `KARVEY_SKIP_TABLES=1` on the `test-hooks.sh` step would halve the leg. The `lint` job is red until F-35 is decided (7 L-18 errors in historical approval data) and `validate --all` until E1.F15.T3; neither is hidden with `continue-on-error`. (E1.F13.T1) **Resolved 2026-09-24:** the `test-hooks.sh` step of the CI `tests` job sets `KARVEY_SKIP_TABLES: '1'`, so each leg runs the guard tables once (`run_tables.py --junit`); `test_ci_workflow.py` `TablesRunOnce` fails if the step loses the switch (red on the previous workflow). Architecture §1.11 updated. The lint-job remark is superseded: L-18 now leaves only the 3 `team-adapters` errors (F-35), cleared by E1.F15.T2/T3. | resolved | lint.yml tests job · test_ci_workflow.py · architecture §1.11 |
| F-38 | 2026-09-24 | impl | spec-gap | low | One real `project.json` (object `management`, fixture `legacy/project/management-object.json`) keeps its status map under `management.status_flow`, with the five logical keys. `resolve management` ignores it and reports `statuses` missing; `propose-settings --from-legacy` copies `status_flow` through unchanged instead of proposing it as `statuses`. Safe (REQ-W1-080: nothing is invented, the human confirms), but the owner of that repo is asked for a map that is already there. Candidate: `propose-settings --from-legacy` proposes `status_flow` → `statuses` when its keys are the logical states; `resolve` keeps reporting it missing until confirmed. (E1.F14.T2) **Resolved 2026-09-24:** `karvey_lib/project.py:legacy_status_flow` recognises a `management.status_flow` keyed only by the logical states (string or `null` values, no `statuses` yet); `propose-settings --from-legacy` proposes it as `statuses` and `validate --fix --accept-proposed` moves it there (plain `--fix` names the proposal and keeps it; `resolve` still reports `statuses` missing until then); a `status_flow` with other keys is kept. `google_chat` → `google-chat` in `validate --fix` (exact) and noted in `propose-settings`. New fixtures `management-status-flow-{google_chat,custom}.json`; tests in `test_config_resolve.py` (propose, `--fix`, `--accept-proposed` idempotent, dry-run) and `test_project.py` `LegacyStatusFlow`. Architecture §1.8, §2.6, §6.3 updated. | resolved | architecture.md §2.6 · karvey-config.py propose-settings · karvey-state.py validate --fix · fixtures legacy/project |
| F-39 | 2026-09-24 | impl | spec-gap | medium | **BUG-05 cannot reach RESUELTO on this branch.** Architecture §6.4 names "L-28 (logical states in impl)" as its automated check, but L-28 checks management text (`!= markdown`, direct `backlog_list_id`, cascade, 🙋 legend, Epic states) and nothing about impl's selection or dependency rule; REQ-W1-085 is not among its REQs. The only named proof is the manual script `tests/manual/impl-resume.md`, not yet run. And the fix is incomplete: `karvey-impl/SKILL.md:39` now states the logical rule (`todo` or orphan `in_progress`, dependencies at `review` or `done`, one declared source, drift reported), but lines 40-41 still say "Do not execute [Backend] until its dependent [DB] is **completed**", the pre-3.10 wording that caused the deadlock. Proposed: rewrite 40-41 as "…until its dependent [DB] is at `review` or `done`", add a lint check (or extend L-28) that fails on `completed`/`pending` as a task state in karvey-impl, index it in `test_incidents.py`, then run `impl-resume.md`. BUG-05 stays DETECTADO until then. (E1.F14.T3) **Resolved 2026-09-24:** karvey-impl/SKILL.md:40-41 now say "only when the [DB] tasks it depends on are at `review` or `done`"; new lint check **L-36** (red on `main`, 5 errors; green here; `test_lint_plugin.py` `L36`) is BUG-05's automated regression, indexed in `test_incidents.py`; architecture §6.4 names L-36. BUG-05 RESUELTO. `impl-resume.md` stays as QA evidence. | resolved | karvey-impl SKILL.md / L-36 / architecture §6.4 / BUG-05 RESUELTO |
