[
  {"tool": "bandit", "rule": "B404", "path": "plugins/karvey/", "reason": "importing subprocess is how the plugin runs git and the fixed tool catalogue; every call uses an argv list", "scope": "import of subprocess in plugins/karvey/scripts and tests only; any call site is still reviewed under B602/B603"},
  {"tool": "bandit", "rule": "B603", "path": "plugins/karvey/", "reason": "subprocess without a shell and with a fixed argv (git subcommands through an allow-list, the security-tool catalogue, the command after `--` of the evidence wrapper, which the human or agent types)", "scope": "argv-list calls under plugins/karvey; a shell=True call (B602) is never covered"},
  {"tool": "bandit", "rule": "B607", "path": "plugins/karvey/", "reason": "git, node and the catalogue tools are resolved from PATH by design (the environment's own tools); no path comes from repository content", "scope": "partial executable names under plugins/karvey"},
  {"tool": "bandit", "rule": "B405", "path": "plugins/karvey/scripts/karvey-trace.py", "reason": "the JUnit XML import; see B314", "scope": "karvey-trace.py and its test only"},
  {"tool": "bandit", "rule": "B314", "path": "plugins/karvey/scripts/karvey-trace.py", "reason": "JUnit XML written by the project's own test run; size-capped before parsing; the standard library parser does not resolve external entities and expat limits entity expansion", "scope": "karvey-trace.py JUnit reading only"},
  {"tool": "bandit", "rule": "B105", "path": "plugins/karvey/", "reason": "names containing 'token' that are not secrets: a self-test marker string, a regex for file tokens in lint, test fixtures with placeholder values", "scope": "the six reported lines (karvey_hooks.py SELFTEST_TOKEN, lint-plugin.py FILE_TOKEN, four unit-test fixtures); a real credential is never covered"},
  {"tool": "bandit", "rule": "B110", "path": "plugins/karvey/scripts/karvey_lib/", "reason": "audit logging inside hooks must never change a guard decision; the swallowed exception is the audit append only", "scope": "guards.py:1235, karvey_hooks.py:227, karvey_hooks.py:239"},
  {"tool": "bandit", "rule": "B108", "path": "plugins/karvey/tests/unit/", "reason": "string literals such as /tmp/x used as fake paths in unit-test inputs; nothing is created there", "scope": "tests/unit only; B108 in scripts is never covered"}
]
