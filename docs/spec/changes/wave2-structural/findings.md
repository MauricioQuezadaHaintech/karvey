# Findings: wave2-structural

Triage inbox of this change (`plugins/karvey/skills/karvey/rules/iteration-loop.md`). Judges, `karvey-test`,
`karvey-qa` and `karvey-browse` append observations here with a type guess and their origin (`judge:{lens}`,
`test`, `qa`, `browse`); only `karvey-iterate` routes them. Types: `bug` → incident `BUG-NN` in
`docs/bugs_dev_testing.md` · `spec-gap` → re-open requirements · `emergent` → `docs/spec/backlog.md`.

Status: `open` → `routed` → `closed`. Convergence needs no `open`/`routed` `bug` or `spec-gap`.

| ID | Date | Phase | Origin | Type | Severity | Finding | Status | Routed to |
|----|------|-------|--------|------|----------|---------|--------|-----------|
| F-01 | 2026-09-26 | impl | impl | emergent | Low | Deviation E1.F4.T2: `karvey-state.py gate <change> <phase> [--granular-gates]` added so the one closing block of `rules/gates.md` can ask "close the gate now?" deterministically; the design names only `gate_mode` and `approve-gate`. | routed | deferred: backlog candidate (implementation deviation recorded; the orchestrator moves it) |
| F-02 | 2026-09-26 | impl | impl | emergent | Low | Deviation E1.F9.T2: L-33 reads "created after the release" as an id above the highest one the file holds on `origin/{production}`; without that ref every duplicate stays a warning. | routed | deferred: backlog candidate (implementation deviation recorded; the orchestrator moves it) |
| F-03 | 2026-09-26 | impl | impl | emergent | Low | Deviation E1.F7.T1: a requirement also counts as covered by a task whose `Tests added` is not `none` (this repo's tasks carry tests inside `[Backend]` tasks); `test_first` is reported apart and is true only with a `[Test]` task that the implementation tasks depend on. | routed | deferred: backlog candidate (implementation deviation recorded; the orchestrator moves it) |
| F-04 | 2026-09-26 | impl | impl | emergent | Low | Deviation E1.F12.T2: lint L-30 (one decision-log path) now ignores `docs/spec/decisions/*.md`, the per-period shape REQ-W2-081 keeps readable; E1.F6.T3's L-49 tests live in `test_spec_merge_check.py`, not `test_lint_plugin.py`. | routed | deferred: backlog candidate (implementation deviation recorded; the orchestrator moves it) |
| F-05 | 2026-09-26 | impl | impl | bug | Low | The method page `docs/karvey.html` still says 32 skills / 18 support / 22 rules after `karvey-judges` and the new rules (`lanes.md`, `judges.md`, `gates.md`); L-11 does not read the page. To fix with the release docs (E1.F13.T7). | closed | BUG-66 (RESUELTO, `test_page_static.py` `CurrentCounts`) |
| F-06 | 2026-09-26 | impl | impl | bug | High | Merged gates could not be walked: with `gates: merged`, `advance {id} tasks` (and `next`) refused `architecture not approved or skipped` although the phase does not close the *how* gate and `rules/gates.md` says to record it generated and continue — so a merged gate with more than one phase was unreachable (found by E1.F13.T3 `test_wave2_flow.py`). Fixed in impl: in merged mode a generated phase that does not close its gate no longer blocks a target inside the same gate; leaving the gate still needs `approve-gate`. Regression: `test_state_gates.py` `MergedGateAdvance` (4 cases). The `BUG-NN` is for `karvey-iterate` to open. | closed | BUG-78 (RESUELTO, `test_state_gates.py` `MergedGateAdvance`) |
| F-07 | 2026-09-26 | impl | impl | emergent | Low | Deviation E1.F13.T6: `karvey-trace.py` also reads JSON test tables (the REQ ids in their cases' `tags`) and treats a `tests/manual/*.md` script as a `manual` exception for the ids its title names (§6.4 says manual scripts are the exceptions, with their reason); this repo's `project.json:tests.globs` declares the guard tables and the manual scripts, test docstrings gained `@req` for the MODIFIED REQ-W1 ids they verify, and three `manual:` lines (REQ-W2-020, 059, 063) were added to `tasks.md`. Result: coverage 97/97 (88 REQ-W2 + 9 MODIFIED REQ-W1); the task's done-when said 88/88 because it did not count the MODIFIED ids. | routed | deferred: backlog candidate (implementation deviation recorded; the orchestrator moves it) |
| F-08 | 2026-09-26 | test | test | emergent | Medium | Five requirements are verified only by agent behaviour that this session cannot run: REQ-W2-008 and 009 (`retro-from-metrics.md`), 041 (`grill-batches.md`), 078's rollback question (`deploy-postdeploy.md`; its script half passes in `test_postdeploy.py`) and 063 (QA runs or cites the run; the fiscal checks it at QA). They are `PENDING (manual)` in `test_evidence.md`, not PASS; the seven manual scripts of architecture §6.4 are the owner's headless run before QA (D-19) — a session here may not write under the user configuration. | closed | qa/manual/*-2026-09-26.md: the 7 scripts run headless (D-19/D-28); REQ-W2-008, 009, 041, 063, 078 PASS |
| F-09 | 2026-09-26 | test | test | emergent | Low | Benchmark against the Wave 1 baseline (`docs/test_evidence.md`, terminal, medians): `karvey-state.py next` 72 → 86 ms (+19 %) and `karvey-context.py` 92 → 117 ms (+27 %); the hook dispatcher (88 → 86 ms) and the session hook (92 → 81 ms) did not move. `next` now resolves the gate mode from `project.json` (F-06) and the dashboard carries the lane, metrics and stall sections. All far under the 5 s / 10 s hook budgets; recorded so the trend is watched, not a regression to fix. | routed | deferred: backlog candidate (trend watch, Low; far under the hook budgets) |
| F-10 | 2026-09-26 | qa | test | bug | High | Manual script deploy-postdeploy, run 1: a DEV `regression` neither showed the rollback nor asked, and the incident was only offered; `karvey-deploy` Step 2-bis limited the rollback question to PROD while REQ-W2-078 covers every regression. | closed | BUG-79 |
| F-11 | 2026-09-26 | qa | test | bug | Medium | Manual script retro-from-metrics, run 1: the agreed action's `backlog.md` row carried no owner (REQ-W2-008); the backlog table has no owner column and the skill did not say where it goes. | closed | BUG-80 |
| F-12 | 2026-09-26 | qa | test | bug | High | Manual script import-through-gates, run 1: after `outcome … how changes_requested`, `next` said `next infra` with no blocker — the F-06 fix passed a generated phase inside an open merged gate even when the human had sent it back. | closed | BUG-81 |
| F-13 | 2026-09-26 | qa | qa | bug | High | `karvey-postdeploy.py`: `pass` with the service unreachable (only `new_5xx`), URL credentials in the evidence, a missing value counted as 0, crashes with exit 1 (= regression) on malformed input. | closed | BUG-52 |
| F-14 | 2026-09-26 | qa | test | bug | High | Requirements written from the `karvey-requirements` template (`### 1.1 {name}`) carry no `REQ-…-NNN` id, so `karvey-trace.py --check` read `coverage: 0/0` as a pass (seen in two manual runs). | closed | BUG-53 |
| F-15 | 2026-09-26 | qa | qa | bug | High | `parse_dt` (state, metrics) dropped the zone of a timestamp with fractional seconds; `--metrics` then crashed on naive vs aware. | closed | BUG-54 |
| F-16 | 2026-09-26 | qa | qa | bug | High | `karvey-evidence.py` stored secrets from argv, accepted `--change ../x`, glued records without a final newline, crashed on an unstartable command. | closed | BUG-55 |
| F-17 | 2026-09-26 | qa | qa | bug | High | Judge `collect`: an unsanitised lens could forge `findings.md` rows; a non-ASCII digit cite crashed it; re-runs duplicated rows; the diff temp file was never deleted. | closed | BUG-56 |
| F-18 | 2026-09-26 | qa | qa | bug | Medium | Metrics: a malformed archived change crashed the report; negative lead time; zone-less deploy times hidden as "no regression deploy" (REQ-W2-004). | closed | BUG-57 |
| F-19 | 2026-09-26 | qa | qa | bug | Medium | `karvey-trace.py` crashed with exit 1 — its "coverage refused" code — on a malformed `tests` config or evidence line. | closed | BUG-58 |
| F-20 | 2026-09-26 | qa | qa | bug | Medium | `karvey-release-gate.py`: wrong `evidence.jsonl:{line}`; manifest mode from the working copy only (the hook takes the stricter of both); `release-branch --version` unvalidated. | closed | BUG-59 |
| F-21 | 2026-09-26 | qa | qa | bug | Medium | `karvey-id.py`: branch scan without the word boundary (`HEAD-977` → `D-978`); a refused `--qualified` burnt a number; corrupt `ids.json` → exit 5; unsafe stale-lock takeover. | closed | BUG-60 |
| F-22 | 2026-09-26 | qa | qa | bug | Medium | `karvey-state.py`: `schema_mode` ignored the registry's `schema.strict`; a missing lane was never an error under strict (architecture §1.2); `judge-run` accepted NaN. | closed | BUG-61 |
| F-23 | 2026-09-26 | qa | qa | spec-gap | High | REQ-W2-016 did not define "raise"; the tool ranked lanes by phase count, so `docs` → `ops` passed as a raise and made QA optional without the human. Requirements revised in place (revision 1, recommended option: a raise keeps every phase's mode); code fixed. | closed | REQ-W2-016 rev. 1 + BUG-62 |
| F-24 | 2026-09-26 | qa | qa | bug | Medium | Trailer guard: `git commit -am "msg"` was not parsed, so a blocking guard let it through. | closed | BUG-63 |
| F-25 | 2026-09-26 | qa | qa | bug | Medium | Lint L-47 (check-mode registry invariants), declared in architecture §5, was not implemented. | closed | BUG-64 |
| F-26 | 2026-09-26 | qa | test | bug | Low | The *how* gate summary listed the post-deploy contract and rollback as missing although `infra.md` held complete `karvey-postdeploy` blocks (manual script security-tools-present, variant). | closed | BUG-65 |
| F-27 | 2026-09-26 | qa | test | bug | Medium | `karvey-impl` never told the agent to add `Karvey-Change: {change-id}` (REQ-W2-043); the merged-gates run committed without it and rewrote the commits later. | closed | BUG-67 |
| F-28 | 2026-09-26 | qa | test | emergent | Low | `karvey-id.py next` reserves on every call; an agent that calls it "to check" burns numbers (retro run: BL-04, BL-05). A read-only `peek` would avoid it. | routed | deferred: backlog candidate (Low; numbers stay unique) |
| F-29 | 2026-09-26 | qa | test | emergent | Low | Manual script judges-gate: the deliberate token-logging gap sits in a draft the architecture skill rewrites under the security rules, so it is fixed before the judges read it; the script needs a gap the author cannot fix silently. | routed | deferred: backlog candidate (test-script fixture; the judge mechanics passed) |
| F-30 | 2026-09-26 | qa | test | emergent | Low | Manual script security-tools-present: its infra half needs a change at `infra` (the fixture puts it at `qa`, behind infra), and "before any model reading of the code" conflicts with `karvey-qa` Step 0, which reads the diff first. | routed | deferred: backlog candidate (test-script wording/fixture) |
| F-31 | 2026-09-26 | qa | qa | emergent | Medium | `manifest.qa_state` and the prod gate's "without QA" check accept `approvals.qa.approved: true` with no role or marker; a hand-edited `spec.json` satisfies them. Defence in depth: `spec.json` writes are guarded by the spec-write hook and reviewed in the PR. | routed | deferred: backlog candidate (hardening; needs a decision on the trust model of reviewed files) |
| F-32 | 2026-09-26 | qa | qa | emergent | Medium | Evidence files are trusted as written: `evaluate` reuses any `postdeploy_probe_{env}.json` (no time/version binding); the release gate takes the last `exit: 0` line; the trace counts a test as run when its name appears in an argv; evidence lines carry no commit SHA. | routed | deferred: backlog candidate (hardening: bind evidence to commit/time; fiscal reviews claims meanwhile) |
| F-33 | 2026-09-26 | qa | qa | emergent | Low | Helpers duplicated across scripts: `parse_dt` (4 copies), `now_iso` (4), `_root(args)` (4). | routed | deferred: backlog candidate (refactor into karvey_lib) |
| F-34 | 2026-09-26 | qa | qa | emergent | Low | The prod-gate hook now builds the release manifest on every production merge (3.13 default `warn`) and prints "MANIFEST not evaluated" when the head branch is not local. | routed | deferred: backlog candidate (noise; non-blocking) |
| F-35 | 2026-09-26 | qa | qa | emergent | Medium | `lane set hotfix` (and `ops`, F-64) at init has no admission check (only `patch` has one); hotfix makes QA optional by design (REQ-W2-018) but still needs BUG-NN, the regression test and the human prod approval. | routed | deferred: backlog candidate (design decision: hotfix admission criteria) |
| F-36 | 2026-09-26 | qa | qa | emergent | Low | Judges get the full `git diff` with no secret redaction, possibly sent to another model provider under `cross_model: prefer`. | routed | deferred: backlog candidate (redaction before cross-model send) |
| F-37 | 2026-09-26 | qa | test | emergent | Low | `karvey-evidence.py` finds the change from the working directory, so a command that must run elsewhere needs `env -C` inside the wrapper; a `--cwd` option would make it explicit. | routed | deferred: backlog candidate (ergonomics) |
| F-38 | 2026-09-26 | qa | qa | emergent | Low | An unknown `spec.json:lane` makes `karvey-judges.py` exit 5 (internal `LaneError`) instead of a validation error; the post-deploy probe may GET any https host, internal ones included (by contract; http only on localhost outside prod). | routed | deferred: backlog candidate (Low robustness) |
| F-39 | 2026-09-26 | qa | test | bug | Medium | `rules/incident-tracking.md` names the incident section `### Regression`, but `karvey-context.py` read only `### Regression test`, so a RESUELTO incident written from the rule failed convergence (merged-gates manual run). | closed | BUG-68 |
| F-40 | 2026-09-26 | qa | test | bug | Medium | `karvey-evidence.py` wrote the user's home path into committed `evidence.jsonl` (merged-gates manual run). Older records of other changes in this repo still carry the owner's home path (`team-adapters` review, `wave1-hardening` architecture): not rewritten here. | closed | BUG-69 (older records: deferred, backlog candidate) |
| F-41 | 2026-09-26 | qa | qa | bug | High | The release gate (`approve-gate … release`) recorded production from a project-wide prod marker and never consumed it — the rule BUG-41 set on the Wave 1 branch for `approve prod`, missing in Wave 2's new path (and in this branch's `approve prod`, cut before that fix). | closed | BUG-70 |
| F-42 | 2026-09-26 | qa | qa | bug | Medium | The BUG-55 redaction hid the argument after `--passWithNoTests` (substring `pass`), so the trace saw the test as not run. | closed | BUG-71 |
| F-43 | 2026-09-26 | qa | qa | bug | Medium | Under strict mode the missing-lane error advised `lane set`, which refuses outside init. | closed | BUG-72 |
| F-44 | 2026-09-26 | qa | qa | emergent | Medium | `advance … deployed --attested` accepts any existing `D-NN` and any https run URL without a marker; allowed whenever the local ledger is missing. Also: the prod-gate reads each manifest change's QA state from the working-tree `spec.json`, not from the head being merged, and a code commit can carry the trailer of an archived QA-approved change. | routed | deferred: backlog candidate (hardening of the attested and manifest paths together with F-31/F-32; to be designed with Wave 1's prod-approval binding D-35) |
| F-45 | 2026-09-26 | qa | qa | emergent | Low | Second-opinion Low items: a narrow window in the BUG-60 stale-lock takeover (rename → link) on filesystems without hard links; any repeat `generated` releases the BUG-81 hold (the human still answers the gate); `-y` approves QA without a marker (REQ-W2-040 allows `auto` outside prod — accepted as designed). | routed | deferred: backlog candidate (Low) |
| F-46 | 2026-09-26 | qa | judge:fiscal | bug | High | The 'Tests: pass' approval item cites evidence.jsonl:20, :21, :26 and :27 (1101 unit, 10 regression, 408 table cases, 65 hooks). The evidence.jsonl in the reviewed diff has only 19 lines, so none of these cited rows exist. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:153) | closed | accepted:bug review revision 2 — evidence.jsonl committed with the review; the cited lines exist |
| F-47 | 2026-09-26 | qa | judge:fiscal | bug | High | The last unit run in the diff's evidence is the test-phase row, which the PLAN records as 1017 unit tests. No row shows 1101 tests, and no row carries a commit SHA tying any run to aec40dc. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:3083) | closed | accepted:bug review revision 2 — the runs are cited at the reviewed commit; a SHA per evidence line is F-32 (deferred) |
| F-48 | 2026-09-26 | qa | judge:fiscal | bug | High | The coverage claim '97/97 (pass, mode warn)' cites evidence.jsonl:25, which is not in the diff (the file ends at line 19). The trace row that does exist is evidence.jsonl:19, and it predates the QA fixes. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:154) | closed | accepted:bug review revision 2 — trace run cited at its committed line |
| F-49 | 2026-09-26 | qa | judge:fiscal | bug | High | Lint (0 errors, 3 warnings, 51 checks) cites evidence.jsonl:22 and validate (0 errors, 27 warnings) cites evidence.jsonl:23. Neither line exists in the diff. The PLAN records lint at 50 checks. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:155) | closed | accepted:bug review revision 2 — lint and validate cited at their committed lines |
| F-50 | 2026-09-26 | qa | judge:fiscal | bug | Medium | The visual audit's 'node page tests pass' cites evidence.jsonl:24 (22/22), a line missing from the diff. The 'No deviation' approval rests on it and on a static count check that has no output. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:130) | closed | accepted:bug review revision 2 — page run and the static count test cited |
| F-51 | 2026-09-26 | qa | judge:fiscal | bug | High | 'Security gate: PASS' supports approval, yet the review states no scanner ran over the diff (secrets/sast/sca/iac not evaluated). The pass rests on a model review and a manual grep that have no recorded output. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:64) | closed | accepted:spec-gap review revision 2 — the tool catalogue now ran over the repository (gitleaks, bandit) and is cited |
| F-52 | 2026-09-26 | qa | judge:fiscal | bug | Medium | 'A manual grep of the diff ... found none' gives no command, output or evidence.jsonl line. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:34) | closed | accepted:bug review revision 2 — the personal-data grep ran through the evidence wrapper and is cited |
| F-53 | 2026-09-26 | qa | judge:fiscal | bug | High | 'All 23 are fixed with a regression test that was red first' has no evidence of a red run or of the post-fix green runs. No evidence.jsonl row postdates the test phase. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:21) | closed | accepted:bug review revision 2 — the regression tests were run against the pre-QA scripts through the evidence wrapper (red) and cited |
| F-54 | 2026-09-26 | qa | judge:fiscal | bug | Medium | '88/88 requirements green' is uncited. The diff's test-phase record says 83 PASS and 5 PENDING, and says the manual closures are only in qa/manual with no evidence row. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:16) | closed | accepted:bug review revision 2 — cites test_evidence.md and the trace line |
| F-55 | 2026-09-26 | qa | judge:fiscal | bug | Medium | Claims read by hand ('approval paths hold', 'no shell=True', 'fixed argv', 'redirects only to the same origin') cite no file:line in the diff. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:59) | closed | accepted:bug review revision 2 — file:line references added |
| F-56 | 2026-09-26 | qa | judge:fiscal | bug | Medium | 'Every command and flag a skill tells the agent to run exists (checked against --help)' has no command output or evidence row. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:76) | closed | accepted:bug review revision 2 — the claim now names the reviewer's check as unrecorded, not as evidence |
| F-57 | 2026-09-26 | qa | judge:fiscal | bug | Medium | The backwards-compatibility claim ('A Wave 1 spec.json validates unchanged in advisory mode') has no validate run or test cited. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:86) | closed | accepted:bug review revision 2 — the Wave 1 spec.json validated through the evidence wrapper and cited |
| F-58 | 2026-09-26 | qa | judge:fiscal | bug | Medium | The second opinion's 'Checked and correct' list (BUG-62, 63, 52, 61) has no recorded reviewer output. The FAIL verdict and the fixes it prompted are asserted without any artifact. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:123) | closed | accepted:bug review revision 2 — the second opinion's output stored as qa/second-opinion-2026-09-26.md |
| F-59 | 2026-09-26 | qa | judge:fiscal | bug | Low | 'lint L-12/L-13 green' and 'versions agree' are uncited. The lint evidence referenced elsewhere (evidence.jsonl:22) is missing from the diff. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:100) | closed | accepted:bug review revision 2 — covered by the cited lint run (all 51 checks) |
| F-60 | 2026-09-26 | qa | judge:fiscal | bug | Medium | No CI run exists for the reviewed commit ('not run for this commit'), so no pass claim has CI backing. Only local evidence remains, and its cited lines are missing. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:161) | closed | rejected: the review claims no CI run (it says not run for this commit); every pass claim cites a local evidence line |
| F-61 | 2026-09-26 | qa | judge:security | spec-gap | High | approve prod --manifest calls find_valid without project_scope=False, so one project-wide _project prod marker approves production for every change in the manifest. BUG-41/BUG-70 removed this for single-change prod. The review keeps it as a documented discrepancy. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:11162) | closed | accepted:spec-gap D-37 → REQ-W2-052 revision 1 (in place), architecture rev. 1, E1.F5.T10 |
| F-62 | 2026-09-26 | qa | judge:security | bug | Medium | Manifest prod path swallows consume failures (ApprovalError/OSError: pass) after the ledger writes. Prod is recorded while the marker can stay live and be reused, which is the 'stayed live' defect BUG-70 fixed. consume is also called without created_at. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:11182) | closed | accepted:bug BUG-73 |
| F-63 | 2026-09-26 | qa | judge:security | bug | Medium | lane lower accepts any valid plan or prod marker, project scope included, and never consumes it. One unrelated or project-wide plan approval can lower a lane (e.g. to hotfix/ops, making QA optional) repeatedly within the TTL. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:11342) | closed | accepted:bug BUG-74 |
| F-64 | 2026-09-26 | qa | judge:security | emergent | Medium | lane set has an admission check only for patch. An agent can set ops (qa: o) or hotfix at init with no criteria and then skip QA with any reason via cmd_skip's lane_optional path. The review defers only hotfix (F-35); ops is not listed. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:11355) | routed | accepted:emergent deferred with F-35 — admission criteria for the ops and hotfix lanes (backlog candidate) |
| F-65 | 2026-09-26 | qa | judge:security | bug | Medium | evidence argv redaction covers only long flags and NAME=val. Short flags (-p secret, -psecret), header values (-H 'Authorization: Bearer x') and URL query tokens (?token=) are written to committed evidence.jsonl unredacted, though postdeploy redacts queries. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:8464) | closed | accepted:bug BUG-75 |
| F-66 | 2026-09-26 | qa | judge:security | bug | Low | --junit path is recorded as given and never passes through _collapse_home. An absolute home path in it still names the user in committed evidence (the gap BUG-69 fixed for argv). (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:8568) | closed | accepted:bug BUG-75 |
| F-67 | 2026-09-26 | qa | judge:security | bug | Medium | collect takes model and intra_model from the judge's own JSON over the orchestrator's --model/--intra-model. A judge can declare itself cross-model, which forges the independence recorded in judge_runs and the gate summary. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:12953) | closed | accepted:bug BUG-76 |
| F-68 | 2026-09-26 | qa | judge:security | emergent | Low | Blocking trailer guard is skipped whenever no -m/-F/--trailer is parsed: editor commits, -C/-c, or abbreviated long options git accepts (--mess=...). Blocking mode can be bypassed without the review noting it. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:12581) | routed | accepted:emergent deferred — editor commits are silent by REQ-W2-044; abbreviated long options (`--mess=`) are a backlog candidate |
| F-69 | 2026-09-26 | qa | judge:security | spec-gap | Medium | The security gate is declared PASS although secrets, SAST, SCA and IaC were not evaluated on this diff and no tool output is cited. The PASS rests only on model review, so the gate line overstates what was verified. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:64) | closed | accepted:spec-gap review revision 2 — tools ran and are cited (same as F-51) |
| F-70 | 2026-09-26 | qa | judge:security | emergent | Low | The review says a manual grep found no personal data, but CHANGELOG adds about 70 lines with the owner's full name and personal work email. That is a personal datum in a possibly shared repo, with no stated rule allowing email as well as name. (/tmp/karvey-judge-wave2-structural-q89zka3w.diff:71) | routed | accepted:emergent deferred — whether CHANGELOG lines carry the responsible human's email as well as the name is a changelog-policy question (backlog candidate) |
| F-71 | 2026-09-26 | qa | qa | bug | Medium | Running the security catalogue over this change wrote `--source /home/<user>/…` into `evidence.jsonl` and absolute file names into the SAST report: the tools ran in the repository but got absolute paths. | closed | BUG-77 |
| F-72 | 2026-09-26 | qa | judge:fiscal | emergent | High | The review says the suites at evidence.jsonl:34-43 ran on reviewed commit 9cb18d4. No evidence record stores a commit sha, and there is no CI run, so nothing ties the 'Tests: pass at 9cb18d4' approval item to that commit. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:159) | routed | accepted:emergent F-32 — evidence lines carry no commit SHA (deferred); the review states the runs were on the clean tree of 9cb18d4 |
| F-73 | 2026-09-26 | qa | judge:fiscal | bug | High | 'Coverage: 97/97' cites evidence.jsonl:39. That run was --check with no --write, it stores only a hash of the output, and in warn mode it exits 0 whatever the coverage is. The committed traceability.md with 97/97 came from the earlier test-phase run on base origin/main. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:160) | closed | accepted:bug review revision 3 — `trace --write --check` recorded (evidence.jsonl:44), traceability.md regenerated |
| F-74 | 2026-09-26 | qa | judge:fiscal | bug | High | '66 of 69 red, the other three guard against over-matching' cites evidence.jsonl:30, which records only exit 1 and a hash. Neither the per-test red count nor the claim that the /tmp tree held the pre-QA scripts has evidence. The red-first claim for all 29 fixes rests on this line. (/tmp/karvey-judge-wave2-structural-ylin1h10.diff:3258) | closed | accepted:bug review revision 3 — per-test red result transcribed in qa/red-run-2026-09-26.md |
| F-75 | 2026-09-26 | qa | judge:fiscal | bug | Medium | '88 commits; every one carries Karvey-Change: wave2-structural (checked with git log --format=%B)' has no evidence.jsonl line and no recorded output. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:16) | closed | accepted:bug review revision 3 — trailer loop recorded (evidence.jsonl:45) |
| F-76 | 2026-09-26 | qa | judge:fiscal | bug | Medium | 'validate-suppressions: 8 entries, 0 problems' is uncited. No run of karvey-security-scan.py validate-suppressions appears in evidence.jsonl. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:45) | closed | accepted:bug review revision 3 — validate-suppressions recorded (evidence.jsonl:46) |
| F-77 | 2026-09-26 | qa | judge:fiscal | emergent | Medium | The backwards-compatibility claim 'Wave 1 spec.json/project.json validate unchanged: 0 errors, 2 warnings' cites evidence.jsonl:33. That run used a /tmp root whose contents are not in the diff, and the record stores no counts, only exit 0 and hashes. (/tmp/karvey-judge-wave2-structural-ylin1h10.diff:3261) | routed | accepted:emergent F-32 — hash-only lines cannot carry the counts; the scratch root's two files are named (git show of the Wave 1 branch) |
| F-78 | 2026-09-26 | qa | judge:fiscal | bug | Low | 'Lint is green over all 51 checks' has no evidence for the check count. The lint record is hash-only, the only lint --list run is from the test phase, and the PLAN records 50 checks. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:97) | closed | accepted:bug review revision 3 — `lint --list` recorded (evidence.jsonl:48) |
| F-79 | 2026-09-26 | qa | judge:fiscal | bug | Low | The D3 reviewer's comparison of skill commands against the scripts' --help ('no mismatch') is declared as not a recorded run, so the claim has no evidence. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:98) | closed | rejected: stated in the review as the reviewer's unrecorded report, not used for any approval item |
| F-80 | 2026-09-26 | qa | judge:fiscal | bug | Low | 'No .env, Dockerfile or pipeline in this repository consumes KARVEY_ID_LOCK_WAIT_S; it has a fallback' is uncited: no search command and no file:line. (docs/spec/changes/wave2-structural/qa/REVISION_PR_none_20260926.md:110) | closed | accepted:bug review revision 3 — consumers search recorded (evidence.jsonl:47) |
| F-81 | 2026-09-26 | qa | judge:fiscal | bug | Low | The personal-data search covers only plugins and docs/karvey.html. The diff still adds e-mail addresses in CHANGELOG lines, so the 'none' result does not cover the whole diff. (/tmp/karvey-judge-wave2-structural-ylin1h10.diff:111) | closed | accepted:bug review revision 3 — the search scope is stated; CHANGELOG e-mail is F-70 (deferred) |
| F-82 | 2026-09-26 | qa | second-opinion | bug | Medium | `approve prod --manifest` appended the approving change to the manifest's ids, so with an empty manifest (or one without the change) the project-wide marker approved that single change off the manifest path (BUG-41). (`karvey-state.py` `_approve_prod_manifest`) | closed | accepted:bug BUG-82 |
| F-83 | 2026-09-26 | qa | qa | bug | Medium | D1: in blocking mode the prod-gate built the manifest from the local branch named like the PR head, which an earlier command can move; an unapproved change in the released commit was not checked. (`guards.py` `manifest_verdict`) | closed | accepted:bug BUG-83 |
| F-84 | 2026-09-26 | qa | qa | emergent | Medium | D1 + second opinion: `--pr-body` is a local file; nothing ties it to the PR body the human read (not fetched from the host, not hashed into the record, not re-checked at merge), so the body check is self-consistency. The human's question names the manifest and head SHA (deploy 2.9) and the OK is bound to that SHA. | routed | accepted:emergent deferred — read the body and head from the host CLI and hash the body into the record (backlog candidate) |
| F-85 | 2026-09-26 | qa | qa | emergent | Low | The manifest path writes the records before it consumes the one marker (a failed consume is a warning, BUG-73); the per-change and release-gate paths ignore consume's result. A marker left live could re-bind the records to a later head within its TTL. | routed | accepted:emergent deferred — consume first with compare-and-swap, refuse on failure (backlog candidate) |
| F-86 | 2026-09-26 | qa | second-opinion | emergent | Low | Manifest path edge cases: unmapped commits are counted but not listed or refused; `known` change ids come from the working tree, not `--sha`; a partial ledger write has no rollback; reopening the approving change drops every covered change's coverage (fails closed; now stated in the architecture and REQ-W2-052). | routed | accepted:emergent deferred — backlog candidate with F-84 |
