# Findings

| ID | Date | Phase | Origin | Type | Severity | Finding | Status | Routed to |
|----|----|----|----|----|----|----|----|----|
| F-01 | 2026-09-26 | requirements | judge:domain | spec-gap | High | Inbox entries (no active change, or several) are held only in the project store. No gate refuses while they stay unassigned, so an instruction can wait forever while every gate closes. That breaks the goal's 'classified before its gate closes'. (docs/spec/changes/living-docs/requirements.md:173) | closed | accepted:spec-gap REQ-LD-060 (inbox entries hold every gate of the project) (revision_history 2026-09-26, pre-approval) |
| F-02 | 2026-09-26 | requirements | judge:domain | spec-gap | High | The agent can classify an inbox entry `not-instruction` without it reaching any change's gate summary (REQ-LD-017 lists only a change's rows). So the dismissal is never shown to the human, which contradicts O-2: every dismissal must be listed at the gate the human approves. (docs/spec/changes/living-docs/requirements.md:176) | closed | accepted:spec-gap REQ-LD-060 + REQ-LD-017 (dismissed inbox entries listed at the next gate) (revision_history 2026-09-26, pre-approval) |
| F-03 | 2026-09-26 | requirements | judge:domain | spec-gap | High | In a multi-repository project the sheet lives under the spec repository's docs/spec/components/, but the code lives in another repository. REQ-LD-028's 'same commit' rule cannot be met across repositories, and the requirements never say how the pairing works there. (docs/spec/changes/living-docs/requirements.md:303) | closed | accepted:spec-gap REQ-LD-028 (multi-repo: pairing by change trailer instead of by commit) (revision_history 2026-09-26, pre-approval) |
| F-04 | 2026-09-26 | requirements | judge:domain | emergent | High | The goal says no instruction is lost, but detection is a vocabulary heuristic that accepts recall 0.90. The fix for a missed instruction is that the human repeats it, though nothing tells the human it was missed. O-1 quietly narrows the goal to 'prompts the detector classifies as instructions'. (docs/spec/changes/living-docs/requirements.md:111) | closed | accepted:spec-gap REQ-LD-058 (acknowledgement to the owner + force-capture marker) and PRD O-1; residual detector risk goes to the architecture risk register (revision_history 2026-09-26, pre-approval) |
| F-05 | 2026-09-26 | requirements | judge:domain | spec-gap | Medium | If capture fails, the requirements rely on the agent asking the human to repeat. Nothing records that the instruction is still pending or blocks a gate until it is. When the helper is missing, even the notice only arrives at the next session, so the instruction is lost silently. (docs/spec/changes/living-docs/requirements.md:194) | closed | accepted:spec-gap REQ-LD-010 (immediate notice to the human, failures listed at the next gate) (revision_history 2026-09-26, pre-approval) |
| F-06 | 2026-09-26 | requirements | judge:domain | spec-gap | Medium | REQ-LD-008 reports a row captured in another clone as `not verifiable here`, but no requirement says whether that state passes or blocks the gate in REQ-LD-016/053. Teams working from several clones meet this all the time, and the owner has to decide it. (docs/spec/changes/living-docs/requirements.md:163) | closed | accepted:spec-gap REQ-LD-059 (`not verifiable here` passes when the row's own hash matches; open point for the owner) (revision_history 2026-09-26, pre-approval) |
| F-07 | 2026-09-26 | requirements | judge:domain | spec-gap | Medium | The capture setting counts only on the reviewed line (origin/{production}). A new project with nothing on production yet, or an in-flight change during LD-1's upgrade branch, gets no capture until merge. Those instructions are lost with no warning. This empty/legacy state is not covered. (docs/spec/changes/living-docs/requirements.md:198) | closed | accepted:spec-gap REQ-LD-011 (a working-copy `on` takes effect at once; the working copy can never turn capture off) (revision_history 2026-09-26, pre-approval) |
| F-08 | 2026-09-26 | requirements | judge:domain | spec-gap | Medium | REQ-LD-054 depends on a 'README opt-in' that no requirement or glossary entry defines. Nothing says when README checks turn on for a project, which leaves a hidden decision for the owner. (docs/spec/changes/living-docs/requirements.md:737) | closed | accepted:spec-gap REQ-LD-044..046, 054 + glossary (`project.json:readme.check`, absent = off) (revision_history 2026-09-26, pre-approval) |
| F-09 | 2026-09-26 | requirements | judge:domain | emergent | Medium | Removal (REQ-LD-012) and review (REQ-LD-042) need an 'approval marker' from messages like 'remove F-14, it was private'. That is not an approval term under REQ-W1-019. The same message is also a directive, so the hook captures it as a new row that repeats the private context. (docs/spec/changes/living-docs/requirements.md:217) | closed | accepted:emergent→spec-gap REQ-LD-063 (confirm phrases with their own markers; a confirm phrase is not captured); REQ-LD-012, 042 updated (revision_history 2026-09-26, pre-approval) |
| F-10 | 2026-09-26 | requirements | judge:domain | spec-gap | Medium | The goal says data shapes must be learnable from documents. Document stores get a sampler check, but nothing checks a relational sheet's columns and constraints against the repository's migration/schema files. Relational sheets can drift unnoticed. (docs/spec/changes/living-docs/requirements.md:316) | closed | accepted:spec-gap REQ-LD-061 (relational sheet vs repository schema files, advisory) (revision_history 2026-09-26, pre-approval) |
| F-11 | 2026-09-26 | requirements | judge:domain | spec-gap | Low | REQ-LD-003 ships five languages, but REQ-LD-004 measures only three. Two shipped languages have no measured recall or precision. (docs/spec/changes/living-docs/requirements.md:109) | closed | accepted:spec-gap REQ-LD-004 (every shipped language measured) (revision_history 2026-09-26, pre-approval) |
| F-12 | 2026-09-26 | requirements | judge:domain | emergent | Low | Redacting every 'phone-number-shaped value' can mask numbers that carry the instruction itself (limits, IDs, amounts). The captured row then differs from what the owner said, against the 'verbatim' goal. (docs/spec/changes/living-docs/requirements.md:147) | closed | accepted:spec-gap REQ-LD-007 (phone redaction limited to the international form; other numbers kept verbatim) (revision_history 2026-09-26, pre-approval) |
| F-13 | 2026-09-26 | requirements | judge:domain | spec-gap | Low | The gate summary lists only rows captured since the previous gate. A row captured earlier and reclassified to `not-instruction` or `no-spec-impact` after that gate never reaches the human. (docs/spec/changes/living-docs/requirements.md:275) | closed | accepted:spec-gap REQ-LD-017 (captured or reclassified since the previous gate) (revision_history 2026-09-26, pre-approval) |
| F-14 | 2026-09-26 | requirements | judge:domain | bug | Low | REQ-LD-047 is about README bootstrap and update, but its trace note says karvey-docs 'now writes docs/spec/components/'. The note describes the component-sheet scope, not this requirement. (docs/spec/changes/living-docs/spec-delta.md:83) | closed | accepted:bug spec-delta.md / requirements.md trace note of REQ-LD-047 corrected (revision_history 2026-09-26, pre-approval) |
| F-15 | 2026-09-26 | requirements | judge:methods | spec-gap | High | Inbox entries (no change, or several changes active) are never tied to a gate. REQ-LD-016 only blocks on rows of a change, so with two active changes an instruction can wait in the inbox forever while both gates close. This breaks the goal's 'classified before its gate closes'. (docs/spec/changes/living-docs/requirements.md:173) | closed | accepted:spec-gap duplicate of F-01 → REQ-LD-060 (revision_history 2026-09-26, pre-approval) |
| F-16 | 2026-09-26 | requirements | judge:methods | spec-gap | High | REQ-LD-054 makes checks silent when there is 'no README opt-in', but no requirement defines that opt-in. REQ-LD-044/045 make the README checks apply to every repository with no opt-in condition. A test cannot tell which rule applies to a 4.1.0 project. (docs/spec/changes/living-docs/requirements.md:737) | closed | accepted:spec-gap duplicate of F-08 → `project.json:readme.check` (revision_history 2026-09-26, pre-approval) |
| F-17 | 2026-09-26 | requirements | judge:methods | spec-gap | Medium | In REQ-LD-008 the SHALL text only says tampered rows are 'reported'. The error scenario and PRD AC-2 say approve refuses. The requirement text never states the blocking behaviour, which is left only to the REQ-LD-053 registry entry. (docs/spec/changes/living-docs/requirements.md:163) | closed | accepted:spec-gap REQ-LD-059 states the refusal (revision_history 2026-09-26, pre-approval) |
| F-18 | 2026-09-26 | requirements | judge:methods | spec-gap | Medium | In the REQ-LD-017 error scenario, an unreadable findings.md makes approval refused 'by REQ-LD-016's check'. REQ-LD-016 only refuses unclassified rows and says nothing about an unreadable file, so the behaviour cannot be derived from the text. (docs/spec/changes/living-docs/requirements.md:283) | closed | accepted:spec-gap REQ-LD-016 (unreadable findings.md refuses while capture is on); REQ-LD-017 error scenario replaced (revision_history 2026-09-26, pre-approval) |
| F-19 | 2026-09-26 | requirements | judge:methods | spec-gap | Medium | The REQ-LD-027 error scenario expects the tasks review gate to report a delta item that no task names. The SHALL text only requires tasks that change code to name sheet sections. It never requires every delta item to be covered or any gate to report it. (docs/spec/changes/living-docs/requirements.md:411) | closed | accepted:spec-gap REQ-LD-027 (every delta item named by a task; tasks review gate reports the rest) (revision_history 2026-09-26, pre-approval) |
| F-20 | 2026-09-26 | requirements | judge:methods | spec-gap | Medium | The REQ-LD-006 threshold 'the vocabulary's pasted-line length' has no value anywhere in the inputs. What counts as a pasted line cannot be measured, so no test can be written from the text alone. (docs/spec/changes/living-docs/requirements.md:135) | closed | accepted:spec-gap REQ-LD-006 (200 characters, overridable) (revision_history 2026-09-26, pre-approval) |
| F-21 | 2026-09-26 | requirements | judge:methods | spec-gap | Medium | REQ-LD-010 'never block or delay … its time budget is the prompt hook's' gives no numeric budget. 'Delay' is unmeasurable without a threshold in ms. (docs/spec/changes/living-docs/requirements.md:186) | closed | accepted:spec-gap REQ-LD-010 (capture gives up after 1 second) (revision_history 2026-09-26, pre-approval) |
| F-22 | 2026-09-26 | requirements | judge:methods | spec-gap | Medium | REQ-LD-051 measures phase growth 'against the 4.1.0 snapshot', but Wave 3 (merged first by REQ-LD-057) also changes closures. The baseline for the 10% limit is ambiguous: 4.1.0, or post-Wave 3. (docs/spec/changes/living-docs/requirements.md:700) | closed | accepted:spec-gap REQ-LD-051 (baseline = snapshot of the implementation base after Wave 3) (revision_history 2026-09-26, pre-approval) |
| F-23 | 2026-09-26 | requirements | judge:methods | emergent | Medium | The spec-delta lists only 2 MODIFIED. Several requirements change the stated behaviour of living requirements that are marked only 'Builds on': the REQ-UP-008 catalogue (LD-049), the REQ-W3-061 registry (LD-053), QA-lite REQ-W2-017 (LD-032) and gates REQ-W2-034/039 (LD-050). If those enumerate thei… (docs/spec/changes/living-docs/spec-delta.md:10) | closed | rejected: REQ-UP-008, REQ-W3-061, REQ-W2-017, 034 and 039 define shape or rule, not an enumeration; their text does not change, so the requirements stay ADDED (reason recorded in spec-delta.md) |
| F-24 | 2026-09-26 | requirements | judge:methods | spec-gap | Medium | Several requirements join many behaviours in one SHALL. For example, REQ-LD-008 covers store, guard, verify, report and the cross-clone rule. REQ-LD-029 covers compare, report, stamp last_change, move to archive and drop from the map. Each has one error scenario, so most branches go untested. (docs/spec/changes/living-docs/requirements.md:429) | closed | accepted:spec-gap split keeping ids: 008→008+059, 029→029+062 (revision_history 2026-09-26, pre-approval) |
| F-25 | 2026-09-26 | requirements | judge:methods | spec-gap | Low | The REQ-LD-002 error scenario describes human workaround and tuning ('the human can repeat it', 'records the case as a miss'). It states no observable system outcome that a test could assert for a missed instruction. (docs/spec/changes/living-docs/requirements.md:90) | closed | accepted:spec-gap REQ-LD-002 error scenario now observable (no row, no acknowledgement) (revision_history 2026-09-26, pre-approval) |
| F-26 | 2026-09-26 | requirements | judge:methods | spec-gap | Low | The REQ-LD-043 error scenario ('reviewed sheet kept') repeats the success behaviour and is not an error condition. The REQ-LD-038 error scenario (no adapter for an engine family) tests a behaviour that is absent from its SHALL text. (docs/spec/changes/living-docs/requirements.md:605) | closed | accepted:spec-gap REQ-LD-043 error scenario replaced; REQ-LD-038 SHALL covers the missing adapter (revision_history 2026-09-26, pre-approval) |
| F-27 | 2026-09-26 | requirements | judge:methods | spec-gap | Low | Scenarios assert outputs that the SHALL text omits: REQ-LD-011 and REQ-LD-003 record ignored local values in the audit log, and REQ-LD-010 reports 'capture unavailable' at the next session start. Tests would verify behaviour that the requirement does not state. (docs/spec/changes/living-docs/requirements.md:206) | closed | accepted:spec-gap REQ-LD-003, 010, 011 SHALL text now states the audit and session outputs (revision_history 2026-09-26, pre-approval) |
| F-28 | 2026-09-26 | requirements | judge:methods | spec-gap | Low | The REQ-LD-037 error scenario masks an e-mail field name with {key} and continues. The SHALL text only says output 'SHALL pass the leak check', and does not say whether a failure masks or refuses. (docs/spec/changes/living-docs/requirements.md:533) | closed | accepted:spec-gap REQ-LD-037 (personal-data path segments masked; any other leak refuses the output) (revision_history 2026-09-26, pre-approval) |
| F-29 | 2026-09-26 | requirements | judge:methods | spec-gap | Low | The REQ-LD-021 error scenario reports 'values are not allowed, names only' for a settings value column. The SHALL text only says 'never a value' and defines no check that reports it. (docs/spec/changes/living-docs/requirements.md:336) | closed | accepted:spec-gap REQ-LD-021 (defaults only for non-secret settings; a value column is reported) (revision_history 2026-09-26, pre-approval) |
| F-30 | 2026-09-26 | requirements | judge:methods | spec-gap | Low | REQ-LD-004 measures the detector in only three languages, while REQ-LD-003 ships five. Recall and precision for two shipped languages have no testable threshold. (docs/spec/changes/living-docs/requirements.md:109) | closed | accepted:spec-gap duplicate of F-11 → REQ-LD-004 (revision_history 2026-09-26, pre-approval) |
| F-31 | 2026-09-26 | requirements | judge:methods | spec-gap | Low | REQ-LD-030 ends with the purpose clause 'so that the second change reconciles against the sheet as the first one left it', which is not testable. REQ-LD-055 uses the adjective 'neutral' and the success test 'finds the three parts', which have no measurable condition beyond the lint. (docs/spec/changes/living-docs/requirements.md:444) | closed | accepted:spec-gap REQ-LD-030 purpose clause removed; REQ-LD-055 made measurable (revision_history 2026-09-26, pre-approval) |
| F-32 | 2026-09-26 | architecture | judge:methods | spec-gap | High | A-06 adds a secret-assignment rule to the shared leak_patterns.json, so existing leak checks (e.g. the sponsor page) can refuse content that passed under 4.1. That breaks REQ-LD-054, and test_compat_ld only replays with map, capture and README off, so it would not catch this. (docs/spec/changes/living-docs/architecture.md:976) | closed | accepted:spec-gap architecture §1.4 + A-06 (capture-only leak group; sponsor page unchanged) (architecture revision 2026-09-26, pre-approval) |
| F-33 | 2026-09-26 | architecture | judge:methods | emergent | Medium | A-02 changes the prompt-event output of dispatch from plain text to one JSON object for every guard, including the approval guard, even with capture off. Yet the compatibility suite claims the 4.1 guard tables give unchanged results. One of the two statements is wrong, and the rollout effect on exi… (docs/spec/changes/living-docs/architecture.md:298) | closed | accepted:spec-gap architecture §1.5 + A-02 (JSON output only where capture is on; 4.1 output byte-identical) (architecture revision 2026-09-26, pre-approval) |
| F-34 | 2026-09-26 | architecture | judge:methods | spec-gap | Medium | REQ-LD-060 holds every gate of the project while an inbox entry waits, but the store is per clone (R-9). Another clone's gates pass, so the requirement is only enforced where the capture happened. The architecture documents this instead of mapping it to a requirement revision or a decision. (docs/spec/changes/living-docs/architecture.md:1112) | closed | accepted:spec-gap architecture A-19 (inbox per clone, recorded decision) + risk R-9 (architecture revision 2026-09-26, pre-approval) |
| F-35 | 2026-09-26 | architecture | judge:methods | spec-gap | Medium | The rationale is inconsistent. Upgrade step LD-1 marks turning capture on as human-only because it records the owner's words, but A-14 lets an agent task turn it on in the working copy. Doing that makes three blocking checks active on every in-flight change, prod gates included. (docs/spec/changes/living-docs/architecture.md:996) | closed | accepted:spec-gap architecture A-14 (consent from D-38 / REQ-LD-056; bounded effect by the active-change rule) (architecture revision 2026-09-26, pre-approval) |
| F-36 | 2026-09-26 | architecture | judge:methods | spec-gap | Medium | REQ-LD-051 names the loaders: the instruction contract through the core, components by 6 skills, README by 3. The architecture also lets karvey-iterate and karvey-context load rules/instructions.md, which the requirement does not list, and records no decision (options and reason) for it. (docs/spec/changes/living-docs/architecture.md:579) | closed | accepted:spec-gap architecture §1.18 (rules/instructions.md in no Load: list; reached through the core) (architecture revision 2026-09-26, pre-approval) |
| F-37 | 2026-09-26 | architecture | judge:methods | emergent | Medium | The upgrade-step fallback sends LD-1..LD-3 to a hand-off file plus a backlog item when the engine is missing from the base. REQ-LD-049 would then be unmet in 4.2.0 while the coverage matrix still claims it via test_upgrade_ld. No gate refuses release in that case. (docs/spec/changes/living-docs/architecture.md:901) | closed | accepted:spec-gap architecture §7.3, §7.1 (no hand-off fallback; engine required by the base check) (architecture revision 2026-09-26, pre-approval) |
| F-38 | 2026-09-26 | architecture | judge:methods | spec-gap | Low | The coverage plan has no test for karvey-init asking instructions.capture and readme.check with 'on' recommended (REQ-LD-011, 044). The matrix maps 011 only to the capture table and test_schema_ld, and 044 only to test_readme. (docs/spec/changes/living-docs/architecture.md:1022) | closed | accepted:spec-gap architecture §1.18 (L-78 checks the init settings text) (architecture revision 2026-09-26, pre-approval) |
| F-39 | 2026-09-26 | architecture | judge:methods | spec-gap | Low | LD-2 applies bootstrap sheets on the upgrade branch, but REQ-LD-040 and REQ-LD-047 require bootstrap writes on a docs branch. Which branch rule wins under the upgrade engine is not decided. (docs/spec/changes/living-docs/architecture.md:907) | closed | accepted:spec-gap architecture §1.15 (docs branch = neither integration nor production; upgrade branch qualifies) (architecture revision 2026-09-26, pre-approval) |
| F-40 | 2026-09-26 | architecture | judge:methods | spec-gap | Low | The component map adds fields that REQ-LD-019 does not list: exclude globs (A-15), sample, and components_settings.sheet_check. The requirement's field list does not trace back to them. No ripple to requirements is recorded. (docs/spec/changes/living-docs/architecture.md:372) | closed | accepted:bug architecture §1.9 (field traceability to REQ-LD-028, 035; exclude additive, A-15) (architecture revision 2026-09-26, pre-approval) |
| F-41 | 2026-09-26 | architecture | judge:methods | spec-gap | Low | REQ-LD-009 and REQ-LD-060 say an inbox entry is 'classified not-instruction'. The architecture uses a separate 'dismiss' subcommand, while the refusal text tells the user to 'assign it or classify it', which points to a command that does not exist for inbox entries. (docs/spec/changes/living-docs/architecture.md:327) | closed | accepted:spec-gap architecture §1.7 (inbox classified with instruction classify; dismiss dropped) (architecture revision 2026-09-26, pre-approval) |
| F-42 | 2026-09-26 | architecture | judge:methods | spec-gap | Low | REQ-LD-052 is covered only by test_contracts.py, a Wave 3 suite not in this branch, and REQ-LD-056 and 057 map to tasks rather than to a suite and level. Test coverage for these depends on external or sequencing artifacts. (docs/spec/changes/living-docs/architecture.md:854) | closed | accepted:spec-gap architecture §6.1, §11 (test_base_wave3; whole-repo gate for 056; Wave 3 suite in the base) (architecture revision 2026-09-26, pre-approval) |
| F-43 | 2026-09-26 | architecture | judge:methods | emergent | Low | Sheets added to the judges' closed inputs count against the existing closed-input budget. The design does not say what is dropped or refused when the budget is exceeded, so REQ-LD-031 could be silently weakened. (docs/spec/changes/living-docs/architecture.md:459) | closed | accepted:spec-gap architecture §1.12 (dropped sheets printed and repeated in the gate summary) (architecture revision 2026-09-26, pre-approval) |
| F-44 | 2026-09-26 | architecture | judge:security | spec-gap | High | A row with no store entry passes as 'not verifiable here' if its text matches its own hash. The agent can fabricate a row, or rewrite a row from another clone, by recomputing SHA-256 into the comment, so S-3's tamper evidence is bypassed. This contradicts REQ-LD-001's error scenario. (docs/spec/changes/living-docs/architecture.md:264) | closed | accepted:spec-gap architecture §1.4 (clone id in the row; same-clone rows must match the store; not-verifiable rows listed) (architecture revision 2026-09-26, pre-approval) |
| F-45 | 2026-09-26 | architecture | judge:security | spec-gap | High | REQ-LD-059 makes 'hash in the row matches its own text' enough to avoid refusal. That hash is attacker-computable, not a keyed MAC or a signature, so an agent-forged or agent-edited row is indistinguishable from a real row from another clone. (docs/spec/changes/living-docs/requirements.md:250) | closed | accepted:emergent risk R-10 (REQ-LD-059 keeps not-verifiable rows unrefused; listed to the owner) (architecture revision 2026-09-26, pre-approval) |
| F-46 | 2026-09-26 | architecture | judge:security | bug | High | vault: references are resolved through 'the vault CLI the project declares'. If that comes from an agent-editable project.json, it is a command-execution path from a file value. S-9 claims no agent-controlled value reaches a command, but no reviewed-line or allowlist control is named. (docs/spec/changes/living-docs/architecture.md:475) | closed | accepted:bug architecture §1.13 + A-20 (vault resolver allow-list of argv templates, reviewed line) (architecture revision 2026-09-26, pre-approval) |
| F-47 | 2026-09-26 | architecture | judge:security | spec-gap | High | The sampler's production guard trusts environments.production, hosts and components[].sample from project.json. Unlike capture (S-5), nothing says these come from the reviewed line, so the agent can flip production:false or drop hosts in the working copy and sample production. (docs/spec/changes/living-docs/architecture.md:376) | closed | accepted:spec-gap architecture §1.9 + A-21 (sampler settings from the reviewed line only) (architecture revision 2026-09-26, pre-approval) |
| F-48 | 2026-09-26 | architecture | judge:security | spec-gap | Medium | The production check is a denylist of declared prod hosts, so an undeclared or aliased prod host (or IP, CNAME) passes. It fails open. Tier 3 should use an allowlist of declared development hosts and refuse on any unparsable host. (docs/spec/changes/living-docs/architecture.md:477) | closed | accepted:spec-gap architecture §1.13 + A-21 (development host allow-list; unparsable host refused) (architecture revision 2026-09-26, pre-approval) |
| F-49 | 2026-09-26 | architecture | judge:security | spec-gap | Medium | The jsonl_export connection reference names a directory, so the host-based production guard cannot apply. A production export directory marked with a dev environment is sampled, and no control ties the directory to a non-production source. (docs/spec/changes/living-docs/architecture.md:469) | closed | accepted:spec-gap architecture §1.13 (export_paths allow-list) + risk R-13 (architecture revision 2026-09-26, pre-approval) |
| F-50 | 2026-09-26 | architecture | judge:security | bug | Medium | Keys are collapsed only when a level has more than 20 distinct keys or a key matches the e-mail or phone pattern. A map keyed by 20 or fewer customer or national IDs prints those IDs as path segments, violating REQ-LD-037's 'no key value'. (docs/spec/changes/living-docs/architecture.md:484) | closed | accepted:bug architecture §1.13 + A-22 (only declared or identifier-shaped segments printed) (architecture revision 2026-09-26, pre-approval) |
| F-51 | 2026-09-26 | architecture | judge:security | emergent | Medium | Redaction covers only secret patterns, e-mail and international phone numbers. Names, national IDs, local phone numbers, card numbers and free-form secrets ('password is x') land verbatim in tracked findings.md and are pushed to the remote. (docs/spec/changes/living-docs/requirements.md:151) | closed | accepted:emergent architecture §1.4 (card numbers, 'is' form) + risk R-2 (widened) (architecture revision 2026-09-26, pre-approval) |
| F-52 | 2026-09-26 | architecture | judge:security | spec-gap | Medium | Removal replaces the row text only in the working tree. The text stays in git history and on the remote after push, and the design names no purge or pre-push step, so removal gives no real erasure of PII or secrets that were captured. (docs/spec/changes/living-docs/requirements.md:222) | closed | accepted:spec-gap architecture §1.7 (remove names the commit holding the text) + risk R-11 (architecture revision 2026-09-26, pre-approval) |
| F-53 | 2026-09-26 | architecture | judge:security | spec-gap | Medium | The findings lock under <state>/locks is not in the protect-paths needles (only karvey/instructions is added), and stale-lock recovery is unspecified. An agent can leave a lock file so that every capture times out. Failed captures never block gates. (docs/spec/changes/living-docs/architecture.md:249) | closed | accepted:spec-gap architecture §1.4 (locks inside the protected dir; stale-lock recovery) (architecture revision 2026-09-26, pre-approval) |
| F-54 | 2026-09-26 | architecture | judge:security | spec-gap | Medium | The classification check compares the row cell with spec.json:instruction_log, and both are tracked files the agent can edit. Editing both consistently forges a classification. S-10 holds only if spec.json itself is protected from direct edits, and this design does not state that. (docs/spec/changes/living-docs/architecture.md:331) | closed | accepted:spec-gap architecture §1.4, §1.7 (protected store is the classification authority) (architecture revision 2026-09-26, pre-approval) |
| F-55 | 2026-09-26 | architecture | judge:security | spec-gap | Medium | status: reviewed lives in sheet front matter that the agent can edit directly. Nothing records that the review command set it, so the D-01 owner-only review can be bypassed. The bypass also shields the sheet from bootstrap regeneration. (docs/spec/changes/living-docs/architecture.md:520) | closed | accepted:spec-gap architecture §1.15 (review block + protected review log; hand-set reviewed treated as draft) (architecture revision 2026-09-26, pre-approval) |
| F-56 | 2026-09-26 | architecture | judge:security | bug | Low | Confirm phrases are matched in step 2, before strip_quoted in step 4. A quoted or pasted line such as '> remove instruction F-14' (punctuation-only surroundings) can write an owner marker from text the human did not author. (docs/spec/changes/living-docs/architecture.md:217) | closed | accepted:bug architecture §1.4 (confirm phrases matched after stripping quoted and pasted lines) (architecture revision 2026-09-26, pre-approval) |
| F-57 | 2026-09-26 | architecture | judge:security | bug | Low | Only \| and newlines are escaped in the text cell. Pasted text of 200 characters or less containing '<!-- karvey:capture sha256=... -->' can inject a second hash comment and confuse the verifier's parse of the row. (docs/spec/changes/living-docs/architecture.md:258) | closed | accepted:bug architecture §1.4 (comment delimiters escaped; only the cell-ending comment read) (architecture revision 2026-09-26, pre-approval) |
| F-58 | 2026-09-26 | architecture | judge:security | emergent | Low | The agent alone can dismiss inbox entries or classify rows not-instruction with a 10-character reason. The only control is that the gate summary shows them. An owner instruction can be dropped without the owner's words, unlike removal, which needs a confirm marker. (docs/spec/changes/living-docs/architecture.md:327) | closed | accepted:emergent risk R-12 (dismissals listed first at the gate) (architecture revision 2026-09-26, pre-approval) |
| F-59 | 2026-09-26 | architecture | judge:security | spec-gap | Low | The trust-boundary table omits two boundaries: project.json sample, environments and vault settings flowing into the sampler, and sheet front matter flowing into the review status. Neither has a named control at Tier 3. (docs/spec/changes/living-docs/architecture.md:673) | closed | accepted:spec-gap architecture §3 (two trust boundaries and controls S-11, S-12 added) (architecture revision 2026-09-26, pre-approval) |
