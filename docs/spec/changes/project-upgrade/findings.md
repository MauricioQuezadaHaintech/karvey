# Findings: project-upgrade

Triage inbox of this change (`plugins/karvey/skills/karvey/rules/iteration-loop.md`). `karvey-impl`, `karvey-test`,
`karvey-qa` and `karvey-browse` append observations here with a type guess; only `karvey-iterate` routes them.
Types: `bug` → incident `BUG-NN` in `docs/bugs_dev_testing.md` · `spec-gap` → re-open requirements or amend the
architecture · `emergent` → `docs/spec/backlog.md`.

Status: `open` → `routed` → `closed` (`deferred` = an emergent item captured with its reason, for the backlog).
Convergence needs no `open`/`routed` `bug` or `spec-gap`.

| # | Date | Source phase | Type | Severity | Title | Status | Routed to |
|---|------|--------------|------|----------|-------|--------|-----------|
| F-01 | 2026-09-25 | impl | spec-gap | low | Architecture §1.8 L-38 says "`since` ≤ the plugin version", but §1.6 / A-12 set every step's `since` to the working number 3.13.0 while `plugin.json` stays 3.11.4 until the release (the version moves only at `karvey-deploy`), so L-38 would fail the whole-repo lint for the entire impl. Smallest deviation (E1.F7.T1): a `since` newer than `plugin.json` is **one warning** naming the steps while `## [Unreleased]` holds entries, and an **error** once `[Unreleased]` is empty (at the release, where `karvey-deploy` must set `since` to the release). Unit test `test_lint_plugin.py: L38.test_since_newer_than_the_plugin`. The architecture §1.8 row should say so. | closed | architecture §1.8 L-38 row amended (rev. 2, karvey-iterate 2026-09-26) |
| F-02 | 2026-09-25 | impl | spec-gap | low | `legacy-shims` must diff an edited copy against the **shipped** shim and `enforcement-defaults` must read the plugin's `project.schema.json`, but the §1.4 `Probe` only reads under the project root and the home. Added `Probe.plugin_read` / `plugin_json` (read-only, confined to the plugin root); L-38 still forbids any direct I/O in step functions. §1.4 should list them. | closed | architecture §1.4 Probe row lists `plugin_read` / `plugin_json` (rev. 2) |
| F-03 | 2026-09-25 | impl | spec-gap | low | `tasks.md` E1.F5.T2 names `bash plugins/karvey/tests/test-hooks.sh`; the suite lives at `plugins/karvey/hooks/tests/test-hooks.sh` (the one CI runs). Ran that one. The same suite ran the session hook with the real `HOME`; it now uses an isolated `HOME` / `XDG_STATE_HOME`, because the startup offer may write its seen record under the state dir of a project outside git. | closed | E1.F5.T2 |
| F-04 | 2026-09-25 | impl | emergent | low | The fingerprint `upgrade-surface.json` is recorded for release 3.11.4 (the top CHANGELOG release, hashed from the `origin/main` tree of that release), so L-37 warns during `[Unreleased]` with every surface file wave1 and this change touched. If 3.12.0 is released before this branch merges, the merge must re-record the fingerprint from the 3.12.0 tree (not `surface --write` on the merged tree, which would hide this change's own surface edits from the release check); otherwise L-37 errors (top release newer than the fingerprint). | deferred | deferred (emergent, reason: it is a release action, not code of this change) → karvey-deploy of the release that ships it; orchestrator moves it to the backlog |
| F-05 | 2026-09-25 | test | bug | medium | E2E Flow 1: the upgrade commit title, the PR title/body and `plan` read `chore(karvey): project upgrade 3.11.4 → 3.11.4`. The skill records `seen --accept` for the installed version **before** `apply`/`commit`, and the journal (and `plan`, and a repeated `write_seen`) took `from` from the record's `version`, which is by then the installed one. Fix: `upgrade.upgraded_from(rec, installed)` — the record's own `from` once it already resolves the installed version; `none → <v>` on a first upgrade. Regression: `test_upgrade_cli.RegressionProjectUpgradeFromVersion` (`regression_project-upgrade_from_after_accept`, 3 tests, FAIL before the fix). Re-run: `none → 3.11.4`. | closed | fixed in test (`karvey_lib/upgrade.py`) |
| F-06 | 2026-09-25 | test | bug | medium | E2E Flow 1: the skill's step 10 `UB=$(git symbolic-ref --short HEAD); git push -u origin "$UB"` is blocked by Karvey's own prod-gate (`the push destination cannot be resolved; rewrite without variables`); the agent recovered by hand. Fix: the skill keeps the `branch` returned by `branch --json` and writes the push and the PR commands with the literal branch name. Regression: `test_upgrade_cli.RegressionProjectUpgradeSkillPush` (`regression_project-upgrade_skill_push_literal`: every `git push` in the skill has no `$` and passes the prod-gate on an upgrade branch; the old line is blocked). | closed | fixed in test (`skills/karvey-upgrade/SKILL.md`) |
| F-07 | 2026-09-25 | test | bug | low | Skill text: after a decline the agent said "the offer will come back next session" (it comes back with the next Karvey version), and at the dry-run it summarised the diffs instead of showing them. Fix: step 4 says what a decline lasts; step 7 says "unified diff **verbatim**, one block per step". Regression: `test_upgrade_cli.RegressionProjectUpgradeSkillWording` (`regression_project-upgrade_skill_wording`). Re-run: diffs shown verbatim. | closed | fixed in test (`skills/karvey-upgrade/SKILL.md`) |
| F-08 | 2026-09-25 | test | spec-gap | low | The offer is per clone (D-20), but the upgrade branch name `chore/karvey-upgrade-<v>` is per version: a second clone (another person, or the same person elsewhere) builds its own branch with the same name, and its push is rejected as non-fast-forward because the first clone's branch is already on the remote (observed in the E2E re-run; the skill handled it per REQ-UP-018's error scenario: commit local, retry command). No requirement says what `branch` does when `origin/chore/karvey-upgrade-<v>` already exists (E-11 covers only a local branch). Options: base on the remote upgrade branch and tell the person a PR may already be open, or add a clone-unique suffix. | closed | REQ-UP-013 rev. 2 (option: base on the remote upgrade branch, say a PR may be open) · architecture §1.5, §1.7, E-11 · `branch_base`, skill step 6 · regression `test_upgrade_steps.RegressionProjectUpgradeIterate.test_f08_…` |
| F-09 | 2026-09-25 | test | emergent | low | In one E2E session whose prompt said "do not run any tool", the model called the offer's instruction ("Ask ONE question … → the decline command above") a prompt injection and did not ask. In the four sessions without that restriction it asked normally. The offer is imperative text naming a command; a softer framing (a notice from the installed plugin, the command as an option for the person) may read less like an injected instruction. | closed | fixed (emergent, cheap): the offer reads as the plugin's notice, like the settings notice ("the user can …", no imperative order naming a command) · architecture §1.2 step 6 · regression `test_karvey_hooks.UpgradeOffer.test_f09_…` |
| F-10 | 2026-09-25 | test | bug | low | `test_lint_plugin.L39` subclassed `L38`, so the 9 L-38 tests ran twice (again under `L39`). Fix: a shared base `UpgradeMiniPlugin` with the mini-plugin setUp; `L38` and `L39` both derive from it. Regression: the test count itself (`L39` runs its 7 own tests only; `python3 -m unittest -v test_lint_plugin \| grep -c '\.L39\.'` → 7). | closed | fixed in test (`tests/unit/test_lint_plugin.py`) |
| F-11 | 2026-09-25 | qa | bug | high | D2/D7: `legacy-shims` globbed `.claude/**/plan-gate.sh`, entering nested worktrees (`.claude/worktrees/*`): it deleted another worktree's shim (outside the dirty-tree check) and `commit` then failed on the pathspec. Fix: only `.claude/hooks/<shim>`; `Probe.glob` never lists a file under a directory holding its own `.git`. Regression `test_upgrade_steps.RegressionProjectUpgradeQA.test_f11_nested_worktree_is_never_touched`. | closed | fixed in qa micro-loop |
| F-12 | 2026-09-25 | qa | bug | high | D2/D7: a git-ignored `.claude/settings.local.json` with a shim entry was rewritten; `git add` refused it on every retry and left the index half staged. Fix: ignored settings are a shown diff (warning, or a `human` result when nothing else is left), `commit` refuses ignored files before staging and resets the index when `git add` fails. Regression `…test_f12_ignored_local_settings_are_shown_not_written`. | closed | fixed in qa micro-loop |
| F-13 | 2026-09-25 | qa | bug | high | D2/D7: the journal was never trimmed after a commit and outlived its branch: a second commit after a deletion failed (`pathspec … did not match`), repeated the first commit's steps, and a later upgrade to the same version reused a merged branch's journal. Fix: `commit` moves the entry to `committed` and resets `applied`/`files`; `ensure_branch` starts a new journal when it creates the branch. Regressions `…test_f13_second_commit_after_a_deletion`, `…test_f13_a_new_upgrade_branch_starts_a_new_journal`. | closed | fixed in qa micro-loop |
| F-14 | 2026-09-25 | qa | bug | medium | D7: a failure inside `legacy-shims` after the delete left the plan gate off and the re-plan said "already satisfied". Fix: edits in the order flag → settings → delete; an entry left pointing at a missing shim is `applies`. Regression `…test_f14_the_flag_is_written_first_and_a_dangling_entry_is_work`. | closed | fixed in qa micro-loop |
| F-15 | 2026-09-25 | qa | bug | medium | D2: `read_journal` checked only `files` (a missing `applied` raised KeyError after the files were written, exit 5); an unwritable journal escaped as a traceback with the tree dirty. Fix: the whole shape is checked (missing lists default), the journal is written before the first file and later write failures are reported with exit 1. Regression `…test_f15_bad_journal_shape_and_unwritable_journal`. | closed | fixed in qa micro-loop |
| F-16 | 2026-09-25 | qa | bug | low | D1/D2: `--values` could set any `notifications.*`/`management.*` key, a `{change-id}` location skipped `check_location`, and a list for `notifications.channel` raised TypeError (exit 5). Fix: only the proposal's keys, strings (or a list of strings), location checked with a sample id, `_run_fix` maps TypeError/ValueError/KeyError to a refusal. Regression `…test_f16_value_types_and_keys`. | closed | fixed in qa micro-loop |
| F-17 | 2026-09-25 | qa | bug | low | D1/D7: `apply` created and checked out the upgrade branch before its refusals (not applicable, stale preview) and for human/report-only picks, so "refused, nothing changed" was false. Fix: refusals are decided on the current tree first; a refusal after the switch goes back and removes a branch it just created; no branch when nothing is written. Regression `…test_f17_a_refusal_leaves_the_branch_as_it_was`. | closed | fixed in qa micro-loop |
| F-18 | 2026-09-25 | qa | bug | medium | D3/D4: `changes-in-flight` reported the approval the change's current phase is waiting for as an unmet gate, so any project with active work got the offer every version (this repo: `project-upgrade (phase qa): unmet gate qa`). Fix: the current phase's own gate is skipped. Regression `…test_f18_the_current_phase_awaiting_its_approval_is_not_unmet`. | closed | fixed in qa micro-loop |
| F-19 | 2026-09-25 | qa | bug | medium | D1: `_strip_hook_entries` removed any command containing `plan-gate.sh`, e.g. a person's own `~/.claude/hooks/my-plan-gate.sh.v2`. Fix: only a command running the project copy `.claude/hooks/<shim>` (relative or under `$CLAUDE_PROJECT_DIR`). Regression `…test_f19_an_own_hook_with_a_similar_name_is_kept`; also `…test_git_read_refuses_writing_options` (D1: `git show --output=<file>` was allowed; `show` dropped, `--output`/`--exec`/`-c` refused). | closed | fixed in qa micro-loop |
| F-20 | 2026-09-25 | qa | bug | medium | D1 (A03) / D3: the skill put the person's words into bash (`--answer "<their words>"`, `printf '…' > "$VALUES"`, a double-quoted `--body` with backticks in `pr_body`); the values redirect is also blocked by the plan-gate; and `get branch_flow.integration … \|\| exit 1` aborted where the tool falls back to `origin/HEAD`. Fix: `--values -`, `--answer-file -` (quoted heredocs), `--pr-body-file` (outside the repo) + `gh pr create --body-file`; the fetch tolerates an unset key. Regressions `test_upgrade_cli.RegressionProjectUpgradeStdinValues` (2 tests). | closed | fixed in qa micro-loop |
| F-21 | 2026-09-25 | qa | spec-gap | medium | D2/D3: REQ-UP-023 makes "no Karvey statusline" a `human` result, so for the usual person without a statusline the plan is never empty and REQ-UP-005's "no offer when nothing applies" never happens (the offer returns every version). Options: "no statusline" → `nothing` with a note, or optional human hints do not count for the hook. | closed | REQ-UP-023 rev. 2 (option: no statusline → `nothing` with a note) · architecture §1.6 row 6, E-20 · regressions `…Iterate.test_f21_…`, `HumanSteps.test_no_statusline_is_a_note_not_work` |
| F-22 | 2026-09-25 | qa | spec-gap | medium | D7: `schema-migrate` rewrites archived changes' `spec.json` (the E2E commit touched `archive/2026-01-10-fixture-61/spec.json`); architecture §1.6 says "archive included", E-22 says pre-3.12 history stays untouched (D-14). The spec contradicts itself. | closed | REQ-UP-020 rev. 2 (option: `docs/spec/changes/archive/` never migrated) · architecture §1.6 row 1, E-22 · regression `…Iterate.test_f22_…` |
| F-23 | 2026-09-25 | qa | spec-gap | medium | D7: a project created by `karvey-init` gets the offer on its first session because init does not write `enforcement.plan_marker_ttl_min` (an `x-karvey-default` key `enforcement-defaults` adds); ss-30 adds the key by hand. | closed | REQ-UP-026 rev. 2 (option: karvey-init writes every enforcement default) · `skills/karvey-init/SKILL.md` Step 3.5 · regression `…Iterate.test_f23_…` (ties the init block to the schema defaults) |
| F-24 | 2026-09-25 | qa | spec-gap | low | D7: a dry-run run on a local integration branch that differs from `origin/<integration>` previews a tree that is not the one `apply` changes (the upgrade branch is based on the remote ref); the refusal then says "the tree changed". The skill runs `branch` before the dry-run, but the tool does not require it. | closed | REQ-UP-012 rev. 2 (option: dry-run refused off the upgrade branch when the tree differs from its base, naming `branch`) · architecture §1.4 step 8, E-28 · regression `…Iterate.test_f24_…` |
| F-25 | 2026-09-25 | qa | spec-gap | low | D1/D7: the probe has no size cap on project reads, `Probe.glob` does not check the deadline while walking (`node_modules` is filtered, not pruned) and the session hook has no watchdog: a huge committed file or tree can exceed the 1.5 s budget up to the 10 s hook timeout, losing the whole startup context. | closed | REQ-UP-006 rev. 2 (option: read cap 2 MiB, pruned deadline-checked walk, hook watchdog) · architecture §1.2, §1.4, E-07 · regressions `…Iterate.test_f25_…`, `test_karvey_hooks.UpgradeOffer.test_f25_…` |
| F-26 | 2026-09-25 | qa | spec-gap | low | D1/D2: L-38's AST scan misses `Path.open("w")`, `os.open`, import aliases (`import os as o`, `from os import remove`) and write functions reachable through `probe.state` / `probe.config`, so "checks are read-only by construction" is only partly enforced. | closed | REQ-UP-031 rev. 2 (option: aliases, `.open()` modes, `os.open`, state/config allow-list) · architecture §1.8 · regression `test_lint_plugin.L38.test_f26_…` (10 cases) |
| F-27 | 2026-09-25 | qa | spec-gap | low | D1: in a Karvey project outside git, the `empty` record and `seen --decline/--accept` go to the XDG state dir under `$HOME` (`~/.local/state/karvey/<hash>/`), while REQ-UP-016 says nothing under the home is written; protect-paths does not cover that path. | closed | REQ-UP-003, REQ-UP-016 rev. 2 (option: outside git no offer and no record) · architecture §1.2, §1.3, E-12 · regressions `test_karvey_hooks.UpgradeOffer.test_f27_…`, `test_upgrade_seen.SeenCli.test_f27_…` |
| F-28 | 2026-09-25 | qa | emergent | low | D1: strings read from the project (a `phase`, a directory name) reach the plan/apply text the agent relays "as returned" without control-character stripping; a crafted value can inject lines into the table. | closed | fixed (emergent, cheap): `upgrade.one_line` escapes control characters in plan rows and report labels · architecture E-29 · regression `…Iterate.test_f28_…` |
| F-29 | 2026-09-25 | qa | emergent | low | D3: duplicated helpers — `current_branch`/`_origin_head` copies of `project`/`karvey_hooks`, four git runners with different error handling, two path normalisers, the release-number check repeated 4 times; `surface_status` docstring names a key it never returns. | deferred | deferred (emergent, reason: a refactor of shared git helpers across `project`/`karvey_hooks`/`upgrade` touches guard code outside this change's risk budget; no behaviour defect) — the `surface_status` docstring was fixed now; orchestrator moves the rest to the backlog |
| F-30 | 2026-09-25 | qa | bug | low | D3/D6: company names in new public text (architecture §7 item 3, §12), the README's `python3 plugins/karvey/scripts/karvey-upgrade.py plan` only works inside the plugin repo, `hooks/README.md` said "any error prints one line" (a failing check still offers), and the test-phase CHANGELOG line lacked "Responsible · AI". Fixed in the text; guarded by the existing lint (L-39) and the review. | closed | fixed in qa micro-loop |
| F-31 | 2026-09-26 | qa | bug | medium | D1 (QA re-run of rev. 2): F-28 stopped at the plan; the dry-run summary, the diff headers and the report lines still printed project paths raw (a directory name with a newline added a line to what the skill shows verbatim). | closed | fixed: `one_line` at the report boundary (summaries, warnings, failed checks, applied/failed lines), `printable_block` for instructions and diff bodies, diff header paths on one line · regression `…Iterate.test_f28_…` (dry-run half, red before) |
| F-32 | 2026-09-26 | qa | bug | medium | D1 (re-run): `branch` took any `origin/chore/karvey-upgrade-<v>` as the base, even an unrelated (orphan) branch, and checked it out silently. | closed | fixed: taken only when it builds on the integration branch (`merge-base --is-ancestor`), else refused before any checkout; `remote_commits` / `remote_files` listed and shown by the skill · regressions `…test_f08_a_remote_upgrade_branch_not_built_on_integration_is_refused`, `…test_f08_the_remote_commits_are_listed_for_the_person` |
| F-33 | 2026-09-26 | qa | bug | low | D1/D4 (re-run): the skill's fetch of the remote upgrade branch did not follow a force-push, did not drop a branch deleted on the remote, and hid errors. | closed | fixed: `git fetch --prune origin '+refs/heads/chore/karvey-upgrade-<v>*:…*'` (pattern: no error when absent) · regression `test_upgrade_cli.RegressionProjectUpgradeSkillFetch` (also passes Karvey's pre-bash guards) |
| F-34 | 2026-09-26 | qa | bug | low | D2 (re-run): the rewritten `Probe.glob` no longer followed symlinked directories (the old `Path.glob` did), so a symlinked `.claude/hooks` read as "nothing" and could record `empty`. | closed | fixed: symlinked directories are followed while they resolve inside the root, once each; links out of the tree are never listed · regression `…test_glob_follows_a_symlinked_directory_inside_the_root` |
| F-35 | 2026-09-26 | qa | bug | medium | D1/D3 (re-run): L-38 still missed `io.open`/`codecs.open` (the mode was read at the wrong position), `Path.rename`, `os.exec*`/`spawn*`, star imports, a `p = probe` alias and `getattr(probe.state, …)`. | closed | fixed, 6 more cases in `test_lint_plugin.L38.test_f26_…` (red before) plus a read-only `io.open` that must pass. Residual, accepted: `.replace()` (indistinguishable from `str.replace`) and a computed `getattr` name on the probe itself; L-38 is defence in depth over a reviewed file |
| F-36 | 2026-09-26 | qa | bug | low | D4 (re-run): a local upgrade branch behind the pushed remote one was only switched to, so this clone's push was again non-fast-forward (the F-08 case in the other order). | closed | fixed: fast-forwarded when strictly behind (clean tree), `remote: true` · regression `…test_f08_a_local_upgrade_branch_behind_the_remote_one_is_fast_forwarded` |
| F-37 | 2026-09-26 | qa | spec-gap | low | D4 (re-run): the skill's new "outside git → stop" rule had no signal from the tool (`computed_on` is null on a detached HEAD too). | closed | closed: `plan --json` carries `in_git` (architecture §1.5 JSON, skill step 2) · regression `…test_plan_json_says_whether_the_project_is_in_git` |
| F-38 | 2026-09-26 | qa | bug | low | D4 (re-run): `test-hooks.sh` still called `write_seen` outside git (a traceback in the output), and the outside-git refusal carried a `[karvey]` prefix no other refusal has. | closed | fixed: the helper tolerates the refusal (comment updated); prefix removed · the suite's output is clean |
| F-39 | 2026-09-26 | qa | bug | low | D3 (re-run): stale text — karvey-init said "every default" (the test and REQ-UP-026 mean `enforcement`), architecture's Probe allow-list still named `show`, A-08 and E-11 predated F-08, tasks E1.F3.T4 still said no statusline → `human`, spec-delta omitted REQ-UP-005's trace, the hooks README anchored the outside-git sentence to unrelated cases, the watchdog docstring misnamed the writer. | closed | fixed in the text |
