# E2E — offer → accept → PR on a throw-away repo (E1.F8.T2)

- **Date:** 2026-09-25, 13:28–13:43 UTC
- **Executed by:** maintainer agent, headless under D-21 (the owner's standing instruction to proceed without
  questions), the way the wave1 manual scripts were run under D-19.
- **Plugin:** branch `feature/project-upgrade` (installed version `3.11.4`), started with
  `claude -p --plugin-dir <worktree>/plugins/karvey --setting-sources project,local --dangerously-skip-permissions`
  (CLI 2.1.282). `--setting-sources project,local` keeps the user's own settings out; the legacy project's
  `.claude/settings.json` (with its copied plan-gate shim) is loaded, as in a real legacy clone.
- **Home:** `HOME=$SCR/home`, a copy of `tests/fixtures/upgrade/fake-home` (a Karvey statusline on a versioned
  plugin path). Credentials reached the CLI through the environment only; nothing was written in the real home.
- **Repository:** `$SCR/proj`, a copy of `tests/fixtures/upgrade/legacy-project`, `git init -b dev`, one commit,
  branches `dev` and `main`, pushed to a bare origin `$SCR/origin.git` (`origin/HEAD → dev`). `$SCR` is a
  throw-away scratch directory, deleted afterwards with the session transcripts.
- **Answers:** a headless session cannot use AskUserQuestion (the agent looked it up, found none and asked in
  text), so every answer is the next turn, sent with `claude -p --resume <session>`. `SessionStart` then fires
  with `resume`, which is also the ss-26 check (no second offer on resume).

Reference plan before the session (`python3 <plugin>/scripts/karvey-upgrade.py plan --json`, exit 0):
`schema-migrate` applies · `schema-migrate-proposed` nothing · `legacy-shims` applies · `team-settings` applies ·
`enforcement-defaults` applies · `statusline-launcher` human · `global-config` nothing · `changes-in-flight` report.
`seen --show` → `no upgrade resolved yet in this clone`.

## Flow 1 — offer → accept → pick → dry-run → apply → commit → push → PR offered

| Step | Action (prompt) | Observed | Status |
|---|---|---|---|
| 1 | new session: "Hi. Start the session as you normally would …" | `SessionStart:startup` context carries `Karvey (upgrade): installed 3.11.4, no upgrade resolved yet in this clone — … to decline: python3 <plugin>/scripts/karvey-upgrade.py seen --decline` and `Ask ONE question … "Karvey → 3.11.4: do you want a plan to upgrade this project?"`. The agent asks it with the two options. After the turn: no `.git/karvey/` directory, `seen --show` → none (unanswered = nothing recorded, REQ-UP-001) | ✅ |
| 2 | resume: "1 — yes, show me the plan." | `SessionStart:resume` output empty (no offer). The agent loads `karvey:karvey-upgrade`, checks the tool, runs `plan --json` and shows 6 rows in the tool's order with its summaries; `schema-migrate-proposed`, `global-config` on the "already satisfied" line; warnings relayed; `schema-migrate`, `team-settings`, `enforcement-defaults` marked Recommended, `legacy-shims` (medium) not; `statusline-launcher` and `changes-in-flight` "shown, not applied" | ✅ |
| 3 | resume: "recommended" | `seen --accept --json` → `{"version": "3.11.4", "resolution": "accepted", "from": null}`; `git fetch origin dev`; `branch --json` → `{"branch": "chore/karvey-upgrade-3.11.4", "base": "refs/remotes/origin/dev", "created": true}`; `apply --steps "schema-migrate,team-settings,enforcement-defaults" --dry-run --json` → preview `4ae53365…`; one question "Apply these changes?". The diffs were **summarised**, not shown verbatim (F-07) | ✅ (F-07) |
| 4 | resume: "Yes, apply them." | `apply … --preview 4ae53365… --json` → `applied: [schema-migrate, team-settings, enforcement-defaults]`, 3 files; `commit --picked-by "E2E Tester" --answer "recommended" --json` → `dd83cc6`. Message: `chore(karvey): project upgrade 3.11.4 → 3.11.4` / `Steps: schema-migrate, team-settings, enforcement-defaults` / `Picked-by: E2E Tester` / `Picked-at: 2026-09-25T13:30:26+00:00` / `Answer: "recommended"`. **The title reads `3.11.4 → 3.11.4`** (F-05). `dev` has no new commit | ❌ → fixed (F-05) |
| 5 | resume: "Yes, go ahead: push and open the PR." | The skill's own line `git push -u origin "$UB"` was **blocked by Karvey's prod-gate**: `[karvey] prod-gate BLOCK change=? missing=target reason=cannot verify the production approval: the push destination cannot be resolved; rewrite without variables` (F-06). The agent rewrote it with the literal branch and pushed: `* [new branch] chore/karvey-upgrade-3.11.4`. `gh pr create --base dev --head chore/karvey-upgrade-3.11.4 …` → exit 4 (`gh auth login`; the origin is a local path) → the agent printed the exact `gh pr create` command with `pr_title` / `pr_body` and merged nothing | ✅ PR offered, ❌ → fixed (F-06) |

Afterwards: the bare origin has `chore/karvey-upgrade-3.11.4`; `git log -1 --format=%B` there shows `Steps:`,
`Picked-by:`, `Picked-at:`; `audit.log` holds `upgrade.seen accepted`, three `upgrade.apply applied`,
`upgrade.commit dd83cc6…`, and the prod-gate block.

## Flow 2 — a second session in the same clone: no offer

| Step | Action | Observed | Status |
|---|---|---|---|
| 1 | new session on `dev`: "Quote verbatim every line … 'Karvey (upgrade)' … or answer NONE" | `SessionStart:startup` context = the settings notice only; the agent answers `NONE`; `seen --show` → `accepted 3.11.4` | ✅ |

## Flow 3 — decline in a fresh clone; no offer until the version changes

| Step | Action | Observed | Status |
|---|---|---|---|
| 1 | `git clone -b dev $SCR/origin.git $SCR/clone2`, new session | no `.git/karvey/`; the offer is shown and asked | ✅ |
| 2 | resume: "2 — not for this version." | `seen --decline` → `recorded: declined 3.11.4`; `git branch` → `dev` only; tree clean | ✅ |
| 3 | new session, "quote … or NONE" | settings notice only; `NONE` | ✅ |
| 4 | edit `.git/karvey/seen-version`: `"version": "3.11.4"` → `"3.11.3"` (simulated update), new session | `Karvey (upgrade): installed 3.11.4, last resolved in this clone 3.11.3 …` and `"Karvey 3.11.3 → 3.11.4: …"` are back. In that session, told not to run any tool, the agent called the offer's instruction a prompt injection and did not ask (F-09) | ✅ (F-09) |

## The manual skill script (`tests/manual/upgrade-skill.md`)

All 6 cases PASS in their own clone (`$SCR/up/proj`): see `upgrade-skill-2026-09-25.md`.

## Re-run after the fixes (F-05, F-06, F-07)

`$SCR/clone3`, a fresh clone of the same origin, three turns: `/karvey:karvey-upgrade` → "The recommended ones."
→ "Yes, apply them, commit, push and open the PR."

| Check | Observed | Status |
|---|---|---|
| dry-run diffs shown verbatim | one `diff` block per step, exactly the tool's `diffs` | ✅ |
| commit title | `chore(karvey): project upgrade none → 3.11.4` (was `3.11.4 → 3.11.4`) | ✅ |
| push line from the skill | `git push -u origin chore/karvey-upgrade-3.11.4`, no prod-gate block (none in `audit.log`) | ✅ |
| push rejected (REQ-UP-018 error) | the origin already held Flow 1's `chore/karvey-upgrade-3.11.4` → non-fast-forward; the commit stays local; the agent reports it with the exact retry command, and neither force-pushes nor merges | ✅ (F-08) |

## Result

**PASS** after the fixes. Findings: F-05 (bug, fixed), F-06 (bug, fixed), F-07 (bug in the skill text, low,
fixed), F-08 (spec-gap: two clones produce the same upgrade branch name on the remote), F-09 (emergent: the
offer's wording can read as an injected instruction). Regression tests:
`test_upgrade_cli.RegressionProjectUpgradeFromVersion`, `RegressionProjectUpgradeSkillPush`,
`RegressionProjectUpgradeSkillWording`.

Clean-up: the scratch directory (repos, fake home, transcripts written under the fake home) deleted; no
transcript of these sessions under the real home.
