# Rule: project configuration

```json
{
  "project": "Name",
  "branch_flow": {"feature_prefix": "feature/", "integration": "dev", "production": "main"},
  "management": {"tool": "markdown", "location": "docs/spec/changes/{change-id}/PLAN.md"}
}
```
