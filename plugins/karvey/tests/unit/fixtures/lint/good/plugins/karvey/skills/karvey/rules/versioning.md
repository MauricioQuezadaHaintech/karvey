# Rule: versioning

Each release increments the version once; impl adds lines under `[Unreleased]`.

## Verified by QA (Dimension 6)

- `[Unreleased]` holds a line per task.
- The front reads its version from the **version file**.
