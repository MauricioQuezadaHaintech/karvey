# Rule: phase close

Run by `karvey-requirements` at its close.

Status changes per task; the close comment and the cascade run per Feature (the cascade is defined in the management adapters rule[^r-ma]).

[^r-ma]: management-adapters.md — context only, not opened.
