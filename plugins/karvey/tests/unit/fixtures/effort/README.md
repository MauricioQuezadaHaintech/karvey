Effort fixtures (wave3 E1.F3.T3): two session transcripts with per-message `usage` records. `transcript-a`
repeats message `msg-1` on two lines (it counts once) and ends with a malformed line; `transcript-b` is a new,
shorter transcript. Capture files are built by the tests themselves (their `at` and root key vary per case).
