# Large adapter

This adapter is longer than the small one, so the worst case of a placeholder load picks it and the best case
picks the small one. Two more sentences make the difference obvious. The end.
