# Budget fixture project

A small Karvey project on which `karvey-context-budget.py measure` runs the session hook to size what it prints
(the `session_hook` row, architecture §1.3). Neutral sample data only.
